---
phase: "03-concurrent-workspace-safety"
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/quantum_runtime/cli.py
  - src/quantum_runtime/errors.py
  - src/quantum_runtime/runtime/contracts.py
  - src/quantum_runtime/runtime/exit_codes.py
  - src/quantum_runtime/runtime/observability.py
  - tests/test_cli_workspace_safety.py
autonomous: true
requirements:
  - RUNT-02
must_haves:
  truths:
    - "Agents and CI receive stable machine-readable reasons when workspace mutation is blocked or recovery is required."
    - "Conflict output includes enough holder or file-state metadata for automated retry or escalation."
    - "The existing schema envelope stays compatible while Phase 3 adds workspace-safety details."
    - "CLI JSON/text error mapping consumes concrete workspace-safety exceptions instead of inferring these cases from generic `ValueError` text."
  artifacts:
    - path: "src/quantum_runtime/errors.py"
      provides: "Concrete workspace-safety exception types on the real runtime error surface."
      contains: "`WorkspaceConflictError` and `WorkspaceRecoveryRequiredError` carrying stable codes plus metadata"
    - path: "src/quantum_runtime/runtime/contracts.py"
      provides: "Structured conflict and recovery payload models for workspace safety."
      contains: "schema-versioned workspace safety error payloads"
    - path: "src/quantum_runtime/runtime/observability.py"
      provides: "Workspace conflict and recovery reason/gate builders."
      contains: "machine-readable reason codes for shared-workspace failures"
    - path: "tests/test_cli_workspace_safety.py"
      provides: "CLI JSON regression coverage for workspace conflict and recovery outputs."
      contains: "payload shape and exit behavior assertions"
  key_links:
    - from: "src/quantum_runtime/cli.py"
      to: "src/quantum_runtime/errors.py"
      via: "explicit exception handling for workspace safety failures"
      pattern: "except WorkspaceConflictError|except WorkspaceRecoveryRequiredError"
    - from: "src/quantum_runtime/cli.py"
      to: "src/quantum_runtime/runtime/contracts.py"
      via: "JSON error serialization"
      pattern: "_json_error|dump_schema_payload"
    - from: "src/quantum_runtime/runtime/observability.py"
      to: "workspace conflict metadata"
      via: "reason and gate block construction"
      pattern: "workspace_conflict|workspace_recovery_required"
---

<objective>
Define the machine-readable public surface for shared-workspace conflicts and interrupted-write recovery before wiring the runtime write path to it.

Purpose: Concurrent safety is only usable for agents and CI if lock conflicts and recovery-required states are emitted as stable structured payloads instead of ad hoc text.
Output: Schema-versioned workspace safety payloads, deterministic exit handling, and CLI-level tests that lock the contract before runtime integration.
</objective>

<execution_context>
@/Users/xizhao/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xizhao/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md

@src/quantum_runtime/cli.py
@src/quantum_runtime/errors.py
@src/quantum_runtime/runtime/contracts.py
@src/quantum_runtime/runtime/observability.py

Depends on Phase 3 plan outputs:
- Plan 01 provides the workspace lease metadata and interrupted-write detection this plan must serialize for agents and CI.

Phase 3 constraints to preserve:
- keep output schema-versioned and agent-friendly
- preserve current alias/history behavior; only add conflict and recovery signaling
- do not include policy gates, bundles, docs, or broader architecture cleanup
- use the existing real exception surface in `src/quantum_runtime/errors.py` rather than inventing anonymous runtime-only failure markers
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the CLI contract for workspace conflict and recovery signals</name>
  <files>tests/test_cli_workspace_safety.py</files>
  <behavior>
    - Test 1: JSON mode maps a held workspace lease to a stable `workspace_conflict` reason with holder metadata and retry-safe guidance.
    - Test 2: JSON mode maps interrupted-write recovery findings to a stable `workspace_recovery_required` reason with leftover file paths and the last known valid revision when available.
    - Test 3: `cli.py` catches concrete `WorkspaceConflictError` and `WorkspaceRecoveryRequiredError` exceptions from `src/quantum_runtime/errors.py` and emits the same contract in both JSON and concise text modes.
  </behavior>
  <action>Create `tests/test_cli_workspace_safety.py` by monkeypatching CLI entry points to raise the real concrete exceptions from `src/quantum_runtime/errors.py` rather than fake `ValueError` strings. Assert the exact reason codes, required metadata fields, and deterministic exit behavior for JSON consumers, and assert text mode still surfaces concise actionable wording for humans. Keep the tests focused on workspace safety only; do not expand the contract to policy or bundle domains.</action>
  <verify>
    <automated>PYTHONPATH=src ./.venv/bin/python -m pytest tests/test_cli_workspace_safety.py -q</automated>
  </verify>
  <done>The CLI-level contract is defined by failing tests before implementation, covering both active conflict and recovery-required states through the real workspace-safety exception types.</done>
</task>

<task type="auto">
  <name>Task 2: Add structured workspace-safety payloads and CLI mapping</name>
  <files>src/quantum_runtime/errors.py, src/quantum_runtime/runtime/contracts.py, src/quantum_runtime/runtime/observability.py, src/quantum_runtime/runtime/exit_codes.py, src/quantum_runtime/cli.py</files>
  <action>Extend `src/quantum_runtime/errors.py` with concrete workspace-safety exceptions on the real runtime exception surface, for example `WorkspaceConflictError` and `WorkspaceRecoveryRequiredError` as `StructuredQuantumRuntimeError` subclasses carrying stable `code` values plus lock-holder or leftover-artifact metadata. Add typed payload helpers for those failures that fit the existing schema envelope. Then update `src/quantum_runtime/cli.py` to import and explicitly catch those concrete exceptions, serializing them through `ErrorPayload` and observability helpers instead of inferring workspace-safety cases from generic `ValueError` text. If a dedicated exit code is already part of the runtime contract surface, extend `runtime/exit_codes.py`; otherwise keep the existing error exit behavior deterministic and documented in tests. Do not add policy evaluation, bundle validation, or unrelated runtime errors in this plan.</action>
  <verify>
    <automated>PYTHONPATH=src ./.venv/bin/python -m pytest tests/test_cli_workspace_safety.py tests/test_cli_observability.py -q</automated>
  </verify>
  <done>Workspace lock conflicts and recovery-needed states raise concrete runtime exceptions and serialize through the CLI as schema-versioned machine-readable payloads with stable reason codes and actionable metadata.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime exception -> CLI payload | Internal workspace failures cross into agent/CI-visible machine-readable output. |
| workspace metadata -> observability blocks | Lock-holder details and recovery findings must be emitted without becoming ambiguous or lossy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | R | CLI error output | mitigate | Add stable reason codes and structured metadata so conflicts and recovery-needed states are attributable and automatable. |
| T-03-02-02 | I | workspace safety payloads | accept | Lock metadata is local workspace process information, not secrets; emit only operational fields required for retry and recovery. |
| T-03-02-03 | D | exit handling for machine consumers | mitigate | Keep deterministic exit behavior for workspace safety failures so CI does not misclassify blocked mutations as success. |
</threat_model>

<verification>
Run the CLI contract suite after implementation:

`PYTHONPATH=src ./.venv/bin/python -m pytest tests/test_cli_workspace_safety.py tests/test_cli_observability.py -q`
</verification>

<success_criteria>
- JSON output exposes stable workspace safety reasons and metadata for machine consumers.
- Text output stays concise without losing the retry or recovery meaning.
- Workspace safety signals fit the existing schema-versioned error envelope.
</success_criteria>

<output>
After completion, create `.planning/phases/03-concurrent-workspace-safety/03-concurrent-workspace-safety-02-SUMMARY.md`
</output>
