---
phase: 08-milestone-verification-bookkeeping-closure
plan: "04"
type: execute
wave: 4
depends_on:
  - "08-01"
files_modified:
  - src/quantum_runtime/runtime/executor.py
  - tests/test_runtime_workspace_safety.py
autonomous: true
requirements:
  - RUNT-02
gap_closure: true
must_haves:
  truths:
    - "A forced failure during `_promote_exec_aliases()` can no longer leave `specs/current.json` ahead of `reports/latest.json` and `manifests/latest.json` without forcing recovery."
    - "The next `exec` fail-closes with `WorkspaceRecoveryRequiredError` when mutable exec aliases are left semantically inconsistent after interrupted promotion."
    - "The focused Phase 03/07 regression subset stays green after the alias-promotion and recovery-guard fix."
  artifacts:
    - path: "src/quantum_runtime/runtime/executor.py"
      provides: "Coherent exec alias promotion plus recovery detection for interrupted mutable alias updates."
      contains: "_promote_exec_aliases and _guard_exec_commit_paths close the alias-promotion recovery hole"
    - path: "tests/test_runtime_workspace_safety.py"
      provides: "Regression coverage for alias-promotion interruption after `specs/current.json` moves."
      contains: "forced failure after qspec alias promotion requires recovery"
  key_links:
    - from: "src/quantum_runtime/runtime/executor.py"
      to: "tests/test_runtime_workspace_safety.py"
      via: "forced alias-promotion interruption is caught as a recovery-required state before the next exec proceeds"
      pattern: "_promote_exec_aliases|_guard_exec_commit_paths|WorkspaceRecoveryRequiredError"
    - from: "src/quantum_runtime/runtime/executor.py"
      to: "tests/test_runtime_imports.py, tests/test_runtime_revision_artifacts.py, tests/test_cli_exec.py"
      via: "focused Phase 07 reopen/import regressions stay green across the shared writer/executor seam"
      pattern: "tests/test_runtime_imports.py|tests/test_runtime_revision_artifacts.py|tests/test_cli_exec.py"
---

<objective>
Close the surviving Phase 08 workspace-safety gap in exec alias promotion and recovery detection.

Purpose: the verifier reproduced one remaining `RUNT-02` hole after `08-01`: if `_promote_exec_aliases()` fails after copying `specs/current.json`, the workspace can expose mixed active aliases and the next `exec` is not forced into recovery. This plan must close that concrete interruption path rather than widening scope into unrelated runtime work.
Output: executor alias promotion and recovery guard behave coherently under forced interruption, with regression coverage that proves the next `exec` fails closed.
</objective>

<execution_context>
@/Users/xizhao/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xizhao/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-milestone-verification-bookkeeping-closure/08-VERIFICATION.md
@.planning/phases/08-milestone-verification-bookkeeping-closure/08-REVIEW.md
@.planning/phases/08-milestone-verification-bookkeeping-closure/08-VALIDATION.md
@.planning/phases/08-milestone-verification-bookkeeping-closure/08-01-SUMMARY.md
@src/quantum_runtime/runtime/executor.py
@tests/test_runtime_workspace_safety.py
@tests/test_runtime_imports.py
@tests/test_runtime_revision_artifacts.py
@tests/test_cli_exec.py

Gap to close, as verified in `08-VERIFICATION.md`:
- `_promote_exec_aliases()` can stop after copying `specs/current.json` and before copying `reports/latest.json` / `manifests/latest.json`
- `_guard_exec_commit_paths()` only scans report/manifest temp files and does not detect the resulting mixed active alias state
- a subsequent `exec` can proceed instead of forcing recovery
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a failing regression for interrupted alias promotion after `specs/current.json` moves</name>
  <files>tests/test_runtime_workspace_safety.py</files>
  <read_first>
    - tests/test_runtime_workspace_safety.py
    - src/quantum_runtime/runtime/executor.py
    - .planning/phases/08-milestone-verification-bookkeeping-closure/08-VERIFICATION.md
    - .planning/phases/08-milestone-verification-bookkeeping-closure/08-REVIEW.md
  </read_first>
  <action>Add the smallest failing regression that reproduces the verifier's surviving hole: monkeypatch the exec alias-promotion path so `atomic_copy_file()` raises immediately after promoting `specs/current.json`, then assert the workspace is not treated as healthy on the next `execute_intent()` call. The regression must prove two concrete facts: the interrupted promotion leaves a mixed active alias state, and the next exec now must raise `WorkspaceRecoveryRequiredError` instead of silently advancing to a new revision. Keep the fixture focused on exec alias promotion; do not widen into compare, bench, doctor, or broader repo-smoke behavior.</action>
  <acceptance_criteria>
    - `rg -n "qspec alias promotion|WorkspaceRecoveryRequiredError|mixed active alias|specs/current.json" tests/test_runtime_workspace_safety.py`
    - `./.venv/bin/python -m pytest tests/test_runtime_workspace_safety.py -q --maxfail=1`
  </acceptance_criteria>
  <verify>
    <automated>./.venv/bin/python -m pytest tests/test_runtime_workspace_safety.py -q --maxfail=1</automated>
  </verify>
  <done>The regression suite can fail red on the exact alias-promotion interruption path that Phase 08 verification identified, instead of only covering manifest-write interruption.</done>
</task>

<task type="auto">
  <name>Task 2: Harden exec alias promotion and recovery guard around the active alias set</name>
  <files>src/quantum_runtime/runtime/executor.py</files>
  <read_first>
    - src/quantum_runtime/runtime/executor.py
    - tests/test_runtime_workspace_safety.py
    - tests/test_runtime_imports.py
    - tests/test_runtime_revision_artifacts.py
    - tests/test_cli_exec.py
    - .planning/phases/08-milestone-verification-bookkeeping-closure/08-VERIFICATION.md
    - .planning/phases/08-milestone-verification-bookkeeping-closure/08-REVIEW.md
  </read_first>
  <action>Implement the narrow production fix for the surviving alias-promotion hole in `src/quantum_runtime/runtime/executor.py`. First, reorder `_promote_exec_aliases()` so `reports/latest.json` and `manifests/latest.json` are promoted before `specs/current.json`, instead of after it. Second, expand `_guard_exec_commit_paths()` beyond report/manifest temp files: it must scan pending temp files for every mutable alias written by `_promote_exec_aliases()` and also validate the semantic coherence of the active exec alias set before a new exec proceeds. At minimum, compare `workspace.json.current_revision`, `reports/latest.json`, `manifests/latest.json`, and `specs/current.json`; if those payloads do not all describe the same authoritative revision after an interrupted promotion, raise `WorkspaceRecoveryRequiredError` with concrete alias paths in the details instead of continuing. Reuse existing recovery error types and current workspace schema; do not redesign the control plane or add new persistent metadata. After the fix is green, rerun the focused Phase 03/07 regression subset `tests/test_runtime_imports.py`, `tests/test_runtime_revision_artifacts.py`, and `tests/test_cli_exec.py` so the shared writer/executor seam remains stable.</action>
  <acceptance_criteria>
    - `rg -n "_promote_exec_aliases|_guard_exec_commit_paths|WorkspaceRecoveryRequiredError|reports/latest.json|manifests/latest.json|specs/current.json" src/quantum_runtime/runtime/executor.py`
    - `./.venv/bin/python -m pytest tests/test_runtime_workspace_safety.py tests/test_runtime_imports.py tests/test_runtime_revision_artifacts.py tests/test_cli_exec.py -q --maxfail=1`
  </acceptance_criteria>
  <verify>
    <automated>./.venv/bin/python -m pytest tests/test_runtime_workspace_safety.py tests/test_runtime_imports.py tests/test_runtime_revision_artifacts.py tests/test_cli_exec.py -q --maxfail=1</automated>
  </verify>
  <done>The next `exec` now fail-closes on interrupted alias promotion, the active alias set cannot silently drift into a mixed revision state, and the focused Phase 03/07 seam regressions remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| history artifacts -> active aliases | Exec promotes mutable aliases after durable history writes; interrupted promotion must not expose mixed revision state as healthy. |
| recovery guard -> next exec | The next execution must detect interrupted alias promotion and fail closed instead of silently advancing the workspace. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | T | `src/quantum_runtime/runtime/executor.py` alias promotion order | mitigate | Promote report and manifest aliases before `specs/current.json` and remove the known "new qspec, old report/manifest" interruption window. |
| T-08-04-02 | T | `src/quantum_runtime/runtime/executor.py` recovery guard scope | mitigate | Detect interrupted alias promotion across the active alias set and raise `WorkspaceRecoveryRequiredError` before the next exec proceeds. |
| T-08-04-03 | R | `tests/test_runtime_workspace_safety.py` | mitigate | Add a direct regression that fails after qspec alias promotion so the surviving hole cannot be hidden behind the older manifest-write interruption test. |
</threat_model>

<verification>
Run the reopened workspace-safety seam gate:

`./.venv/bin/python -m pytest tests/test_runtime_workspace_safety.py tests/test_runtime_imports.py tests/test_runtime_revision_artifacts.py tests/test_cli_exec.py -q --maxfail=1`
</verification>

<success_criteria>
- A forced failure after `specs/current.json` promotion no longer lets the next `exec` continue without recovery.
- `src/quantum_runtime/runtime/executor.py` now treats the active alias set as one coherent recovery surface rather than guarding only report/manifest temp files.
- The focused Phase 03/07 regression subset remains green after the fix.
</success_criteria>

<output>
After completion, create `.planning/phases/08-milestone-verification-bookkeeping-closure/08-04-SUMMARY.md`
</output>
