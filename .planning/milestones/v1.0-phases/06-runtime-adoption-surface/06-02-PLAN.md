---
phase: "06-runtime-adoption-surface"
plan: "02"
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - docs/agent-ci-adoption.md
  - docs/aionrs-integration.md
  - docs/fluxq-qaoa-maxcut-case-study.md
  - integrations/aionrs/CLAUDE.md.example
  - integrations/aionrs/hooks.example.toml
  - tests/test_aionrs_assets.py
  - tests/test_runtime_adoption_workflow.py
autonomous: true
requirements:
  - SURF-02
must_haves:
  truths:
    - "仓库里有一条面向 agent/CI 的端到端 adoption workflow，覆盖 ingress、execution、policy evaluation 和 delivery handoff。"
    - "aionrs integration asset 展示的是 file + shell runtime contract，并明确要求先过 compare/doctor gate，再进入 handoff。"
    - "QAOA case study 与 adoption doc 都把 `pack-inspect` 放在 `pack-import` 前，并明确说明机器消费者应读取 `reason_codes`、`next_actions`、`gate`。"
  artifacts:
    - path: "docs/agent-ci-adoption.md"
      provides: "一条可引用的 canonical adoption workflow 文档。"
      contains: "## Delivery Handoff"
    - path: "docs/aionrs-integration.md"
      provides: "aionrs host integration 的 policy + handoff 合同。"
      contains: "qrun compare --workspace .quantum --baseline --fail-on subject_drift --json"
    - path: "integrations/aionrs/CLAUDE.md.example"
      provides: "agent host 规则样板，明确 stop-on-gate。"
      contains: "If compare or doctor returns a blocking gate"
    - path: "docs/fluxq-qaoa-maxcut-case-study.md"
      provides: "带 policy/handoff 结尾的 end-to-end case study。"
      contains: "pack-inspect"
    - path: "tests/test_runtime_adoption_workflow.py"
      provides: "adoption workflow / case study 文档回归锁。"
      contains: "test_agent_ci_adoption_doc_covers_canonical_runtime_loop"
  key_links:
    - from: "docs/agent-ci-adoption.md"
      to: "docs/aionrs-integration.md"
      via: "canonical loop 被 host integration 复用，而不是每份文档各写一套命令"
      pattern: "qrun doctor --workspace \\.quantum --json --ci"
    - from: "docs/fluxq-qaoa-maxcut-case-study.md"
      to: "integrations/aionrs/CLAUDE.md.example"
      via: "case study 与 host rule 都要求先过 gate，再 handoff"
      pattern: "pack-inspect|pack-import"
---

<objective>
把 FluxQ 现有 runtime surface 组织成一条可复用的 adoption workflow，完整交付 SURF-02。

Purpose: 当前 repo 明明已经有 ingress、policy gate、bundle verify/import 这些能力，但 public doc 还是散的。这个计划把它们收束成一个 canonical adoption loop，并把 aionrs integration、case study、host asset 都拉到同一条 runtime contract 上。
Output: 新的 adoption workflow 文档、更新后的 aionrs integration asset、补齐 policy + handoff 结尾的 QAOA case study，以及锁定这些 surface 的 focused tests。
</objective>

<execution_context>
@/Users/xizhao/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xizhao/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-runtime-adoption-surface/06-RESEARCH.md
@.planning/phases/06-runtime-adoption-surface/06-VALIDATION.md
@docs/aionrs-integration.md
@docs/fluxq-qaoa-maxcut-case-study.md
@integrations/aionrs/CLAUDE.md.example
@integrations/aionrs/hooks.example.toml
@.planning/phases/04-policy-acceptance-gates/04-policy-acceptance-gates-04-SUMMARY.md
@.planning/phases/05-verified-delivery-bundles/05-verified-delivery-bundles-02-SUMMARY.md
@.planning/phases/05-verified-delivery-bundles/05-verified-delivery-bundles-03-SUMMARY.md
@tests/test_aionrs_assets.py

本计划只改 docs / integration examples / tests，不改 `src/quantum_runtime/`、`README.md`、`docs/versioning.md` 或 package metadata。
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先把 adoption workflow、aionrs asset 和 case study 的缺口锁进测试</name>
  <files>tests/test_aionrs_assets.py, tests/test_runtime_adoption_workflow.py</files>
  <read_first>
- .planning/phases/06-runtime-adoption-surface/06-RESEARCH.md
- docs/aionrs-integration.md
- docs/fluxq-qaoa-maxcut-case-study.md
- integrations/aionrs/CLAUDE.md.example
- integrations/aionrs/hooks.example.toml
- tests/test_aionrs_assets.py
- .planning/phases/04-policy-acceptance-gates/04-policy-acceptance-gates-04-SUMMARY.md
- .planning/phases/05-verified-delivery-bundles/05-verified-delivery-bundles-02-SUMMARY.md
- .planning/phases/05-verified-delivery-bundles/05-verified-delivery-bundles-03-SUMMARY.md
  </read_first>
  <behavior>
- 测试 1：`docs/aionrs-integration.md`、`integrations/aionrs/CLAUDE.md.example`、`integrations/aionrs/hooks.example.toml` 必须一起展示 `qrun compare --workspace .quantum --baseline --fail-on subject_drift --json`、`qrun doctor --workspace .quantum --json --ci`、`qrun pack --workspace .quantum --revision rev_000001 --json`、`qrun pack-inspect --pack-root .quantum/packs/rev_000001 --json`、`qrun pack-import --pack-root .quantum/packs/rev_000001 --workspace downstream/.quantum --json` 这些 surface。
- 测试 2：`integrations/aionrs/CLAUDE.md.example` 必须包含精确句子 `If compare or doctor returns a blocking gate, revise the intent and rerun FluxQ; do not hand-edit generated quantum code to bypass the gate.`。
- 测试 3：新增 `docs/agent-ci-adoption.md`，并在新测试文件里锁定它包含 `## Canonical Loop`、`## Agent Host Loop`、`## CI Gate`、`## Delivery Handoff` 四个 section，以及完整 command block。
- 测试 4：`docs/fluxq-qaoa-maxcut-case-study.md` 必须新增 `## Agent/CI continuation` 与 `## Delivery handoff` 两个 section，并明确把 `pack-inspect` 排在 `pack-import` 前，同时出现 `reason_codes`、`next_actions`、`gate`。
  </behavior>
  <action>扩展 `tests/test_aionrs_assets.py`，新增一个 focused test `test_aionrs_assets_cover_policy_and_delivery_handoff()`，不要只在现有 smoke test 里继续堆 assert。新测试要对 aionrs doc、CLAUDE 样板和 hooks 样板分别做精确字符串断言，尤其是 `doctor --workspace .quantum --json --ci` 和 `If compare or doctor returns a blocking gate...` 那句 stop-on-gate 规则。新建 `tests/test_runtime_adoption_workflow.py`，放两个测试：`test_agent_ci_adoption_doc_covers_canonical_runtime_loop()` 和 `test_qaoa_case_study_covers_policy_and_delivery_handoff()`；它们分别锁 `docs/agent-ci-adoption.md` 与 `docs/fluxq-qaoa-maxcut-case-study.md` 的 section 标题、命令块、`pack-inspect` 早于 `pack-import` 的顺序，以及 `reason_codes` / `next_actions` / `gate` 的出现。</action>
  <acceptance_criteria>
- `rg -n "test_aionrs_assets_cover_policy_and_delivery_handoff" tests/test_aionrs_assets.py`
- `rg -n "test_agent_ci_adoption_doc_covers_canonical_runtime_loop|test_qaoa_case_study_covers_policy_and_delivery_handoff" tests/test_runtime_adoption_workflow.py`
- `rg -n "pack-inspect|pack-import|doctor --workspace \\.quantum --json --ci|If compare or doctor returns a blocking gate" tests/test_aionrs_assets.py tests/test_runtime_adoption_workflow.py`
  </acceptance_criteria>
  <verify>
    <automated>./.venv/bin/python -m pytest tests/test_aionrs_assets.py tests/test_runtime_adoption_workflow.py -q --maxfail=1</automated>
  </verify>
  <done>adoption workflow、host integration asset 和 case study 的 public contract 已经先被测试表达成可失败的要求。</done>
</task>

<task type="auto">
  <name>Task 2: 编写 canonical adoption workflow，并把 integration/case-study surface 拉齐到同一条 runtime contract</name>
  <files>docs/agent-ci-adoption.md, docs/aionrs-integration.md, docs/fluxq-qaoa-maxcut-case-study.md, integrations/aionrs/CLAUDE.md.example, integrations/aionrs/hooks.example.toml</files>
  <read_first>
- docs/aionrs-integration.md
- docs/fluxq-qaoa-maxcut-case-study.md
- integrations/aionrs/CLAUDE.md.example
- integrations/aionrs/hooks.example.toml
- tests/test_aionrs_assets.py
- tests/test_runtime_adoption_workflow.py
- .planning/phases/04-policy-acceptance-gates/04-policy-acceptance-gates-04-SUMMARY.md
- .planning/phases/05-verified-delivery-bundles/05-verified-delivery-bundles-02-SUMMARY.md
- .planning/phases/05-verified-delivery-bundles/05-verified-delivery-bundles-03-SUMMARY.md
  </read_first>
  <behavior>
- 行为 1：`docs/agent-ci-adoption.md` 成为一份可直接引用的 canonical doc，包含 `## Canonical Loop`、`## Agent Host Loop`、`## CI Gate`、`## Delivery Handoff`，并给出一条完整 runtime/adoption command chain。
- 行为 2：`docs/aionrs-integration.md` 不再止步于 `plan/exec/show/doctor/bench`，而是补齐 `baseline set`、`compare --baseline --fail-on subject_drift --json`、`doctor --json --ci`、`pack`、`pack-inspect`、`pack-import`。
- 行为 3：`integrations/aionrs/CLAUDE.md.example` 与 `hooks.example.toml` 明确体现 stop-on-gate 规则和 CI-oriented doctor hook；样板仍坚持 file + shell，而不是发明自定义 aionrs tool。
- 行为 4：QAOA case study 在现有执行/benchmark/doctor 叙事之后，补出 agent/CI continuation 与 delivery handoff 结尾，并明确说明机器消费者读取 `reason_codes`、`next_actions`、`gate`。
  </behavior>
  <action>新建 `docs/agent-ci-adoption.md`，文件标题用 `# Runtime Adoption Workflow`，至少写出四个 section：`## Canonical Loop`、`## Agent Host Loop`、`## CI Gate`、`## Delivery Handoff`。在 `## Canonical Loop` 放一组精确命令：`qrun prompt "Build a 4-qubit GHZ circuit and measure all qubits." --json`、`qrun resolve --workspace .quantum --intent-file examples/intent-ghz.md --json`、`qrun init --workspace .quantum --json`、`qrun plan --workspace .quantum --intent-file examples/intent-ghz.md --json`、`qrun exec --workspace .quantum --intent-file examples/intent-ghz.md --jsonl`、`qrun baseline set --workspace .quantum --revision rev_000001 --json`、`qrun compare --workspace .quantum --baseline --fail-on subject_drift --json`、`qrun doctor --workspace .quantum --json --ci`、`qrun pack --workspace .quantum --revision rev_000001 --json`、`qrun pack-inspect --pack-root .quantum/packs/rev_000001 --json`、`qrun pack-import --pack-root .quantum/packs/rev_000001 --workspace downstream/.quantum --json`。在 `docs/aionrs-integration.md` 中把当前 numbered workflow 改写成 `## Canonical host workflow` 和 `## Machine-readable signals to consume` 两个 section，并加入同样的 policy/handoff surface。把 `integrations/aionrs/CLAUDE.md.example` 改成 8 步 runtime workflow，精确加入句子 `If compare or doctor returns a blocking gate, revise the intent and rerun FluxQ; do not hand-edit generated quantum code to bypass the gate.`。把 `integrations/aionrs/hooks.example.toml` 的命令改成 `bash -lc 'if [ -f .quantum/specs/current.json ]; then qrun doctor --workspace .quantum --json --ci >/dev/null 2>&1 || true; fi'`。最后在 `docs/fluxq-qaoa-maxcut-case-study.md` 新增 `## Agent/CI continuation` 与 `## Delivery handoff`，把 `qrun compare --workspace .quantum --baseline --fail-on subject_drift --json`、`qrun doctor --workspace .quantum --json --ci`、`qrun pack --workspace .quantum --revision rev_000001 --json`、`qrun pack-inspect --pack-root .quantum/packs/rev_000001 --json`、`qrun pack-import --pack-root .quantum/packs/rev_000001 --workspace downstream/.quantum --json` 写进命令块，并显式说明 agent/CI 应该读取 `reason_codes`、`next_actions`、`gate` 而不是猜测状态。</action>
  <acceptance_criteria>
- `rg -n "^# Runtime Adoption Workflow|^## Canonical Loop|^## Agent Host Loop|^## CI Gate|^## Delivery Handoff" docs/agent-ci-adoption.md`
- `rg -n "qrun compare --workspace \\.quantum --baseline --fail-on subject_drift --json|qrun doctor --workspace \\.quantum --json --ci|qrun pack-inspect --pack-root \\.quantum/packs/rev_000001 --json|qrun pack-import --pack-root \\.quantum/packs/rev_000001 --workspace downstream/\\.quantum --json" docs/agent-ci-adoption.md docs/aionrs-integration.md docs/fluxq-qaoa-maxcut-case-study.md integrations/aionrs/CLAUDE.md.example`
- `rg -n "If compare or doctor returns a blocking gate, revise the intent and rerun FluxQ; do not hand-edit generated quantum code to bypass the gate\\." integrations/aionrs/CLAUDE.md.example`
- `rg -n "qrun doctor --workspace \\.quantum --json --ci >/dev/null 2>&1 \\|\\| true" integrations/aionrs/hooks.example.toml`
  </acceptance_criteria>
  <verify>
    <automated>./.venv/bin/python -m pytest tests/test_aionrs_assets.py tests/test_runtime_adoption_workflow.py -q --maxfail=1</automated>
  </verify>
  <done>仓库里出现一条可复用的 adoption workflow，aionrs asset 与 case study 都跟这条 runtime contract 对齐。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| integration docs / host rules -> agent host 行为 | 文档与样板会直接决定 host 是不是先过 policy gate、是不是先验证 bundle、是不是绕过 runtime contract 手改生成代码。 |
| case study / adoption doc -> CI 与 handoff 习惯 | 案例文档会把 pack/inspect/import 的顺序和机器可读信号消费方式固化给 adopters。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | T | aionrs integration workflow | mitigate | 在 integration doc 与 sample rules 中显式加入 `compare --fail-on subject_drift`、`doctor --ci`，防止 host 直接跳过 policy gate。 |
| T-06-02-02 | T | delivery handoff docs | mitigate | 在 adoption doc 与 case study 中锁定 `pack-inspect` 先于 `pack-import`，并用测试阻止顺序倒置。 |
| T-06-02-03 | R | sample host rules | mitigate | 在 `CLAUDE.md.example` 里加入 stop-on-gate 句子，并强调修改 intent + rerun，而不是手改生成代码绕过 runtime contract。 |
</threat_model>

<verification>
执行本计划后，至少运行：

`./.venv/bin/python -m pytest tests/test_aionrs_assets.py tests/test_runtime_adoption_workflow.py -q --maxfail=1`
</verification>

<success_criteria>
- SURF-02 的 adoption workflow 有独立 owner，并且不再散落在彼此断裂的文档里。
- aionrs integration asset、case study、canonical adoption doc 共享同一条 runtime command contract。
- public adoption surface 明确要求先过 policy gate，再做 bundle handoff。
</success_criteria>

<output>
完成后创建 `.planning/phases/06-runtime-adoption-surface/06-runtime-adoption-surface-02-SUMMARY.md`
</output>
