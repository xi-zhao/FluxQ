---
phase: "06-runtime-adoption-surface"
plan: "03"
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - docs/versioning.md
  - docs/releases/v0.3.1.md
  - CHANGELOG.md
  - pyproject.toml
  - tests/test_release_docs.py
  - tests/test_packaging_release.py
autonomous: true
requirements:
  - SURF-03
must_haves:
  truths:
    - "adopter 能从 `docs/versioning.md` 明确区分 stable、evolving、optional runtime contract。"
    - "adopter 能从 `docs/releases/v0.3.1.md` 读到面向当前 released line 的 stable/evolving/optional guidance，而不是只看到 highlights。"
    - "release/versioning/package metadata 在 public positioning 上一致，不再把 FluxQ 包装成 code generator。"
    - "safe consumption rules 清楚说明哪些 JSON/runtime surface 只能做 additive 演进，哪些 optional surface 需要 capability-gated 消费。"
  artifacts:
    - path: "docs/versioning.md"
      provides: "stable / evolving / optional runtime contract taxonomy。"
      contains: "## Stable Runtime Contracts"
    - path: "docs/releases/v0.3.1.md"
      provides: "released line 的 runtime contract stability guidance。"
      contains: "## Runtime Contract Stability"
    - path: "CHANGELOG.md"
      provides: "Unreleased 下的 runtime contract guidance 变更记录。"
      contains: "clarify stable, evolving, and optional runtime contracts for adopters"
    - path: "pyproject.toml"
      provides: "与 runtime-control-plane 定位一致的 package metadata。"
      contains: "control-plane"
    - path: "tests/test_release_docs.py"
      provides: "release-note stability guidance 回归锁。"
      contains: "test_release_notes_document_runtime_contract_stability"
    - path: "tests/test_packaging_release.py"
      provides: "versioning/changelog/metadata 对齐回归锁。"
      contains: "test_runtime_contract_stability_is_documented_in_versioning_and_packaging_metadata"
  key_links:
    - from: "docs/versioning.md"
      to: "docs/releases/v0.3.1.md"
      via: "versioning taxonomy 要落到当前 released line 的 adopter guidance"
      pattern: "Runtime Contract Stability|stable runtime contracts|evolving runtime contracts|optional runtime contracts"
    - from: "docs/versioning.md"
      to: "pyproject.toml"
      via: "package metadata 不再自相矛盾地声称 code generators"
      pattern: "Topic :: Software Development :: Code Generators|control-plane"
    - from: "docs/releases/v0.3.1.md"
      to: "CHANGELOG.md"
      via: "release notes 与 unreleased narrative 同步记录 contract guidance 更新"
      pattern: "clarify stable, evolving, and optional runtime contracts for adopters"
---

<objective>
补齐 stable/evolving/optional runtime contract 说明，并同步 release notes、versioning 与 package metadata，完整交付 SURF-03。

Purpose: 现在最大的 adoption 风险不是功能缺失，而是 public contract 没说清哪些 surface 已稳定、哪些只承诺 additive evolution、哪些只是 optional integration。这个计划把 `docs/versioning.md`、`docs/releases/v0.3.1.md`、`CHANGELOG.md` 和 `pyproject.toml` 拉齐，并用 docs + packaging tests 锁住。
Output: 带稳定性 taxonomy 的 versioning 文档、面向 `v0.3.1` 的 release-note guidance、记录到 `Unreleased` 的 release narrative、移除 generator-first classifier 的 package metadata，以及相应回归测试。
</objective>

<execution_context>
@/Users/xizhao/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xizhao/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-runtime-adoption-surface/06-RESEARCH.md
@.planning/phases/06-runtime-adoption-surface/06-VALIDATION.md
@docs/versioning.md
@docs/releases/v0.3.1.md
@CHANGELOG.md
@pyproject.toml
@tests/test_release_docs.py
@tests/test_packaging_release.py

本计划只改 versioning / release stability notes / packaging metadata / 对应 tests。不要回头修改 `README.md`、integration docs 或任何 `src/quantum_runtime/` 代码。
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先把 stable/evolving/optional contract、release-note guidance 与 package metadata 对齐要求锁进测试</name>
  <files>tests/test_release_docs.py, tests/test_packaging_release.py</files>
  <read_first>
- .planning/phases/06-runtime-adoption-surface/06-RESEARCH.md
- docs/versioning.md
- docs/releases/v0.3.1.md
- CHANGELOG.md
- pyproject.toml
- tests/test_release_docs.py
- tests/test_packaging_release.py
  </read_first>
  <behavior>
- 测试 1：`docs/versioning.md` 必须出现 `## Stable Runtime Contracts`、`## Evolving Runtime Contracts`、`## Optional Runtime Contracts`、`## Safe Consumption Rules` 四个精确 section。
- 测试 2：`docs/releases/v0.3.1.md` 必须新增 `## Runtime Contract Stability`，并明确包含 `Stable runtime contracts:`、`Evolving runtime contracts:`、`Optional runtime contracts:`、`Safe consumption rule:` 四个 guidance 前缀。
- 测试 3：stable section 必须出现 `QSpec.version`、`schema_version`、`append-only` 与 `pack-inspect` / `pack-import` 这些稳定 contract 关键词；evolving guidance 必须出现 `reason_codes`、`next_actions`、`decision`、`gate`；optional guidance 必须出现 `classiq`、`aionrs`、`hooks.example.toml`。
- 测试 4：`CHANGELOG.md` 的 `## Unreleased` 必须包含两条精确 bullet：`clarify stable, evolving, and optional runtime contracts for adopters` 和 `align packaging metadata with the runtime control plane positioning`。
- 测试 5：`pyproject.toml` 必须不再包含 `"Topic :: Software Development :: Code Generators"`，并在 `keywords` 中包含 `control-plane`。
  </behavior>
  <action>在 `tests/test_packaging_release.py` 新增一个 focused test `test_runtime_contract_stability_is_documented_in_versioning_and_packaging_metadata()`，不要把这些断言继续塞进现有 packaging smoke test。这个新测试要读取 `docs/versioning.md`、`CHANGELOG.md`、`pyproject.toml`，并做精确正负断言：`"## Stable Runtime Contracts"`、`"## Evolving Runtime Contracts"`、`"## Optional Runtime Contracts"`、`"## Safe Consumption Rules"`、`"QSpec.version"`、`"schema_version"`、`"append-only"`、`"pack-inspect"`、`"pack-import"`、`"reason_codes"`、`"next_actions"`、`"decision"`、`"gate"`、`"classiq"`、`"aionrs"`、`"hooks.example.toml"`、`"clarify stable, evolving, and optional runtime contracts for adopters"`、`"align packaging metadata with the runtime control plane positioning"`。同时加入负向断言，要求 `pyproject.toml` 中不再出现 `"Topic :: Software Development :: Code Generators"`。在 `tests/test_release_docs.py` 新增一个 focused test `test_release_notes_document_runtime_contract_stability()`，专门锁 `docs/releases/v0.3.1.md` 中的 `## Runtime Contract Stability` section 及其四条 guidance 前缀，不要把这些断言继续塞进现有 README/release smoke test。</action>
  <acceptance_criteria>
- `rg -n "test_runtime_contract_stability_is_documented_in_versioning_and_packaging_metadata" tests/test_packaging_release.py`
- `rg -n "test_release_notes_document_runtime_contract_stability" tests/test_release_docs.py`
- `rg -n "Stable Runtime Contracts|Evolving Runtime Contracts|Optional Runtime Contracts|Safe Consumption Rules|Runtime Contract Stability|Stable runtime contracts:|Evolving runtime contracts:|Optional runtime contracts:|Safe consumption rule:|control-plane" tests/test_release_docs.py tests/test_packaging_release.py`
- `rg -n "Code Generators" tests/test_packaging_release.py`
  </acceptance_criteria>
  <verify>
    <automated>./.venv/bin/python -m pytest tests/test_release_docs.py tests/test_packaging_release.py -q --maxfail=1</automated>
  </verify>
  <done>stable/evolving/optional taxonomy、release-note guidance 与 package metadata 对齐要求已经先被定义成可失败的回归测试。</done>
</task>

<task type="auto">
  <name>Task 2: 写清 runtime contract taxonomy，并把 release/package metadata 与之对齐</name>
  <files>docs/versioning.md, docs/releases/v0.3.1.md, CHANGELOG.md, pyproject.toml</files>
  <read_first>
- docs/versioning.md
- docs/releases/v0.3.1.md
- CHANGELOG.md
- pyproject.toml
- tests/test_release_docs.py
- tests/test_packaging_release.py
- .planning/phases/06-runtime-adoption-surface/06-RESEARCH.md
  </read_first>
  <behavior>
- 行为 1：`docs/versioning.md` 把 FluxQ 的 public contract 明确拆成 stable、evolving、optional 三类，并附上 safe consumption rules。
- 行为 2：`docs/releases/v0.3.1.md` 为当前 released line 增加面向 adopter 的 stable/evolving/optional guidance，而不是只停留在 highlights/quickstart。
- 行为 3：`CHANGELOG.md` `## Unreleased` 明确记录本次 adoption-surface 对 contract taxonomy 与 metadata alignment 的增量，而不是继续留空。
- 行为 4：`pyproject.toml` 的 metadata 不再与 runtime/control-plane 叙事冲突；删除 `Code Generators` classifier，并把 `control-plane` 放进 keywords。
- 行为 5：本计划不要回头改 `README.md` 或 integration docs；06-01 仍只负责 quickstart coherence。
  </behavior>
  <action>重写 `docs/versioning.md` 的主体结构，但保留现有 `Released line: \`0.3.1\`` 和 release-line 段落。新增以下精确 section 标题：`## Stable Runtime Contracts`、`## Evolving Runtime Contracts`、`## Optional Runtime Contracts`、`## Safe Consumption Rules`。在 `## Stable Runtime Contracts` 里至少写出这四条精确短语：`QSpec.version remains explicit in serialized specs.`、`CLI/result/artifact schema_version stays separate from QSpec.version.`、`Workspace revision history remains append-only under .quantum/*/history/.`、`Verified delivery handoff runs through qrun pack, qrun pack-inspect, and qrun pack-import.`。在 `## Evolving Runtime Contracts` 里至少写出 ``reason_codes`、`next_actions`、`decision`、`gate` are additive machine signals``，以及 `CLI JSON responses can add fields, but should not rename or remove released fields.`。在 `## Optional Runtime Contracts` 里明确列出 `classiq` extra、`docs/aionrs-integration.md`、`integrations/aionrs/hooks.example.toml`、export profiles。然后更新 `docs/releases/v0.3.1.md`：保留现有 release framing 和 `## What To Try First`，新增精确 section 标题 `## Runtime Contract Stability`，并在该 section 里至少写出这四条精确 guidance 前缀：`Stable runtime contracts:`、`Evolving runtime contracts:`、`Optional runtime contracts:`、`Safe consumption rule:`；其内容必须与 `docs/versioning.md` 对齐，并明确提到 `QSpec.version`、`schema_version`、append-only history、`qrun pack` / `qrun pack-inspect` / `qrun pack-import`、`reason_codes` / `next_actions` / `decision` / `gate`、`classiq`、`docs/aionrs-integration.md`、`integrations/aionrs/hooks.example.toml`。然后在 `CHANGELOG.md` 的 `## Unreleased` 下新增两条精确 bullet：`- clarify stable, evolving, and optional runtime contracts for adopters`、`- align packaging metadata with the runtime control plane positioning`。最后更新 `pyproject.toml`：从 `classifiers` 删除 `"Topic :: Software Development :: Code Generators"`，并把 `keywords` 变成包含 `"control-plane"` 的列表。不要修改版本号、description、依赖、或 CI/build 配置。</action>
  <acceptance_criteria>
- `rg -n "^## Stable Runtime Contracts|^## Evolving Runtime Contracts|^## Optional Runtime Contracts|^## Safe Consumption Rules" docs/versioning.md`
- `rg -n "QSpec.version remains explicit in serialized specs\\.|CLI/result/artifact schema_version stays separate from QSpec.version\\.|Workspace revision history remains append-only under \\.quantum/\\*/history/\\.|Verified delivery handoff runs through qrun pack, qrun pack-inspect, and qrun pack-import\\." docs/versioning.md`
- `rg -n "^## Runtime Contract Stability|Stable runtime contracts:|Evolving runtime contracts:|Optional runtime contracts:|Safe consumption rule:" docs/releases/v0.3.1.md`
- `rg -n "QSpec.version|schema_version|append-only|pack-inspect|pack-import|reason_codes|next_actions|decision|gate|classiq|aionrs|hooks.example.toml" docs/releases/v0.3.1.md`
- `rg -n "reason_codes|next_actions|decision|gate|classiq|aionrs|hooks.example.toml" docs/versioning.md`
- `rg -n "clarify stable, evolving, and optional runtime contracts for adopters|align packaging metadata with the runtime control plane positioning" CHANGELOG.md`
- `rg -n "control-plane" pyproject.toml`
- `! rg -n "Topic :: Software Development :: Code Generators" pyproject.toml`
  </acceptance_criteria>
  <verify>
    <automated>./.venv/bin/python -m pytest tests/test_release_docs.py tests/test_packaging_release.py -q --maxfail=1 && ./.venv/bin/python -m build</automated>
  </verify>
  <done>adopter 能从 versioning/release/package metadata 一眼读懂 stable/evolving/optional contract，且 package metadata 不再把 FluxQ 误导成 generator。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| versioning/release docs -> adopter 兼容性假设 | 文档会直接决定调用方把哪些 surface 当稳定 API、哪些只做 additive 消费。 |
| package metadata -> 外部目录/包管理器认知 | PyPI/build metadata 中的 classifier 和 keywords 会影响外部读者对产品类别的第一印象。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | T | package metadata | mitigate | 删除 `Topic :: Software Development :: Code Generators`，并把 `control-plane` 写入 keywords，使 packaging metadata 与 runtime-first positioning 一致。 |
| T-06-03-02 | R | versioning guidance | mitigate | 明确写出 stable / evolving / optional taxonomy 与 safe consumption rules，并用测试锁住关键短语。 |
| T-06-03-03 | R | release narrative | mitigate | 在 `CHANGELOG.md` `Unreleased` 中记录本次 contract taxonomy 与 metadata alignment，避免版本线叙事继续空白。 |
</threat_model>

<verification>
执行本计划后，至少运行：

`./.venv/bin/python -m pytest tests/test_release_docs.py tests/test_packaging_release.py -q --maxfail=1 && ./.venv/bin/python -m build`
</verification>

<success_criteria>
- SURF-03 有独立 owner，并且不依赖读者自行猜测哪些 surface 稳定。
- `docs/versioning.md` 与 `docs/releases/v0.3.1.md` 明确给出 stable/evolving/optional taxonomy 与 safe consumption rules。
- `pyproject.toml` 不再使用 generator-first classifier，`CHANGELOG.md` 记录本次 adoption-surface 合同更新。
</success_criteria>

<output>
完成后创建 `.planning/phases/06-runtime-adoption-surface/06-runtime-adoption-surface-03-SUMMARY.md`
</output>
