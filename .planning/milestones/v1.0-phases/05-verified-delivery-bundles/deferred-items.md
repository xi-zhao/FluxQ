- 2026-04-14: `./.venv/bin/python -m pytest tests/test_cli_runtime_gap.py tests/test_pack_bundle.py -q --maxfail=1` is currently blocked by pre-existing `tests/test_cli_runtime_gap.py::test_qrun_compare_json_fail_on_subject_drift_returns_failed_gate`, which fails with `report_qspec_semantic_hash_mismatch` before the new pack regressions run. This plan does not modify compare semantics, so the issue is out of scope for 05-01.
