---
phase: "04-policy-acceptance-gates"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/dev-bootstrap.sh
  - CONTRIBUTING.md
autonomous: true
must_haves:
  truths:
    - "Phase 4 executors can run one repo-local verification path that covers Ruff, MyPy, and the targeted policy-gate pytest files."
    - "Local verification still works when `./.venv/bin/mypy` is unusable under the current workspace path, because the repo runs MyPy through `./.venv/bin/python -m mypy`."
    - "Phase 4 plans no longer rely on pytest-only verification; lint and type checks are part of the owned validation path."
  artifacts:
    - path: "scripts/dev-bootstrap.sh"
      provides: "Path-safe local verify gate for Ruff, MyPy, and pytest."
      contains: "\"$ROOT_DIR/.venv/bin/python\" -m mypy src"
    - path: "CONTRIBUTING.md"
      provides: "Documented Phase 4 validation commands for local execution."
      contains: "./.venv/bin/python -m mypy src"
  key_links:
    - from: "scripts/dev-bootstrap.sh"
      to: ".venv/bin/python"
      via: "module-form MyPy invocation that avoids the broken launcher path"
      pattern: "-m mypy src"
    - from: "CONTRIBUTING.md"
      to: "scripts/dev-bootstrap.sh"
      via: "documented repo-local verify command and exact lint/type/test sequence"
      pattern: "dev-bootstrap.sh verify|python -m mypy src"
---

<objective>
Make Phase 4 validation concrete and executable before product work begins.

Purpose: The current Phase 4 plan set fails Nyquist validation because every verify block is pytest-only and the local `./.venv/bin/mypy` launcher is broken under this workspace path. This plan is validation enablement only: it makes Ruff and MyPy first-class validation steps and gives later plans one stable local verification path, while POLC-01, POLC-02, and POLC-03 remain owned by 04-02, 04-03, and 04-04.
Output: A path-safe local verification command in `scripts/dev-bootstrap.sh` plus matching contributor guidance with the exact Phase 4 lint, type-check, and pytest commands.
</objective>

<execution_context>
@/Users/xizhao/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xizhao/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-policy-acceptance-gates/04-RESEARCH.md
@.planning/phases/04-policy-acceptance-gates/04-VALIDATION.md
@scripts/dev-bootstrap.sh
@CONTRIBUTING.md
@mypy.ini

Observed blocker this plan must close:
- `./.venv/bin/mypy --version` currently fails because the launcher points at `/Users/xizhao/Nutstore Files/...`, while `./.venv/bin/python -m mypy --version` succeeds.
- AGENTS.md and codebase conventions require Ruff and MyPy alongside pytest for repo validation.
- Later Phase 4 plans should not repeat the launcher-repair decision; this plan owns the path-safe validation contract once.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the repo-local verification gate path-safe for MyPy</name>
  <files>scripts/dev-bootstrap.sh</files>
  <action>Update `verify_project()` in `scripts/dev-bootstrap.sh` so the MyPy step runs through `"$ROOT_DIR/.venv/bin/python" -m mypy src` instead of `"$ROOT_DIR/.venv/bin/mypy" src`. Keep the existing install flow and the `qrun`/Ruff/pytest checks intact. In the shell logic, add a short preflight note or failure branch that makes the current workspace-path issue explicit: if `"$ROOT_DIR/.venv/bin/mypy" --version` fails, the script should still continue with the module-form invocation rather than aborting. Do not change CI commands in `.github/workflows/ci.yml`; this plan is about the local verification entrypoint only.</action>
  <verify>
    <automated>./.venv/bin/ruff check src tests && ./.venv/bin/python -m mypy src && ./.venv/bin/python -m pytest tests/test_cli_compare.py tests/test_cli_runtime_gap.py tests/test_cli_bench.py tests/test_cli_doctor.py tests/test_cli_observability.py -q --maxfail=1</automated>
  </verify>
  <done>`scripts/dev-bootstrap.sh verify` no longer depends on the broken direct MyPy launcher and still runs Ruff, MyPy, and pytest from the repo-local virtualenv.</done>
</task>

<task type="auto">
  <name>Task 2: Document the exact Phase 4 validation commands</name>
  <files>CONTRIBUTING.md</files>
  <action>Update `CONTRIBUTING.md` so the development verification section explicitly lists `./.venv/bin/ruff check src tests`, `./.venv/bin/python -m mypy src`, and the targeted Phase 4 pytest command `./.venv/bin/python -m pytest tests/test_cli_compare.py tests/test_cli_runtime_gap.py tests/test_cli_bench.py tests/test_cli_doctor.py tests/test_cli_observability.py -q --maxfail=1`. Also direct contributors to `./scripts/dev-bootstrap.sh verify` as the one-shot local gate. Keep the existing `uv run ...` guidance for general repo validation; this is an additive local-workspace note, not a workflow rewrite.</action>
  <verify>
    <automated>./.venv/bin/ruff check src tests && ./.venv/bin/python -m mypy src && ./.venv/bin/python -m pytest tests/test_cli_compare.py tests/test_cli_runtime_gap.py tests/test_cli_bench.py tests/test_cli_doctor.py tests/test_cli_observability.py -q --maxfail=1</automated>
  </verify>
  <done>Contributors can see one explicit Ruff/MyPy/pytest sequence for Phase 4, and the documented commands match the path-safe local verification entrypoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local shell -> repo validation commands | The executor depends on local tooling commands to prove later policy-gate changes are safe. |
| broken `.venv/bin/mypy` launcher -> claimed type-check coverage | A bad launcher can silently remove type-checking from the validation path if the repo does not own a path-safe invocation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | T | `scripts/dev-bootstrap.sh` verification path | mitigate | Run MyPy through `./.venv/bin/python -m mypy src` so the validation path does not depend on the broken direct launcher under this workspace path. |
| T-04-01-02 | R | contributor validation workflow | mitigate | Document the exact Ruff/MyPy/pytest commands in `CONTRIBUTING.md` so later plans can point to one auditable verification path. |
</threat_model>

<verification>
Run the full Phase 4 local validation gate after this plan:

`./.venv/bin/ruff check src tests && ./.venv/bin/python -m mypy src && ./.venv/bin/python -m pytest tests/test_cli_compare.py tests/test_cli_runtime_gap.py tests/test_cli_bench.py tests/test_cli_doctor.py tests/test_cli_observability.py -q --maxfail=1`
</verification>

<success_criteria>
- Phase 4 has an explicit validation owner for Ruff and MyPy instead of relying on pytest-only verify blocks.
- The repo-local validation path survives the current broken `./.venv/bin/mypy` launcher.
- Later Phase 4 plans can reference one concrete local validation command without re-deciding tooling behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/04-policy-acceptance-gates/04-policy-acceptance-gates-01-SUMMARY.md`
</output>
