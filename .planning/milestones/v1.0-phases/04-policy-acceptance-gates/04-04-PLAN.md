---
phase: "04-policy-acceptance-gates"
plan: "04"
type: execute
wave: 4
depends_on:
  - "04-03"
files_modified:
  - src/quantum_runtime/runtime/policy.py
  - src/quantum_runtime/runtime/doctor.py
  - src/quantum_runtime/cli.py
  - src/quantum_runtime/runtime/exit_codes.py
  - tests/test_cli_doctor.py
  - tests/test_cli_observability.py
  - tests/test_runtime_policy.py
autonomous: true
requirements:
  - POLC-03
must_haves:
  truths:
    - "Agent or CI can run `qrun doctor --ci` and receive explicit `blocking_issues`, `advisory_issues`, `verdict`, and `gate` output in machine-readable form."
    - "Advisory-only doctor CI results exit `0`, while blocking doctor CI findings exit `2`, without changing the existing non-`--ci` command behavior."
    - "The final `doctor_completed` JSONL payload carries the same blocking/advisory decision data as the JSON payload."
  artifacts:
    - path: "src/quantum_runtime/runtime/policy.py"
      provides: "Doctor CI policy primitives."
      contains: "class DoctorPolicy"
    - path: "src/quantum_runtime/runtime/doctor.py"
      provides: "Policy-bearing doctor reports with explicit blocking and advisory fields."
      contains: "blocking_issues"
    - path: "src/quantum_runtime/cli.py"
      provides: "Doctor `--ci` option and policy-aware JSON/JSONL flow."
      contains: "--ci"
    - path: "tests/test_cli_doctor.py"
      provides: "Regression coverage for advisory-only and blocking `doctor --ci` flows."
      contains: "blocking_issues"
  key_links:
    - from: "src/quantum_runtime/cli.py"
      to: "src/quantum_runtime/runtime/doctor.py"
      via: "`--ci` flag threads into `run_doctor(ci=True)`"
      pattern: "run_doctor\\(.*ci="
    - from: "src/quantum_runtime/runtime/doctor.py"
      to: "src/quantum_runtime/runtime/policy.py"
      via: "doctor CI mode projects existing findings into the shared policy envelope"
      pattern: "apply_doctor_policy"
---

<objective>
Add a CI-oriented doctor surface with explicit blocking versus advisory semantics.

Purpose: POLC-03 requires FluxQ to tell CI exactly when doctor findings are blocking, without forcing callers to reverse-engineer the current `issues` and `advisories` lists. This plan adds that CI layer while preserving the legacy doctor contract for existing consumers.
Output: `doctor --ci`, policy-bearing doctor reports, blocking/advisory fields, and verdict-driven exit behavior.
</objective>

<execution_context>
@/Users/xizhao/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xizhao/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-policy-acceptance-gates/04-RESEARCH.md
@.planning/phases/04-policy-acceptance-gates/04-VALIDATION.md
@.planning/phases/04-policy-acceptance-gates/04-03-PLAN.md
@src/quantum_runtime/runtime/doctor.py
@src/quantum_runtime/cli.py
@src/quantum_runtime/runtime/exit_codes.py
@src/quantum_runtime/runtime/policy.py
@tests/test_cli_doctor.py
@tests/test_cli_observability.py

<interfaces>
From `src/quantum_runtime/runtime/doctor.py`:
```python
class DoctorReport(BaseModel):
    schema_version: str = SCHEMA_VERSION
    status: Literal["ok", "degraded", "error"]
    workspace_ok: bool
    issues: list[str]
    advisories: list[str] = Field(default_factory=list)
```

Expected from `src/quantum_runtime/runtime/policy.py` after Plan 04-03:
```python
class PolicyVerdict(BaseModel): ...
def policy_gate_payload(*, policy, verdict, ready, severity, reason_codes, next_actions) -> dict[str, Any]: ...
```
</interfaces>

Rules this plan must preserve:
- `doctor --json` and `doctor --jsonl` without `--ci` keep the existing behavior.
- Do not rewrite the current optional-versus-required backend classification logic; project the existing `issues` and `advisories` lists into CI semantics instead.
- Workspace-safety errors still flow through the existing structured error path rather than the new doctor verdict path.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing doctor CI regressions</name>
  <files>tests/test_cli_doctor.py, tests/test_cli_observability.py, tests/test_runtime_policy.py</files>
  <behavior>
    - Test 1: `apply_doctor_policy()` maps existing `issues` to `blocking_issues`, preserves `advisories` as `advisory_issues`, and returns `verdict.status == "fail"` when blocking findings exist.
    - Test 2: `qrun doctor --ci --json` exits `0` with `blocking_issues == []`, `verdict.status == "pass"`, and `gate.ready == true` when only advisory findings remain.
    - Test 3: `qrun doctor --ci --json` exits `2` with non-empty `blocking_issues` and `gate.ready == false` for workspace integrity failures or required-backend unavailability.
    - Test 4: `qrun doctor --json` without `--ci` keeps the current exit-code and payload behavior.
    - Test 5: `qrun doctor --ci --jsonl` emits a final `doctor_completed` payload containing `blocking_issues`, `advisory_issues`, `verdict`, and `gate`.
  </behavior>
  <action>Extend the existing doctor and observability suites rather than creating a new harness. In `tests/test_cli_doctor.py`, reuse the current optional-unrequested Classiq scenario for the advisory-only case and a workspace or required-backend failure for the blocking case. In `tests/test_runtime_policy.py`, add a compact unit test for the doctor evaluator using a synthetic `DoctorReport`. Preserve the non-`--ci` assertions so backward compatibility is pinned before implementation.</action>
  <verify>
    <automated>./.venv/bin/python -m pytest tests/test_cli_doctor.py tests/test_cli_observability.py tests/test_runtime_policy.py -q --maxfail=1</automated>
  </verify>
  <done>The doctor CI contract is pinned in tests, including advisory-only, blocking, JSONL, and backward-compatible non-`--ci` behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Project doctor findings into the shared policy envelope</name>
  <files>src/quantum_runtime/runtime/policy.py, src/quantum_runtime/runtime/doctor.py</files>
  <action>In `src/quantum_runtime/runtime/policy.py`, add `DoctorPolicy` plus `apply_doctor_policy(*, report, policy)` that treats the existing `issues` list as blocking findings and the existing `advisories` list as advisory findings. Emit stable reason-code prefixes `doctor_blocking_issue:<slug>` and `doctor_advisory_issue:<slug>` using a deterministic lowercase slug transform. In `src/quantum_runtime/runtime/doctor.py`, extend `DoctorReport` additively with `blocking_issues`, `advisory_issues`, `policy`, `verdict`, `reason_codes`, `next_actions`, and `gate`, then add a `ci: bool = False` parameter to `run_doctor()`. When `ci=True`, apply `DoctorPolicy(mode="ci", block_on_issues=True)` before persistence and output. Keep the underlying dependency classification logic unchanged; this plan only exposes a stable CI verdict on top of the existing findings.</action>
  <verify>
    <automated>./.venv/bin/python -m pytest tests/test_runtime_policy.py tests/test_cli_doctor.py -q --maxfail=1</automated>
  </verify>
  <done>Doctor reports now carry an additive CI decision layer without breaking the existing raw findings contract.</done>
</task>

<task type="auto">
  <name>Task 3: Wire `doctor --ci` and verdict-based exit behavior</name>
  <files>src/quantum_runtime/cli.py, src/quantum_runtime/runtime/exit_codes.py</files>
  <action>Add `ci: bool = typer.Option(False, "--ci", help="Emit blocking/advisory CI gate output and return exit code 2 when blocking findings remain.")` to `doctor_command()`, pass `ci=ci` into `run_doctor()`, and keep the non-`--ci` text output unchanged. Update `exit_code_for_doctor()` so `verdict.status == "fail"` returns `EXIT_DEGRADED`, `verdict.status == "pass"` returns `EXIT_OK`, and the current workspace/dependency mapping remains the fallback when no CI verdict was requested. In JSONL mode, emit the final `doctor_completed` event after the CI verdict has been attached so the streamed payload matches the JSON contract.</action>
  <verify>
    <automated>./.venv/bin/python -m pytest tests/test_cli_doctor.py tests/test_cli_observability.py tests/test_runtime_policy.py -q --maxfail=1</automated>
  </verify>
  <done>`qrun doctor --ci` becomes a first-class CI gate, while the default doctor command stays backward compatible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doctor findings -> CI verdict | Existing workspace and dependency findings are translated into blocking versus advisory outcomes for machine consumers. |
| CLI `--ci` flag -> exit-code mapping | Shell-level CI behavior must stay aligned with the machine-readable doctor verdict. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | T | doctor blocking/advisory classification | mitigate | Reuse the existing `issues` versus `advisories` split and only project it into explicit CI-facing fields. |
| T-04-04-02 | R | `doctor --ci` JSON and JSONL output | mitigate | Build the CI payload through the shared policy envelope so JSON, JSONL, and exit-code behavior describe the same blocking state. |
| T-04-04-03 | D | doctor shell-level CI behavior | mitigate | Map `verdict.status == "fail"` to exit code `2` and `pass` to `0` only when `--ci` is active, preserving legacy behavior otherwise. |
</threat_model>

<verification>
Run the doctor CI suite plus the Phase 4 validation gate after this plan:

`./.venv/bin/ruff check src tests && ./.venv/bin/python -m mypy src && ./.venv/bin/python -m pytest tests/test_cli_doctor.py tests/test_cli_observability.py tests/test_runtime_policy.py -q --maxfail=1`
</verification>

<success_criteria>
- POLC-03 has an explicit owner for `qrun doctor --ci`.
- Doctor CI output distinguishes blocking from advisory findings in both JSON and JSONL modes.
- CI exit behavior is verdict-driven without changing the legacy non-`--ci` command behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/04-policy-acceptance-gates/04-policy-acceptance-gates-04-SUMMARY.md`
</output>
