---
phase: 01-canonical-ingress-resolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_cli_ingress_resolution.py
autonomous: true
requirements:
  - INGR-01
  - INGR-02
must_haves:
  truths:
    - "CLI prompt, resolve, and plan ingress paths are proven to stay side-effect-free before `exec`."
    - "Equivalent prompt text, markdown intent, and structured JSON intent produce matching canonical identity fields at the CLI boundary."
  artifacts:
    - path: "tests/test_cli_ingress_resolution.py"
      provides: "CLI regression coverage for pre-exec ingress normalization and no-write behavior"
      min_lines: 120
  key_links:
    - from: "tests/test_cli_ingress_resolution.py"
      to: "src/quantum_runtime/cli.py"
      via: "CliRunner.invoke(app, ...)"
      pattern: "RUNNER\\.invoke\\(app,"
    - from: "tests/test_cli_ingress_resolution.py"
      to: "src/quantum_runtime/runtime/control_plane.py"
      via: "resolve and plan JSON payload assertions"
      pattern: "semantic_hash|workload_hash|workload_id"
---

<objective>
Lock down the agent-facing CLI ingress contract so `prompt`, `resolve`, and `plan` remain side-effect-free and parity-safe before execution.

Purpose: Phase 1 only holds if the actual CLI surface is trustworthy for agents and CI, not just the helper layer.
Output: A focused CLI regression suite that proves no-write behavior and cross-ingress parity for machine-readable payloads.
</objective>

<execution_context>
@/Users/xizhao/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xizhao/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-canonical-ingress-resolution/01-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@src/quantum_runtime/cli.py
@src/quantum_runtime/runtime/control_plane.py
@src/quantum_runtime/runtime/resolve.py
@tests/test_cli_control_plane.py
@tests/test_cli_runtime_gap.py
@examples/intent-ghz.md
@examples/intent-qaoa-maxcut-sweep.md

<interfaces>
From `src/quantum_runtime/runtime/resolve.py`:
```python
class IntentResolution(SchemaPayload): ...
def intent_resolution_from_prompt(text: str) -> IntentResolution: ...
def resolve_runtime_input(..., intent_text: str | None = None) -> ResolvedRuntimeInput: ...
```

From `src/quantum_runtime/runtime/control_plane.py`:
```python
def resolve_runtime_object(..., intent_text: str | None = None) -> ResolveResult: ...
def build_execution_plan(..., intent_text: str | None = None) -> PlanResult: ...
```

From `src/quantum_runtime/cli.py`:
```python
@app.command("prompt")
@app.command("resolve")
@app.command("plan")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create CLI no-write regression coverage for pre-exec ingress</name>
  <files>tests/test_cli_ingress_resolution.py</files>
  <read_first>
- tests/test_cli_ingress_resolution.py (new file)
- tests/test_cli_control_plane.py
- tests/test_cli_runtime_gap.py
- src/quantum_runtime/cli.py
- .planning/phases/01-canonical-ingress-resolution/01-RESEARCH.md
  </read_first>
  <behavior>
- Test 1: `qrun prompt ... --json` returns schema version `0.3.0`, normalized intent defaults, and does not create `.quantum` when invoked from an empty temp directory.
- Test 2: `qrun resolve ... --json` against a temp workspace path returns a machine-readable payload and leaves `workspace.json`, `events.jsonl`, `trace/events.ndjson`, `intents/`, `plans/`, `specs/`, and `reports/` absent.
- Test 3: `qrun plan ... --json` remains a dry run and leaves the workspace absent, matching the Phase 1 pre-exec contract.
  </behavior>
  <action>Create `tests/test_cli_ingress_resolution.py` with `CliRunner` integration tests that run `prompt`, `resolve`, and `plan` in JSON mode against temporary directories. Use explicit filesystem assertions for the absence of `.quantum`, `workspace.json`, `events.jsonl`, `trace/events.ndjson`, `intents/history`, `plans/history`, `specs/current.json`, and `reports/latest.json`. Follow the existing CLI test style: module-level `RUNNER`, `PROJECT_ROOT`, `tmp_path`, and `json.loads(result.stdout)` assertions.</action>
  <acceptance_criteria>
- `rg -n "test_qrun_prompt_json_does_not_create_workspace_artifacts|test_qrun_resolve_json_is_side_effect_free|test_qrun_plan_json_is_side_effect_free" tests/test_cli_ingress_resolution.py`
- `rg -n "workspace\\.json|events\\.jsonl|trace/events\\.ndjson|intents/history|plans/history|specs/current\\.json|reports/latest\\.json" tests/test_cli_ingress_resolution.py`
  </acceptance_criteria>
  <verify>
    <automated>uv run --python 3.11 --extra dev --extra qiskit pytest -q tests/test_cli_ingress_resolution.py</automated>
  </verify>
  <done>`tests/test_cli_ingress_resolution.py` proves that `prompt`, `resolve`, and `plan` stay side-effect-free before `exec`, and the targeted pytest file passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add cross-ingress CLI parity assertions for resolve and plan</name>
  <files>tests/test_cli_ingress_resolution.py</files>
  <read_first>
- tests/test_cli_ingress_resolution.py
- src/quantum_runtime/cli.py
- src/quantum_runtime/runtime/control_plane.py
- src/quantum_runtime/runtime/resolve.py
- src/quantum_runtime/intent/parser.py
- examples/intent-qaoa-maxcut-sweep.md
  </read_first>
  <behavior>
- Test 1: Equivalent prompt text, markdown intent, and structured JSON intent passed to `qrun resolve --json` return the same `qspec.pattern`, `qspec.workload_id`, `qspec.workload_hash`, and `qspec.semantic_hash`.
- Test 2: The same three ingress forms passed to `qrun plan --json` return the same `qspec` identity block, `execution.selected_backends`, and `artifacts_expected`.
- Test 3: The parity assertions compare canonical `QSpec` semantics rather than raw prompt text or source-path metadata, so `source_kind` and `source` may differ without failing the test.
  </behavior>
  <action>Extend `tests/test_cli_ingress_resolution.py` with helper builders that serialize the markdown QAOA sweep example into structured JSON via `parse_intent_file(...).model_dump_json(indent=2)` and provide an equivalent inline prompt string for `--intent-text`. Compare normalized payload subsets only: `qspec.pattern`, `qspec.width`, `qspec.layers`, `qspec.workload_id`, `qspec.workload_hash`, `qspec.semantic_hash`, `plan.execution.selected_backends`, and `plan.artifacts_expected`. Do not assert raw `intent.source`, `intent.source_kind`, or filesystem paths, because those are expected to differ by ingress form.</action>
  <acceptance_criteria>
- `rg -n "test_qrun_resolve_json_keeps_prompt_markdown_json_parity|test_qrun_plan_json_keeps_prompt_markdown_json_parity" tests/test_cli_ingress_resolution.py`
- `rg -n "workload_id|workload_hash|semantic_hash|selected_backends|artifacts_expected" tests/test_cli_ingress_resolution.py`
  </acceptance_criteria>
  <verify>
    <automated>uv run --python 3.11 --extra dev --extra qiskit pytest -q tests/test_cli_ingress_resolution.py</automated>
  </verify>
  <done>The CLI regression suite proves cross-ingress parity for `resolve` and `plan` without treating source metadata differences as failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ingress text/file -> CLI parser | Untrusted prompt, markdown, or JSON intent data crosses into the control-plane normalization path. |
| CLI dry-run commands -> workspace filesystem | Phase 1 requires pre-exec ingress commands to avoid mutating workspace state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | T | `prompt` / `resolve` / `plan` CLI paths | mitigate | Add regression tests that fail if pre-exec commands create workspace artifacts or event logs. |
| T-01-01-02 | T | CLI identity fields returned from `resolve` / `plan` | mitigate | Add parity tests that compare `workload_id`, `workload_hash`, and `semantic_hash` across equivalent prompt, markdown, and JSON inputs. |
</threat_model>

<verification>
Run `uv run --python 3.11 --extra dev --extra qiskit pytest -q tests/test_cli_ingress_resolution.py tests/test_cli_control_plane.py` after the file is added to confirm the new coverage passes alongside the existing dry-run control-plane tests.
</verification>

<success_criteria>
`tests/test_cli_ingress_resolution.py` exists, passes, and fails if pre-exec CLI commands start writing workspace artifacts or if equivalent ingress forms diverge on canonical identity fields.
</success_criteria>

<output>
After completion, create `.planning/phases/01-canonical-ingress-resolution/01-01-SUMMARY.md`
</output>
