---
phase: 01-canonical-ingress-resolution
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_runtime_ingress_resolution.py
  - tests/test_qspec_semantics.py
autonomous: true
requirements:
  - INGR-02
  - INGR-03
must_haves:
  truths:
    - "Shared resolver and control-plane helpers produce the same canonical `QSpec` from equivalent prompt, markdown, and structured JSON inputs."
    - "Semantic summary hashing remains tied to normalized `QSpec` semantics rather than ingress source metadata."
  artifacts:
    - path: "tests/test_runtime_ingress_resolution.py"
      provides: "Runtime-layer parity coverage for canonical ingress normalization"
      min_lines: 110
    - path: "tests/test_qspec_semantics.py"
      provides: "Semantic hash regression coverage for equivalent and non-equivalent workloads"
      min_lines: 80
  key_links:
    - from: "tests/test_runtime_ingress_resolution.py"
      to: "src/quantum_runtime/runtime/resolve.py"
      via: "direct calls to resolve_runtime_input"
      pattern: "resolve_runtime_input"
    - from: "tests/test_qspec_semantics.py"
      to: "src/quantum_runtime/qspec/semantics.py"
      via: "direct calls to summarize_qspec_semantics"
      pattern: "summarize_qspec_semantics"
---

<objective>
Pin the shared normalization and semantic-hash contracts so equivalent ingress forms map to the same canonical runtime object regardless of source representation.

Purpose: CLI parity is not enough if the underlying resolver or semantic summary can drift under refactors; this plan locks the lower layers directly.
Output: Runtime-level tests for canonicalization and semantic summary identity across supported ingress forms.
</objective>

<execution_context>
@/Users/xizhao/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xizhao/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-canonical-ingress-resolution/01-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@src/quantum_runtime/intent/parser.py
@src/quantum_runtime/runtime/resolve.py
@src/quantum_runtime/runtime/control_plane.py
@src/quantum_runtime/qspec/semantics.py
@tests/test_intent_parser.py
@tests/test_planner.py
@tests/golden/qspec_ghz.json
@tests/golden/qspec_qaoa_maxcut.json
@examples/intent-ghz.md
@examples/intent-qaoa-maxcut-sweep.md

<interfaces>
From `src/quantum_runtime/intent/parser.py`:
```python
def parse_intent_file(path: Path) -> IntentModel: ...
def parse_intent_text(text: str) -> IntentModel: ...
def parse_intent_json_file(path: Path) -> IntentModel: ...
def parse_intent_json_text(text: str) -> IntentModel: ...
```

From `src/quantum_runtime/runtime/resolve.py`:
```python
class ResolvedRuntimeInput(BaseModel): ...
def resolve_runtime_input(..., intent_text: str | None = None) -> ResolvedRuntimeInput: ...
```

From `src/quantum_runtime/runtime/control_plane.py`:
```python
def build_execution_plan(..., intent_text: str | None = None) -> PlanResult: ...
def resolve_runtime_object(..., intent_text: str | None = None) -> ResolveResult: ...
```

From `src/quantum_runtime/qspec/semantics.py`:
```python
def summarize_qspec_semantics(qspec: QSpec) -> dict[str, Any]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shared resolver parity coverage across supported ingress forms</name>
  <files>tests/test_runtime_ingress_resolution.py</files>
  <read_first>
- tests/test_runtime_ingress_resolution.py (new file)
- src/quantum_runtime/runtime/resolve.py
- src/quantum_runtime/runtime/control_plane.py
- src/quantum_runtime/intent/parser.py
- tests/test_intent_parser.py
- tests/test_planner.py
  </read_first>
  <behavior>
- Test 1: Equivalent prompt text, markdown intent, and structured JSON intent passed to `resolve_runtime_input` produce identical canonical `qspec.model_dump(mode="json")` payloads and matching `requested_exports`.
- Test 2: The same three ingress forms passed to `resolve_runtime_object` and `build_execution_plan` return matching `qspec.workload_id`, `qspec.workload_hash`, `qspec.semantic_hash`, and `execution.selected_backends`.
- Test 3: The shared resolver still rejects zero-input and multi-input calls with `ValueError("expected_exactly_one_input")`.
  </behavior>
  <action>Create `tests/test_runtime_ingress_resolution.py` that imports leaf functions from `quantum_runtime.runtime.resolve` and `quantum_runtime.runtime.control_plane`. Build one structured JSON fixture from `examples/intent-qaoa-maxcut-sweep.md` via `parse_intent_file(...).model_dump_json(indent=2)` and one equivalent inline prompt string for `intent_text`. Compare canonical `QSpec` payloads and plan identity fields, but do not compare `source` or `source_kind` metadata because those intentionally vary by ingress form. Include explicit assertions that helper calls remain dry-run and do not require a pre-existing workspace.</action>
  <acceptance_criteria>
- `rg -n "test_resolve_runtime_input_keeps_prompt_markdown_json_qspec_parity|test_resolve_runtime_object_and_plan_keep_cross_ingress_identity|test_resolve_runtime_input_requires_exactly_one_input" tests/test_runtime_ingress_resolution.py`
- `rg -n "expected_exactly_one_input|workload_id|workload_hash|semantic_hash|selected_backends" tests/test_runtime_ingress_resolution.py`
  </acceptance_criteria>
  <verify>
    <automated>uv run --python 3.11 --extra dev --extra qiskit pytest -q tests/test_runtime_ingress_resolution.py</automated>
  </verify>
  <done>`tests/test_runtime_ingress_resolution.py` proves the shared resolver and control-plane helpers canonicalize supported ingress forms identically and preserve the one-input rule.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add semantic summary regression coverage for equivalent and distinct workloads</name>
  <files>tests/test_qspec_semantics.py</files>
  <read_first>
- tests/test_qspec_semantics.py (new file)
- src/quantum_runtime/qspec/semantics.py
- src/quantum_runtime/intent/parser.py
- src/quantum_runtime/intent/planner.py
- tests/test_planner.py
- tests/golden/qspec_ghz.json
- tests/golden/qspec_qaoa_maxcut.json
  </read_first>
  <behavior>
- Test 1: `summarize_qspec_semantics()` returns identical `workload_id`, `workload_hash`, `execution_hash`, and `semantic_hash` for equivalent `QSpec` objects built from prompt, markdown, and structured JSON ingress.
- Test 2: Distinct canonical workloads such as GHZ versus QAOA produce different `workload_hash` and `semantic_hash`.
- Test 3: The current contract stays pinned: `semantic_hash == execution_hash`, while `workload_hash` remains a separate field derived from workload-only semantics.
  </behavior>
  <action>Create `tests/test_qspec_semantics.py` with focused unit tests that build equivalent `QSpec` objects through `parse_intent_text`, `parse_intent_file`, and structured JSON parsing plus `plan_to_qspec`, then compare the full identity block from `summarize_qspec_semantics()`. Add one distinct-workload regression using existing GHZ and QAOA examples so future refactors cannot collapse non-equivalent workloads into the same hashes. Do not change the semantics payload schema or broaden the planner pattern catalog in this phase.</action>
  <acceptance_criteria>
- `rg -n "test_summarize_qspec_semantics_keeps_equivalent_ingress_hashes_aligned|test_summarize_qspec_semantics_distinguishes_different_workloads|test_semantic_hash_tracks_execution_hash_contract" tests/test_qspec_semantics.py`
- `rg -n "workload_hash|execution_hash|semantic_hash|workload_id" tests/test_qspec_semantics.py`
  </acceptance_criteria>
  <verify>
    <automated>uv run --python 3.11 --extra dev --extra qiskit pytest -q tests/test_qspec_semantics.py</automated>
  </verify>
  <done>`tests/test_qspec_semantics.py` locks down the current semantic summary contract for equivalent and distinct workloads without widening Phase 1 scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parser and resolver helpers -> canonical `QSpec` | Ingress-specific parsing must collapse different representations into one canonical runtime object. |
| canonical `QSpec` -> semantic identity summary | Hash fields must reflect normalized workload semantics, not incidental source metadata. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | T | `resolve_runtime_input` and control-plane parity helpers | mitigate | Add runtime-layer parity tests for prompt, markdown, and JSON ingress plus the exact-one-input guard. |
| T-01-02-02 | T | `summarize_qspec_semantics` hash contract | mitigate | Add semantic summary tests that pin equivalent-workload hashes together and distinct-workload hashes apart. |
</threat_model>

<verification>
Run `uv run --python 3.11 --extra dev --extra qiskit pytest -q tests/test_runtime_ingress_resolution.py tests/test_qspec_semantics.py` after both files are added, then run the two new files together with `tests/test_planner.py` if any planner-facing assertion needs confirmation.
</verification>

<success_criteria>
The runtime-layer regression files exist, pass, and fail if canonical `QSpec` identity drifts across equivalent ingress forms or if semantic hashes stop reflecting the current normalized contract.
</success_criteria>

<output>
After completion, create `.planning/phases/01-canonical-ingress-resolution/01-02-SUMMARY.md`
</output>
