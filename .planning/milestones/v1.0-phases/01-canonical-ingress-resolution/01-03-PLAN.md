---
phase: 01-canonical-ingress-resolution
plan: 03
type: execute
wave: 2
depends_on:
  - 01-02
files_modified:
  - tests/test_qspec_semantics.py
autonomous: true
gap_closure: true
requirements:
  - INGR-03
must_haves:
  truths:
    - "Equivalent markdown text, markdown file, and structured JSON derived from the same intent keep the same canonical workload identity and semantic hash."
    - "Execution-only `QSpec` changes preserve workload identity while changing execution-oriented semantic hashes."
  artifacts:
    - path: "tests/test_qspec_semantics.py"
      provides: "Expanded semantic identity regression coverage for equivalent ingress and execution-only divergence"
      min_lines: 80
  key_links:
    - from: "tests/test_qspec_semantics.py"
      to: "src/quantum_runtime/qspec/semantics.py"
      via: "direct calls to summarize_qspec_semantics"
      pattern: "summarize_qspec_semantics"
---

<objective>
Close the only remaining Phase 1 verification gap by expanding the semantic-hash regression artifact until it satisfies the original plan contract with substantive tests.

Purpose: `01-VERIFICATION.md` reports that Phase 1 runtime behavior already verifies and the sole remaining miss is the `tests/test_qspec_semantics.py` artifact threshold declared in `01-02-PLAN.md`, so this gap plan should add semantic evidence only.
Output: One focused semantic regression update in `tests/test_qspec_semantics.py` that raises the file past the planned threshold and preserves Phase 1 canonical-runtime guarantees.
</objective>

<execution_context>
@/Users/xizhao/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xizhao/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-canonical-ingress-resolution/01-VERIFICATION.md
@.planning/phases/01-canonical-ingress-resolution/01-02-PLAN.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@src/quantum_runtime/qspec/semantics.py
@src/quantum_runtime/intent/parser.py
@src/quantum_runtime/intent/planner.py
@tests/test_qspec_semantics.py
@tests/golden/qspec_ghz.json
@tests/golden/qspec_qaoa_maxcut.json
@examples/intent-ghz.md

<interfaces>
From `src/quantum_runtime/qspec/semantics.py`:
```python
def summarize_qspec_semantics(qspec: QSpec) -> dict[str, Any]: ...
```

From `src/quantum_runtime/intent/parser.py`:
```python
def parse_intent_file(path: Path) -> IntentModel: ...
def parse_intent_text(text: str) -> IntentModel: ...
def parse_intent_json_text(text: str) -> IntentModel: ...
```

From `src/quantum_runtime/intent/planner.py`:
```python
def plan_to_qspec(intent: IntentModel) -> QSpec: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand semantic identity regressions past the remaining artifact threshold</name>
  <files>tests/test_qspec_semantics.py</files>
  <read_first>
- tests/test_qspec_semantics.py
- src/quantum_runtime/qspec/semantics.py
- src/quantum_runtime/intent/parser.py
- src/quantum_runtime/intent/planner.py
- tests/golden/qspec_ghz.json
- tests/golden/qspec_qaoa_maxcut.json
- .planning/phases/01-canonical-ingress-resolution/01-VERIFICATION.md
  </read_first>
  <behavior>
- Test 1: Truly equivalent GHZ ingress fixtures built from the same markdown intent source produce identical `workload_id`, `workload_hash`, `execution_hash`, and `semantic_hash` across markdown text, markdown file, and structured JSON.
- Test 2: The raw prompt `Build a 4-qubit GHZ circuit and measure all qubits.` keeps `workload_id` and `workload_hash` aligned with the markdown-derived GHZ fixture, but does not claim `execution_hash` or `semantic_hash` equality when the markdown fixture carries extra execution constraints.
- Test 3: Execution-only changes on one canonical GHZ `QSpec` preserve `workload_id` and `workload_hash` while changing `execution_hash` and `semantic_hash`.
- Test 4: Distinct canonical workloads such as GHZ and QAOA still produce different `workload_hash` and `semantic_hash`.
  </behavior>
  <action>Extend `tests/test_qspec_semantics.py` with substantive semantic-summary cases instead of filler lines. Keep the existing equivalent-ingress and distinct-workload assertions, then split the GHZ regression expectations cleanly in the same artifact. First, build a truly equivalent trio from `examples/intent-ghz.md` by using the markdown file contents as markdown text, the markdown file path itself, and structured JSON derived from that same markdown intent; assert identical `workload_id`, `workload_hash`, `execution_hash`, and `semantic_hash` across those three `QSpec` objects. Second, keep the exact raw prompt string `Build a 4-qubit GHZ circuit and measure all qubits.` as a separate regression against the markdown-derived GHZ fixture, but scope it to `workload_id` and `workload_hash` parity only, with an explicit assertion that `execution_hash` and `semantic_hash` may differ because the markdown fixture carries additional execution constraints such as `max_width` and `max_depth`. Then add the execution-settings divergence case that deep-copies one canonical GHZ `QSpec`, changes execution-only fields such as `backend_preferences` and `constraints.shots`, and asserts `workload_id` plus `workload_hash` stay equal while `execution_hash` and `semantic_hash` change. Use `QSpec.model_copy(deep=True)` or the repository's equivalent deep-copy pattern rather than hand-building new models. Keep the file focused on the semantic contract from `01-02-PLAN.md`, and end above 80 real lines.</action>
  <acceptance_criteria>
- `rg -n "test_summarize_qspec_semantics_keeps_equivalent_ghz_ingress_hashes_aligned|test_summarize_qspec_semantics_keeps_raw_prompt_ghz_workload_hashes_aligned|test_summarize_qspec_semantics_changes_execution_hash_without_changing_workload_hash" tests/test_qspec_semantics.py`
- `rg -n "Build a 4-qubit GHZ circuit and measure all qubits\\.|model_copy\\(deep=True\\)|constraints\\.shots|backend_preferences" tests/test_qspec_semantics.py`
- `wc -l tests/test_qspec_semantics.py` reports at least `80`
  </acceptance_criteria>
  <verify>
    <automated>uv run --python 3.11 --extra dev --extra qiskit pytest -q tests/test_qspec_semantics.py</automated>
  </verify>
  <done>`tests/test_qspec_semantics.py` clears the original `min_lines: 80` artifact contract and protects true equivalent-ingress identity, raw-prompt workload parity, and workload-versus-execution hash separation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| supported ingress fixtures -> canonical `QSpec` summary | Equivalent prompt, markdown, and structured JSON inputs must collapse into the same semantic identity without source-format drift. |
| canonical workload semantics -> execution summary fields | Execution-only settings must change execution semantics without mutating workload identity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | T | `summarize_qspec_semantics` equivalent-ingress contract | mitigate | Add GHZ regressions in `tests/test_qspec_semantics.py` that use markdown text, markdown file, and structured JSON derived from the same intent so truly equivalent ingress forms keep the same workload and semantic identity fields. |
| T-01-03-02 | T | `summarize_qspec_semantics` workload-versus-execution separation | mitigate | Add a raw-prompt GHZ workload-parity regression plus a deep-copied execution-only divergence regression so the suite distinguishes stable `workload_hash` from changed `execution_hash` and `semantic_hash`. |
</threat_model>

<verification>
Run `uv run --python 3.11 --extra dev --extra qiskit pytest -q tests/test_qspec_semantics.py tests/test_runtime_ingress_resolution.py`, then confirm `wc -l tests/test_qspec_semantics.py` reports at least `80`.
</verification>

<success_criteria>
Phase 1 has one focused semantic gap-closure slice, no override file is needed, and the verifier can close the remaining artifact contract using stronger automated evidence rather than paperwork.
</success_criteria>

<output>
After completion, create `.planning/phases/01-canonical-ingress-resolution/01-03-SUMMARY.md`
</output>
