---
phase: 02-trusted-revision-artifacts
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/quantum_runtime/runtime/imports.py
  - src/quantum_runtime/runtime/control_plane.py
  - tests/test_runtime_imports.py
  - tests/test_cli_control_plane.py
autonomous: true
gap_closure: true
requirements:
  - RUNT-03
must_haves:
  truths:
    - "Resolving the current workspace cannot be downgraded from trusted to legacy by editing mutable `reports/latest.json` alone while immutable history and manifest files still match."
    - "`qrun show --json` treats genuinely old runs as legacy-compatible when trusted digest evidence is absent, even if `schema_version` is still present."
    - "Trusted current-workspace replay continues to fail closed when immutable history evidence drifts."
  artifacts:
    - path: "src/quantum_runtime/runtime/imports.py"
      provides: "Current-workspace replay resolution pinned to immutable revision history"
      min_lines: 1100
    - path: "src/quantum_runtime/runtime/control_plane.py"
      provides: "Legacy detection aligned with replay-integrity trust evidence"
      min_lines: 600
    - path: "tests/test_runtime_imports.py"
      provides: "Regression coverage for current-workspace trust pinning"
      min_lines: 460
    - path: "tests/test_cli_control_plane.py"
      provides: "Show/control-plane regressions for legacy detection and mutable-alias tampering"
      min_lines: 900
  key_links:
    - from: "src/quantum_runtime/runtime/imports.py"
      to: "src/quantum_runtime/workspace/manifest.py"
      via: "Use `current_revision` to reopen immutable `reports/history/<revision>.json` and `specs/history/<revision>.json` when available"
      pattern: "current_revision|reports/history|specs/history"
    - from: "src/quantum_runtime/runtime/control_plane.py"
      to: "src/quantum_runtime/runtime/imports.py"
      via: "Legacy fallback uses replay-integrity evidence rather than a missing `schema_version` heuristic"
      pattern: "_is_legacy_report_payload|artifact_output_digests|replay_integrity"
    - from: "tests/test_runtime_imports.py"
      to: "src/quantum_runtime/runtime/imports.py"
      via: "Tamper `reports/latest.json` only and assert current-workspace resolution stays trusted against history"
      pattern: "resolve_workspace_current|resolve_import_reference"
    - from: "tests/test_cli_control_plane.py"
      to: "src/quantum_runtime/runtime/control_plane.py"
      via: "Show current-workspace trust pinning and legacy-manifest synthesis semantics"
      pattern: "show|legacy|replay_integrity"
---

<objective>
Close the remaining Phase 2 replay/control-plane trust gaps by pinning current-workspace resolution to immutable history and by aligning legacy detection with trusted replay evidence.

Purpose: Right now a mutable `reports/latest.json` edit can downgrade a trusted current workspace to `legacy`, and `show` still decides “legacy” using `schema_version` instead of the Phase 2 replay-integrity contract.
Output: History-pinned current-workspace resolution in `imports.py`, replay-integrity-based legacy detection in `control_plane.py`, and focused regressions proving both behaviors.
</objective>

<execution_context>
@/Users/xizhao/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xizhao/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-trusted-revision-artifacts/02-VERIFICATION.md
@.planning/phases/02-trusted-revision-artifacts/02-REVIEW.md
@.planning/phases/02-trusted-revision-artifacts/02-02-PLAN.md
@.planning/phases/02-trusted-revision-artifacts/02-02-SUMMARY.md
@.planning/phases/02-trusted-revision-artifacts/02-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@src/quantum_runtime/runtime/imports.py
@src/quantum_runtime/runtime/control_plane.py
@tests/test_runtime_imports.py
@tests/test_cli_control_plane.py

<interfaces>
From `src/quantum_runtime/runtime/imports.py`:
```python
def resolve_workspace_current(workspace_root: Path) -> ImportResolution: ...
def resolve_report_revision(workspace_root: Path, revision: str) -> ImportResolution: ...
def _build_resolution(...) -> ImportResolution: ...
```

From `src/quantum_runtime/runtime/control_plane.py`:
```python
def show_run(*, workspace_root: Path, revision: str | None = None) -> ShowResult: ...
def workspace_status(*, workspace_root: Path) -> StatusResult: ...
def _is_legacy_report_payload(report_payload: dict[str, Any]) -> bool: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add regressions for current-workspace trust pinning and legacy detection</name>
  <files>tests/test_runtime_imports.py, tests/test_cli_control_plane.py</files>
  <behavior>
- Test 1: Mutating only `reports/latest.json` to remove trusted replay evidence does not downgrade `resolve_workspace_current(...)`; the current workspace still resolves against the selected revision's immutable history and remains trusted.
- Test 2: `qrun show --json` for the current workspace still reports trusted replay when only mutable aliases are tampered and the immutable history manifest/report remain valid.
- Test 3: `qrun show --json` still synthesizes a legacy-compatible manifest for an old-style report that is missing trusted digest evidence, but the test removes only digest evidence and manifest files, not `schema_version`.
  </behavior>
  <action>Add focused regressions in `tests/test_runtime_imports.py` and `tests/test_cli_control_plane.py` before changing implementation. Model the mutable-alias trust hole precisely: leave `reports/history/<revision>.json`, `specs/history/<revision>.json`, and the revision manifest untouched, mutate only `reports/latest.json`, then assert current-workspace resolution and `show` remain trusted. Replace the existing legacy show setup that deletes `schema_version`; Phase 2 legacy compatibility must now be exercised by missing trusted digest evidence instead.</action>
  <verify>
    <automated>uv run --python 3.11 --extra dev --extra qiskit pytest -q tests/test_runtime_imports.py tests/test_cli_control_plane.py</automated>
  </verify>
  <done>The new regressions fail red against the current mutable-alias downgrade path and the stale `schema_version`-only legacy predicate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin current-workspace replay to immutable history and align legacy fallback with replay-integrity evidence</name>
  <files>src/quantum_runtime/runtime/imports.py, src/quantum_runtime/runtime/control_plane.py, tests/test_runtime_imports.py, tests/test_cli_control_plane.py</files>
  <behavior>
- Behavior 1: `resolve_workspace_current(...)` prefers `reports/history/<current_revision>.json` and `specs/history/<current_revision>.json` when the workspace has a real current revision, using mutable aliases only as compatibility fallback when history artifacts do not exist.
- Behavior 2: `_is_legacy_report_payload(...)` recognizes legacy-compatible runs from missing trusted replay evidence, not just missing `schema_version`, so `show` can synthesize a manifest for genuine pre-Phase-2 reports without weakening trusted runs.
- Behavior 3: Trusted current-workspace replay still fails closed if the immutable history report, qspec, or manifest evidence drifts, because the resolver now consults history artifacts first.
  </behavior>
  <action>Update `src/quantum_runtime/runtime/imports.py` so current-workspace resolution reopens immutable history artifacts for `manifest.current_revision` instead of trusting `manifest.active_report` and `manifest.active_spec` aliases when history copies exist. Update `src/quantum_runtime/runtime/control_plane.py` so legacy fallback is keyed to the Phase 2 replay-integrity contract, specifically the absence of trusted artifact-digest evidence, rather than `schema_version` alone. Preserve compatibility for true legacy workspaces and do not widen scope into baseline, compare policy, or workspace-locking work in this plan.</action>
  <verify>
    <automated>uv run --python 3.11 --extra dev --extra qiskit pytest -q tests/test_runtime_imports.py tests/test_cli_control_plane.py</automated>
  </verify>
  <done>Mutable alias edits no longer downgrade a trusted current workspace, and `show` handles genuine legacy reports using replay-integrity evidence instead of a stale schema-version heuristic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workspace manifest aliases -> current-workspace replay | Mutable alias files cross into current-workspace import resolution and can weaken trust if not replaced by immutable history. |
| report payload -> control-plane legacy fallback | `show` decides whether a missing manifest is acceptable based on report metadata; the predicate must distinguish legacy from trusted runs correctly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | T | `src/quantum_runtime/runtime/imports.py` | mitigate | Reopen the current workspace from immutable `reports/history/<revision>.json` and `specs/history/<revision>.json` when available so `latest` alias edits cannot downgrade trust. |
| T-02-04-02 | S | `src/quantum_runtime/runtime/control_plane.py` | mitigate | Base legacy fallback on missing trusted digest evidence rather than `schema_version`, so trusted reports cannot masquerade as legacy and real legacy reports still reopen. |
| T-02-04-03 | R | replay/control-plane regression suites | mitigate | Add explicit current-workspace and `show` regressions that mutate only aliases, preserving immutable history, to prove the trust boundary is pinned correctly. |
</threat_model>

<verification>
Run `uv run --python 3.11 --extra dev --extra qiskit pytest -q tests/test_runtime_imports.py tests/test_cli_control_plane.py`.
</verification>

<success_criteria>
The current workspace resolves from immutable history for its selected revision, mutable `latest` alias edits cannot downgrade trusted replay to `legacy`, and `show` treats genuine legacy reports according to replay-integrity evidence rather than `schema_version`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-trusted-revision-artifacts/02-04-SUMMARY.md`
</output>
