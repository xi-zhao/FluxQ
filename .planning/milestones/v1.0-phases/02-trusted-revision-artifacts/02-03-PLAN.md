---
phase: 02-trusted-revision-artifacts
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/quantum_runtime/runtime/run_manifest.py
  - tests/test_runtime_revision_artifacts.py
autonomous: true
gap_closure: true
requirements:
  - RUNT-01
  - RUNT-03
must_haves:
  truths:
    - "The persisted run manifest rejects `intent`, `plan`, and revision-event references that do not point to the selected revision's canonical history files."
    - "Phase 2 keeps a substantive revision-artifact regression suite for completeness and immutability without using a frontmatter override."
    - "A later run cannot satisfy trusted manifest evidence by re-pointing new manifest blocks at unrelated files with matching hashes."
  artifacts:
    - path: "src/quantum_runtime/runtime/run_manifest.py"
      provides: "Canonical-path validation for additive manifest trust blocks"
      min_lines: 280
    - path: "tests/test_runtime_revision_artifacts.py"
      provides: "Revision-artifact completeness, immutability, and manifest path-binding regressions"
      min_lines: 140
  key_links:
    - from: "src/quantum_runtime/runtime/run_manifest.py"
      to: "src/quantum_runtime/workspace/paths.py"
      via: "Derive canonical history paths for `intent`, `plan`, `events_jsonl`, and `trace_ndjson` from `WorkspacePaths`"
      pattern: "WorkspacePaths\\(root=workspace_root\\)"
    - from: "tests/test_runtime_revision_artifacts.py"
      to: "src/quantum_runtime/runtime/run_manifest.py"
      via: "Tamper manifest blocks and assert path-bound integrity failures"
      pattern: "load_run_manifest|parse_and_validate_run_manifest|RunManifestIntegrityError"
---

<objective>
Close the remaining Phase 2 plan-01 gap by making the additive manifest trust blocks path-bound to revision history and by expanding the regression file into a full contract suite.

Purpose: The verifier gap is not just a line-count heuristic. The current manifest validator can still trust `intent`, `plan`, and event references that hash-check but point somewhere else, which weakens the immutable revision contract.
Output: Path-bound manifest validation in `run_manifest.py` and a meaningful `tests/test_runtime_revision_artifacts.py` suite that clears the declared artifact threshold.
</objective>

<execution_context>
@/Users/xizhao/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xizhao/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-trusted-revision-artifacts/02-VERIFICATION.md
@.planning/phases/02-trusted-revision-artifacts/02-REVIEW.md
@.planning/phases/02-trusted-revision-artifacts/02-01-PLAN.md
@.planning/phases/02-trusted-revision-artifacts/02-01-SUMMARY.md
@.planning/phases/02-trusted-revision-artifacts/02-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@src/quantum_runtime/runtime/run_manifest.py
@src/quantum_runtime/workspace/paths.py
@tests/test_runtime_revision_artifacts.py

<interfaces>
From `src/quantum_runtime/runtime/run_manifest.py`:
```python
def load_run_manifest(
    *,
    workspace_root: Path,
    revision: str,
    expected_qspec_path: Path,
    expected_report_path: Path,
) -> dict[str, Any] | None: ...

def parse_and_validate_run_manifest(
    *,
    path: Path,
    expected_revision: str,
    expected_qspec_path: Path,
    expected_report_path: Path,
) -> dict[str, Any]: ...

class RunManifestIntegrityError(ValueError):
    mismatches: list[str]
    details: dict[str, Any]
```

From `src/quantum_runtime/workspace/paths.py`:
```python
class WorkspacePaths:
    def intent_history_json(self, revision: str) -> Path: ...
    def plan_history_json(self, revision: str) -> Path: ...
    def manifest_history_json(self, revision: str) -> Path: ...
    def event_history_jsonl(self, revision: str) -> Path: ...
    def trace_history_ndjson(self, revision: str) -> Path: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand revision-artifact regressions to cover path-bound manifest evidence</name>
  <files>tests/test_runtime_revision_artifacts.py</files>
  <behavior>
- Test 1: The existing artifact-completeness contract still proves one execution persists `intent`, `plan`, `qspec`, `report`, `manifest`, `events/history/<revision>.jsonl`, and `trace/history/<revision>.ndjson`.
- Test 2: If `intent`, `plan`, `events.events_jsonl`, or `events.trace_ndjson` is rewritten to a same-hash file outside the canonical revision history location, manifest validation fails with a deterministic integrity error instead of accepting the redirected block.
- Test 3: The immutability regression still proves a later execution leaves the earlier revision's manifest and event snapshots byte-identical.
  </behavior>
  <action>Replace the current minimal Phase 2 regression file with a fuller contract suite, but keep it focused on revision artifacts. Add meaningful tamper cases for the additive manifest trust blocks, using `load_run_manifest(...)` or `parse_and_validate_run_manifest(...)` directly so the suite proves the trust boundary at the manifest layer. Use the extra assertions to clear the `min_lines: 140` gate with real coverage; do not add a verifier override, filler helpers, or unrelated import/control-plane behavior here.</action>
  <verify>
    <automated>bash -lc 'uv run --python 3.11 --extra dev --extra qiskit pytest -q tests/test_runtime_revision_artifacts.py && python -c "from pathlib import Path; assert len(Path(\"tests/test_runtime_revision_artifacts.py\").read_text().splitlines()) >= 140"'</automated>
  </verify>
  <done>`tests/test_runtime_revision_artifacts.py` exceeds the declared threshold through substantive coverage and fails red until manifest path-binding is implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind additive manifest trust blocks to canonical revision history paths</name>
  <files>src/quantum_runtime/runtime/run_manifest.py, tests/test_runtime_revision_artifacts.py</files>
  <behavior>
- Behavior 1: Manifest validation derives the expected `intent`, `plan`, `events/history/<revision>.jsonl`, and `trace/history/<revision>.ndjson` paths from `WorkspacePaths(root=workspace_root)` plus `revision`, then rejects mismatches before hash validation.
- Behavior 2: Hash validation for those optional blocks still runs after the path check, so tampering with content at the canonical history file also fails closed.
- Behavior 3: Older manifests that omit the additive blocks remain readable because absence stays optional; only present blocks are path-bound and validated.
  </behavior>
  <action>Update `load_run_manifest(...)` and `parse_and_validate_run_manifest(...)` so the additive `intent`, `plan`, and `events` sections are validated like the existing `qspec` and `report` blocks: canonical history path first, then existence, then hash. Record mismatch details for each redirected block so downstream `run_manifest_integrity_invalid` surfaces stay machine-readable. Keep the schema additive and do not introduce alias fallbacks, workspace-current special cases, or control-plane logic in this plan.</action>
  <verify>
    <automated>bash -lc 'uv run --python 3.11 --extra dev --extra qiskit pytest -q tests/test_runtime_revision_artifacts.py && python -c "from pathlib import Path; assert len(Path(\"tests/test_runtime_revision_artifacts.py\").read_text().splitlines()) >= 140"'</automated>
  </verify>
  <done>The manifest trust contract now rejects redirected `intent`/`plan`/event blocks, and the Phase 2 artifact regression suite closes the verifier gap without overrides.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted manifest -> replay/import logic | Future runtime commands trust the manifest to identify immutable revision evidence. |
| mutable filesystem content -> canonical revision history | Attackers can try to reuse unrelated files with matching hashes unless the manifest is path-bound. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | T | `src/quantum_runtime/runtime/run_manifest.py` | mitigate | Derive expected `intent`, `plan`, and event history paths from `WorkspacePaths` and reject any redirected block before hash validation. |
| T-02-03-02 | R | `tests/test_runtime_revision_artifacts.py` | mitigate | Add tamper regressions that prove the validator blocks redirected trusted evidence and preserves immutable earlier revisions after later runs. |
| T-02-03-03 | I | additive manifest trust blocks | mitigate | Keep optional-block compatibility, but validate present blocks against canonical revision-history paths so mutable aliases never become accepted trust sources. |
</threat_model>

<verification>
Run `bash -lc 'uv run --python 3.11 --extra dev --extra qiskit pytest -q tests/test_runtime_revision_artifacts.py && python -c "from pathlib import Path; assert len(Path(\"tests/test_runtime_revision_artifacts.py\").read_text().splitlines()) >= 140"'`.
</verification>

<success_criteria>
Phase 2 no longer relies on a frontmatter override for `tests/test_runtime_revision_artifacts.py`, and manifest validation blocks redirected `intent`, `plan`, or event evidence that does not match the selected revision's canonical history files.
</success_criteria>

<output>
After completion, create `.planning/phases/02-trusted-revision-artifacts/02-03-SUMMARY.md`
</output>
