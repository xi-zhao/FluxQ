---
phase: "03-concurrent-workspace-safety"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/quantum_runtime/workspace/__init__.py
  - src/quantum_runtime/workspace/locking.py
  - src/quantum_runtime/workspace/manager.py
  - tests/test_workspace_locking.py
autonomous: true
requirements:
  - RUNT-02
must_haves:
  truths:
    - "Only one writer can hold the workspace mutation lease at a time."
    - "Revision reservation and current-alias replacement happen only while the lease is held."
    - "Interrupted temp writes do not replace the last committed workspace JSON."
  artifacts:
    - path: "src/quantum_runtime/workspace/locking.py"
      provides: "Workspace-scoped lock lease and conflict metadata."
      exports:
        - "WorkspaceLock"
        - "WorkspaceLockConflict"
        - "acquire_workspace_lock"
    - path: "src/quantum_runtime/workspace/manager.py"
      provides: "Atomic alias-write helpers and lock-aware revision reservation."
      contains: "temp-file-plus-replace write path for workspace state"
    - path: "tests/test_workspace_locking.py"
      provides: "Concurrency and interrupted-write regression coverage."
      contains: "parallel writer and temp-file recovery assertions"
  key_links:
    - from: "src/quantum_runtime/workspace/manager.py"
      to: "src/quantum_runtime/workspace/locking.py"
      via: "lock acquisition before mutation"
      pattern: "acquire_workspace_lock|WorkspaceLock"
    - from: "src/quantum_runtime/workspace/manager.py"
      to: ".quantum/workspace.json"
      via: "same-directory temp write plus replace"
      pattern: "os\\.replace|Path\\.replace|NamedTemporaryFile"
---

<objective>
Create the workspace-level mutation contract that makes one shared workspace safe for concurrent writers without changing the existing alias/history layout.

Purpose: Fix the documented single-writer assumption at the lowest layer so later runtime plans can reuse one lock and one atomic-write path instead of hand-rolling concurrency handling in each command.
Output: A lock primitive, atomic write helpers for current workspace state, and regression tests that define safe behavior under conflict and interrupted writes.
</objective>

<execution_context>
@/Users/xizhao/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xizhao/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md

@src/quantum_runtime/workspace/manager.py
@src/quantum_runtime/workspace/__init__.py

Use the workspace facts already captured in planning:
- workspace mutation currently assumes one writer and has no lock
- `workspace.json` and current alias files are written non-atomically
- Phase 3 must preserve the current alias/history contract while adding machine-readable safety behavior
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock down the workspace mutation contract with failing concurrency tests</name>
  <files>tests/test_workspace_locking.py</files>
  <behavior>
    - Test 1: A second writer targeting the same workspace fails fast with deterministic conflict metadata instead of reserving another revision.
    - Test 2: A simulated interrupted temp write beside `workspace.json` or `specs/current.json` leaves the last committed JSON readable.
    - Test 3: Lock metadata captures enough holder details for later machine-readable reporting (`pid`, `hostname`, `command`, `started_at`, `lock_path`).
  </behavior>
  <action>Create `tests/test_workspace_locking.py` as the contract for Phase 3. Use a temporary initialized workspace and parallel acquisition attempts against the same `.quantum` root. Simulate interruption by creating recognized temp files next to alias targets and assert readers still parse the committed JSON payloads while the temp files remain non-authoritative. Keep coverage focused on the workspace layer only; do not route through policy, bundle, or broader runtime flows in this plan.</action>
  <verify>
    <automated>PYTHONPATH=src ./.venv/bin/python -m pytest tests/test_workspace_locking.py -q</automated>
  </verify>
  <done>The new test file fails before implementation and defines exclusivity, temp-file safety, and lock metadata expectations for the shared workspace.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the workspace lock lease and atomic alias-write helpers</name>
  <files>src/quantum_runtime/workspace/locking.py, src/quantum_runtime/workspace/manager.py, src/quantum_runtime/workspace/__init__.py</files>
  <action>Add a workspace-scoped lease file under `.quantum/` using exclusive create/open semantics so a second writer receives a conflict instead of mutating state concurrently. Persist holder metadata as JSON in the lease file for later CLI/reporting use. Add same-directory temp-write helpers that flush, fsync, and replace for mutable workspace targets such as `workspace.json` and the `current` alias files. Route revision reservation and mutable pointer updates in `WorkspaceManager` through these helpers, but do not rename existing alias paths or immutable history paths. Expose the new lock and helper surface from `workspace/__init__.py` so later plans can reuse it directly.</action>
  <verify>
    <automated>PYTHONPATH=src ./.venv/bin/python -m pytest tests/test_workspace_locking.py tests/test_workspace_manager.py -q</automated>
  </verify>
  <done>Workspace mutations can acquire one lease, reserve revisions only while that lease is held, and update mutable JSON files through atomic replace semantics without altering the established alias/history contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process -> `.quantum/.workspace.lock` | Untrusted concurrent processes compete to mutate shared workspace state. |
| temp file -> current alias JSON | Interrupted or partial writes can appear beside authoritative workspace files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | T | `workspace/manager.py` alias writes | mitigate | Replace direct writes with same-directory temp write plus fsync plus atomic replace so partial JSON never becomes authoritative. |
| T-03-01-02 | D | `workspace/locking.py` lease file | mitigate | Use exclusive lease acquisition with deterministic conflict metadata so concurrent writers fail fast instead of racing indefinitely. |
| T-03-01-03 | R | lock-holder identity | mitigate | Persist holder metadata in the lease file so later conflict payloads can attribute the active writer and support retry logic. |
</threat_model>

<verification>
Run the workspace-only regression suite after implementation:

`PYTHONPATH=src ./.venv/bin/python -m pytest tests/test_workspace_locking.py tests/test_workspace_manager.py -q`
</verification>

<success_criteria>
- Only one writer can hold the workspace mutation lease at a time.
- Mutable workspace JSON is promoted via atomic replace instead of in-place overwrite.
- Simulated interrupted writes leave the last committed workspace state readable.
</success_criteria>

<output>
After completion, create `.planning/phases/03-concurrent-workspace-safety/03-concurrent-workspace-safety-01-SUMMARY.md`
</output>
