---
phase: 08-milestone-verification-bookkeeping-closure
plan: "02"
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - CONTRIBUTING.md
  - scripts/dev-bootstrap.sh
  - .planning/phases/04-policy-acceptance-gates/04-VERIFICATION.md
autonomous: true
requirements:
  - RUNT-02
gap_closure: true
must_haves:
  truths:
    - "The canonical Phase 04 local gate is the exact targeted Ruff + module-form MyPy + policy-gate pytest sequence, and that gate is still green in the current workspace."
    - "`./scripts/dev-bootstrap.sh verify` is described truthfully as a broader repo smoke command, not as the same Phase 04 gate."
    - "Phase 04 no longer stays at `gaps_found` because of docs/script bookkeeping drift once the exact gate and wording are reconciled."
  artifacts:
    - path: "CONTRIBUTING.md"
      provides: "Canonical contributor-facing description of the exact Phase 04 local gate."
      contains: "exact Phase 4 repo-local sequence"
    - path: "scripts/dev-bootstrap.sh"
      provides: "Truthful usage/help/logging for the broader local smoke command."
      contains: "verify runs qrun version, Ruff, module-form MyPy, and full pytest"
    - path: ".planning/phases/04-policy-acceptance-gates/04-VERIFICATION.md"
      provides: "Refreshed passed verification artifact grounded in the exact Phase 04 gate."
      contains: "status: passed"
  key_links:
    - from: "CONTRIBUTING.md"
      to: "scripts/dev-bootstrap.sh"
      via: "matching wording about exact phase gate versus broader smoke command"
      pattern: "Phase 4 local workspace gate|full local smoke|pytest -q"
    - from: ".planning/phases/04-policy-acceptance-gates/04-VERIFICATION.md"
      to: "tests/test_cli_compare.py, tests/test_cli_runtime_gap.py, tests/test_cli_bench.py, tests/test_cli_doctor.py, tests/test_cli_observability.py, tests/test_runtime_policy.py"
      via: "verification evidence cites the exact targeted gate"
      pattern: "tests/test_cli_compare.py|tests/test_cli_bench.py|tests/test_cli_doctor.py|tests/test_runtime_policy.py"
---

<objective>
Reconcile the canonical Phase 04 local gate, docs/script semantics, and the Phase 04 verification artifact without dragging unrelated full-suite failures into scope.

Purpose: `INT-02` remains open because Phase 04 work exists and the exact gate is green, but contributor docs and the one-shot script still describe different truths. This plan must preserve the distinction between the exact phase gate and broader repo smoke.
Output: Truthful docs/tooling wording for Phase 04 verification, and a refreshed `04-VERIFICATION.md` with `status: passed`.
</objective>

<execution_context>
@/Users/xizhao/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xizhao/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/08-milestone-verification-bookkeeping-closure/08-RESEARCH.md
@.planning/phases/08-milestone-verification-bookkeeping-closure/08-VALIDATION.md
@.planning/phases/04-policy-acceptance-gates/04-VALIDATION.md
@.planning/phases/04-policy-acceptance-gates/04-VERIFICATION.md
@.planning/phases/04-policy-acceptance-gates/04-SECURITY.md
@CONTRIBUTING.md
@scripts/dev-bootstrap.sh
@tests/test_cli_compare.py
@tests/test_cli_runtime_gap.py
@tests/test_cli_bench.py
@tests/test_cli_doctor.py
@tests/test_cli_observability.py
@tests/test_runtime_policy.py

Use the current truth already rechecked in planning:
- the exact Phase 04 gate passes, including `tests/test_runtime_policy.py` as the shared policy unit/regression suite
- `./scripts/dev-bootstrap.sh verify` still runs full `pytest -q`
- broader repo smoke failures outside the exact Phase 04 gate are not Phase 04 blockers once the distinction is documented truthfully
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the Phase 04 gate contract truthful in docs and tooling</name>
  <files>CONTRIBUTING.md, scripts/dev-bootstrap.sh</files>
  <read_first>
    - CONTRIBUTING.md
    - scripts/dev-bootstrap.sh
    - .planning/phases/04-policy-acceptance-gates/04-VALIDATION.md
    - .planning/phases/04-policy-acceptance-gates/04-VERIFICATION.md
    - tests/test_runtime_policy.py
    - .planning/phases/08-milestone-verification-bookkeeping-closure/08-RESEARCH.md
  </read_first>
  <action>Preserve the distinction that Phase 08 research and the user already called out: the exact Phase 04 gate is the targeted `ruff + python -m mypy + selected pytest files` sequence, and that selected pytest set explicitly includes `tests/test_cli_compare.py`, `tests/test_cli_runtime_gap.py`, `tests/test_cli_bench.py`, `tests/test_cli_doctor.py`, `tests/test_cli_observability.py`, and `tests/test_runtime_policy.py`, while `./scripts/dev-bootstrap.sh verify` is a broader local smoke command. Update `CONTRIBUTING.md` so it no longer says the one-shot script runs “the same repo-local gate”. Update `scripts/dev-bootstrap.sh` usage/help/log text so `verify` is explicitly described as `qrun version`, Ruff, module-form MyPy, and full `pytest -q`. Use a docs/tooling smoke command for this task-level verify so feedback stays fast; reserve the full exact Phase 04 gate for Task 2 and the plan-level verification block. Do not shrink the script to the Phase 04 subset, and do not widen the exact Phase 04 gate to include unrelated inspect/report-writer failures.</action>
  <acceptance_criteria>
    - `rg -n "Phase 4 local workspace gate|full local smoke|full pytest|same repo-local gate" CONTRIBUTING.md scripts/dev-bootstrap.sh`
    - `rg -n "tests/test_cli_compare.py tests/test_cli_runtime_gap.py tests/test_cli_bench.py tests/test_cli_doctor.py tests/test_cli_observability.py tests/test_runtime_policy.py" CONTRIBUTING.md`
    - `./scripts/dev-bootstrap.sh --help >/tmp/fluxq-dev-bootstrap-help.txt && rg -n "verify|smoke|pytest" /tmp/fluxq-dev-bootstrap-help.txt`
  </acceptance_criteria>
  <verify>
    <automated>./scripts/dev-bootstrap.sh --help >/tmp/fluxq-dev-bootstrap-help.txt && rg -n "verify|smoke|pytest" /tmp/fluxq-dev-bootstrap-help.txt CONTRIBUTING.md scripts/dev-bootstrap.sh && rg -n "tests/test_cli_compare.py tests/test_cli_runtime_gap.py tests/test_cli_bench.py tests/test_cli_doctor.py tests/test_cli_observability.py tests/test_runtime_policy.py" CONTRIBUTING.md</automated>
  </verify>
  <done>The contributor docs and the helper script describe the same truth: the exact Phase 04 gate is targeted, `dev-bootstrap verify` is a broader smoke command, and the task-level verify remains a small docs/tooling smoke instead of rerunning the full gate.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh `04-VERIFICATION.md` from `gaps_found` to `passed`</name>
  <files>.planning/phases/04-policy-acceptance-gates/04-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/04-policy-acceptance-gates/04-VERIFICATION.md
    - .planning/phases/04-policy-acceptance-gates/04-VALIDATION.md
    - .planning/phases/04-policy-acceptance-gates/04-SECURITY.md
    - CONTRIBUTING.md
    - scripts/dev-bootstrap.sh
    - tests/test_runtime_policy.py
    - .planning/phases/07-compare-trust-closure/07-VERIFICATION.md
  </read_first>
  <action>After Task 1 wording is truthful and the exact Phase 04 gate is green, rewrite `.planning/phases/04-policy-acceptance-gates/04-VERIFICATION.md` so it no longer carries the old `gaps_found` block about docs/script drift. Change frontmatter to `status: passed`, update Observable Truth 4 so it explicitly names the exact Phase 04 gate as canonical, and record `./scripts/dev-bootstrap.sh verify` as a broader repo smoke command rather than as equivalent proof for POLC-01/02/03. Preserve the existing verified evidence for compare, benchmark, and doctor, and keep `tests/test_runtime_policy.py` in the canonical gate description because Phase 04 validation and verification already treat it as owned proof. Do not fabricate repo-wide green claims or remove the note that the one-shot script can fail outside the phase-owned scope.</action>
  <acceptance_criteria>
    - `rg -n "^status: passed$" .planning/phases/04-policy-acceptance-gates/04-VERIFICATION.md`
    - `rg -n "exact Phase 4|broader repo smoke|POLC-01|POLC-02|POLC-03|tests/test_runtime_policy.py" .planning/phases/04-policy-acceptance-gates/04-VERIFICATION.md`
    - `./.venv/bin/ruff check src tests && ./.venv/bin/python -m mypy src && ./.venv/bin/python -m pytest tests/test_cli_compare.py tests/test_cli_runtime_gap.py tests/test_cli_bench.py tests/test_cli_doctor.py tests/test_cli_observability.py tests/test_runtime_policy.py -q --maxfail=1`
  </acceptance_criteria>
  <verify>
    <automated>./.venv/bin/ruff check src tests && ./.venv/bin/python -m mypy src && ./.venv/bin/python -m pytest tests/test_cli_compare.py tests/test_cli_runtime_gap.py tests/test_cli_bench.py tests/test_cli_doctor.py tests/test_cli_observability.py tests/test_runtime_policy.py -q --maxfail=1</automated>
  </verify>
  <done>`04-VERIFICATION.md` is a passed proof artifact grounded in the exact Phase 04 gate, with no remaining blocker caused solely by docs/script wording drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contributor docs -> local verification behavior | Contributors and agents rely on docs and helper scripts to know what proof actually closes Phase 04. |
| exact phase gate -> milestone bookkeeping | A stale or ambiguous Phase 04 verification contract would keep milestone closure evidence untrustworthy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | R | `CONTRIBUTING.md` and `scripts/dev-bootstrap.sh` | mitigate | Make docs and script wording explicit about exact Phase 04 proof versus broader repo smoke so contributors cannot claim the wrong gate passed. |
| T-08-02-02 | T | `.planning/phases/04-policy-acceptance-gates/04-VERIFICATION.md` | mitigate | Refresh the verification artifact only after the exact targeted gate is green and the docs/script mismatch is resolved. |
| T-08-02-03 | D | phase-owned verification scope | accept | `dev-bootstrap verify` can still fail on unrelated repo smoke; this is acceptable once it is clearly documented as outside the exact Phase 04 closure gate. |
</threat_model>

<verification>
Run the exact Phase 04 gate:

`./.venv/bin/ruff check src tests && ./.venv/bin/python -m mypy src && ./.venv/bin/python -m pytest tests/test_cli_compare.py tests/test_cli_runtime_gap.py tests/test_cli_bench.py tests/test_cli_doctor.py tests/test_cli_observability.py tests/test_runtime_policy.py -q --maxfail=1`
</verification>

<success_criteria>
- Docs and tooling no longer claim that `./scripts/dev-bootstrap.sh verify` is the same thing as the Phase 04 gate.
- The exact targeted Phase 04 gate, including `tests/test_runtime_policy.py`, remains green.
- `.planning/phases/04-policy-acceptance-gates/04-VERIFICATION.md` is updated to `status: passed` and grounded in the exact gate.
</success_criteria>

<output>
After completion, create `.planning/phases/08-milestone-verification-bookkeeping-closure/08-02-SUMMARY.md`
</output>
