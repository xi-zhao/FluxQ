---
phase: 08-milestone-verification-bookkeeping-closure
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/quantum_runtime/reporters/writer.py
  - src/quantum_runtime/runtime/executor.py
  - tests/test_runtime_workspace_safety.py
  - tests/test_report_writer.py
  - .planning/phases/03-concurrent-workspace-safety/03-VERIFICATION.md
autonomous: true
requirements:
  - RUNT-02
gap_closure: true
must_haves:
  truths:
    - "The current Phase 03 truth probe passes, including the interrupted-commit path that keeps the previous `reports/latest.json` authoritative."
    - "Exec no longer promotes `reports/latest.json` before the same revision's manifest is durably written."
    - "Report payloads and summaries point at canonical history artifacts for the evaluated revision instead of mutable aliases."
    - "The shared writer/executor history contract still satisfies the focused Phase 07 import/exec regression subset after the seam fix."
    - "Phase 03 has a truthful `03-VERIFICATION.md` with `status: passed` backed by the current rerun, not by historical summaries alone."
  artifacts:
    - path: "src/quantum_runtime/reporters/writer.py"
      provides: "History-first report persistence and canonical report/qspec provenance for one revision."
      contains: "history report write plus optional latest promotion switch"
    - path: "src/quantum_runtime/runtime/executor.py"
      provides: "Exec-side sequencing that promotes latest report only after manifest persistence succeeds."
      contains: "history-first write_report call and alias promotion after write_run_manifest"
    - path: "tests/test_runtime_workspace_safety.py"
      provides: "Live interrupted-commit regression proof for authoritative latest report behavior."
      contains: "test_interrupted_commit_keeps_previous_current_revision_authoritative"
    - path: "tests/test_report_writer.py"
      provides: "Writer-side canonical history path and suggestion regressions tied to the same seam."
      contains: "write_report canonicalizes history qspec/report paths"
    - path: ".planning/phases/03-concurrent-workspace-safety/03-VERIFICATION.md"
      provides: "Phase 03 proof artifact closing `RUNT-02` with current evidence."
      contains: "status: passed"
  key_links:
    - from: "src/quantum_runtime/runtime/executor.py"
      to: "src/quantum_runtime/reporters/writer.py"
      via: "history-only report write before alias promotion"
      pattern: "write_report\\(.*promote_latest=False|write_report\\("
    - from: "src/quantum_runtime/runtime/executor.py"
      to: "src/quantum_runtime/runtime/run_manifest.py"
      via: "manifest write completes before latest alias promotion"
      pattern: "write_run_manifest\\(.*promote_latest=False|_promote_exec_aliases"
    - from: "tests/test_runtime_workspace_safety.py"
      to: "src/quantum_runtime/reporters/writer.py"
      via: "interrupted manifest failure must leave latest report unchanged"
      pattern: "report changed after interrupted commit|reports/latest\\.json"
    - from: "src/quantum_runtime/reporters/writer.py, src/quantum_runtime/runtime/executor.py"
      to: "tests/test_runtime_imports.py, tests/test_runtime_revision_artifacts.py, tests/test_cli_exec.py"
      via: "focused Phase 07 regression subset guards the shared writer/executor history contract"
      pattern: "test_runtime_imports|test_runtime_revision_artifacts|test_cli_exec"
    - from: ".planning/phases/03-concurrent-workspace-safety/03-VERIFICATION.md"
      to: "tests/test_cli_workspace_safety.py, tests/test_runtime_workspace_safety.py, tests/test_report_writer.py"
      via: "verification evidence cites the current rerun commands"
      pattern: "test_interrupted_commit_keeps_previous_current_revision_authoritative|tests/test_report_writer.py"
---

<objective>
Re-establish truthful Phase 03 verification by fixing only the live report/commit seam that still breaks the current workspace-safety truth probe.

Purpose: `RUNT-02` is still open because Phase 03 has no verification file and the current interrupted-commit probe is red. This plan must recover proof from current behavior first, then mint `03-VERIFICATION.md`.
Output: A minimal writer/exec fix for the interrupted-commit report seam, targeted regression coverage, and a truthful `03-VERIFICATION.md`.
</objective>

<execution_context>
@/Users/xizhao/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xizhao/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/08-milestone-verification-bookkeeping-closure/08-RESEARCH.md
@.planning/phases/08-milestone-verification-bookkeeping-closure/08-VALIDATION.md
@.planning/phases/03-concurrent-workspace-safety/03-concurrent-workspace-safety-03-SUMMARY.md
@.planning/phases/03-concurrent-workspace-safety/03-concurrent-workspace-safety-04-SUMMARY.md
@.planning/phases/07-compare-trust-closure/07-VERIFICATION.md
@src/quantum_runtime/reporters/writer.py
@src/quantum_runtime/runtime/executor.py
@src/quantum_runtime/runtime/run_manifest.py
@tests/test_runtime_workspace_safety.py
@tests/test_report_writer.py
@tests/test_runtime_imports.py
@tests/test_runtime_revision_artifacts.py
@tests/test_cli_exec.py

<interfaces>
From `src/quantum_runtime/reporters/writer.py`:

```python
def write_report(
    *,
    workspace: WorkspaceHandle,
    revision: str,
    input_data: dict[str, Any],
    qspec: QSpec,
    qspec_path: Path,
    artifacts: dict[str, Any],
    diagnostics: dict[str, Any],
    backend_reports: dict[str, Any],
    warnings: list[str],
    errors: list[str],
) -> dict[str, Any]:
```

From `src/quantum_runtime/runtime/run_manifest.py`:

```python
def write_run_manifest(
    *,
    workspace_root: Path,
    revision: str,
    report_payload: dict[str, Any],
    qspec: QSpec,
    qspec_path: Path,
    report_path: Path,
    intent_path: Path | None = None,
    plan_path: Path | None = None,
    event_history_path: Path | None = None,
    trace_history_path: Path | None = None,
    promote_latest: bool = True,
) -> dict[str, Any]:
```

Current exec ordering to preserve, but with report latest-promotion delayed:

```python
report = write_report(...)
write_run_manifest(..., promote_latest=False)
_promote_exec_aliases(...)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the live Phase 03 regression to the report/commit seam</name>
  <files>tests/test_runtime_workspace_safety.py, tests/test_report_writer.py</files>
  <read_first>
    - tests/test_runtime_workspace_safety.py
    - tests/test_report_writer.py
    - src/quantum_runtime/reporters/writer.py
    - src/quantum_runtime/runtime/executor.py
    - src/quantum_runtime/runtime/run_manifest.py
    - .planning/phases/08-milestone-verification-bookkeeping-closure/08-RESEARCH.md
    - .planning/phases/08-milestone-verification-bookkeeping-closure/08-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: `test_interrupted_commit_keeps_previous_current_revision_authoritative` proves `reports/latest.json` stays byte-identical when manifest persistence fails mid-commit.
    - Test 2: `write_report()` canonicalizes `qspec`, `report`, and artifact provenance to `specs/history/<revision>.json`, `reports/history/<revision>.json`, and `artifacts/history/<revision>/...` when the evaluated revision already has immutable history artifacts.
    - Test 3: `write_report()` can still produce backend-specific suggestions from the passed `QSpec` object without requiring `qspec_path` to be a valid mutable-alias JSON file.
  </behavior>
  <action>Run the current failing seam first with `./.venv/bin/python -m pytest tests/test_runtime_workspace_safety.py tests/test_report_writer.py -q --maxfail=1`. Keep scope to the report/manifest seam only. If the existing tests already pin the three behaviors above, preserve them and only tighten assertions where the current failure is ambiguous. If coverage is missing, add the smallest new assertions necessary in `tests/test_runtime_workspace_safety.py` and `tests/test_report_writer.py` to bind latest-report immutability, canonical history paths, and `QSpec`-object-driven suggestion generation. Do not add inspect, Classiq, or unrelated repo-smoke assertions.</action>
  <acceptance_criteria>
    - `rg -n "test_interrupted_commit_keeps_previous_current_revision_authoritative|test_write_report_persists_latest_report|test_write_report_canonicalizes_current_alias_artifacts|test_write_report_adds_backend_specific_suggestions" tests/test_runtime_workspace_safety.py tests/test_report_writer.py`
    - `./.venv/bin/python -m pytest tests/test_runtime_workspace_safety.py tests/test_report_writer.py -q --maxfail=1`
  </acceptance_criteria>
  <verify>
    <automated>./.venv/bin/python -m pytest tests/test_runtime_workspace_safety.py tests/test_report_writer.py -q --maxfail=1</automated>
  </verify>
  <done>The targeted regression suite expresses the exact Phase 03 seam: latest report immutability on interrupted commit, canonical history paths in report payloads, and writer behavior driven by the passed `QSpec` object rather than mutable alias content.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the history-first report/write ordering and mint `03-VERIFICATION.md`</name>
  <files>src/quantum_runtime/reporters/writer.py, src/quantum_runtime/runtime/executor.py, .planning/phases/03-concurrent-workspace-safety/03-VERIFICATION.md</files>
  <read_first>
    - src/quantum_runtime/reporters/writer.py
    - src/quantum_runtime/runtime/executor.py
    - src/quantum_runtime/runtime/run_manifest.py
    - tests/test_cli_workspace_safety.py
    - tests/test_runtime_workspace_safety.py
    - tests/test_report_writer.py
    - tests/test_runtime_imports.py
    - tests/test_runtime_revision_artifacts.py
    - tests/test_cli_exec.py
    - .planning/phases/03-concurrent-workspace-safety/03-concurrent-workspace-safety-03-SUMMARY.md
    - .planning/phases/03-concurrent-workspace-safety/03-concurrent-workspace-safety-04-SUMMARY.md
    - .planning/phases/07-compare-trust-closure/07-VERIFICATION.md
  </read_first>
  <action>Implement the smallest production fix that makes the Task 1 seam green. In `src/quantum_runtime/reporters/writer.py`, make the revision history report the authoritative write target and add an explicit switch so latest promotion is opt-in instead of implicit. Canonicalize `qspec` and artifact provenance to the revision history paths when those artifacts already exist, and derive semantics and suggestion logic from the passed `qspec` object so the writer does not require a valid mutable alias file to exist at `qspec_path`. In `src/quantum_runtime/runtime/executor.py`, keep the history-first flow: write the report as history-only, keep `write_run_manifest(..., promote_latest=False)`, and let `_promote_exec_aliases()` move both latest report and latest manifest only after manifest persistence succeeds. After the seam is green, rerun the focused Phase 07 regression subset `tests/test_runtime_imports.py`, `tests/test_runtime_revision_artifacts.py`, and `tests/test_cli_exec.py` so the shared writer/executor history contract stays guarded across import and exec reopen flows. After the targeted Phase 03 truth set is green, create `.planning/phases/03-concurrent-workspace-safety/03-VERIFICATION.md` with `status: passed`, current rerun evidence, and explicit closure of `RUNT-02`; do not copy old summary prose or leave `verification_status: missing` language in place.</action>
  <acceptance_criteria>
    - `rg -n "promote_latest=False|reports/latest\\.json|reports/history" src/quantum_runtime/runtime/executor.py src/quantum_runtime/reporters/writer.py`
    - `test -f .planning/phases/03-concurrent-workspace-safety/03-VERIFICATION.md`
    - `rg -n "^status: passed$|RUNT-02|test_interrupted_commit_keeps_previous_current_revision_authoritative" .planning/phases/03-concurrent-workspace-safety/03-VERIFICATION.md tests/test_runtime_workspace_safety.py`
    - `./.venv/bin/python -m pytest tests/test_cli_workspace_safety.py tests/test_runtime_workspace_safety.py tests/test_report_writer.py tests/test_runtime_imports.py tests/test_runtime_revision_artifacts.py tests/test_cli_exec.py -q --maxfail=1`
  </acceptance_criteria>
  <verify>
    <automated>./.venv/bin/python -m pytest tests/test_cli_workspace_safety.py tests/test_runtime_workspace_safety.py tests/test_report_writer.py tests/test_runtime_imports.py tests/test_runtime_revision_artifacts.py tests/test_cli_exec.py -q --maxfail=1</automated>
  </verify>
  <done>The current Phase 03 truth probe is green, the focused Phase 07 import/exec regression subset stays green, latest report promotion is no longer ahead of manifest durability, report payloads resolve to canonical history artifacts, and `03-VERIFICATION.md` truthfully closes `RUNT-02` with current evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| exec commit path -> mutable latest report alias | A failed manifest commit can otherwise leave `reports/latest.json` ahead of the last authoritative revision. |
| report writer -> replay/import consumers | Report qspec and artifact provenance are later trusted by compare/import/replay flows and must point at canonical history artifacts. |
| current test reality -> verification document | Phase verification must describe the current rerun truth, not historical plan summaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | T | `src/quantum_runtime/reporters/writer.py` latest promotion | mitigate | Make history report writes authoritative and require explicit latest promotion so interrupted manifest failures cannot overwrite `reports/latest.json` early. |
| T-08-01-02 | T | `src/quantum_runtime/runtime/executor.py` commit ordering | mitigate | Keep report and manifest writes history-first and promote latest aliases only after `write_run_manifest(..., promote_latest=False)` succeeds. |
| T-08-01-03 | R | `.planning/phases/03-concurrent-workspace-safety/03-VERIFICATION.md` | mitigate | Regenerate Phase 03 verification from the current targeted rerun and cite the exact tests/commands used to close `RUNT-02`. |
</threat_model>

<verification>
Run the targeted Phase 03 closeout probe:

`./.venv/bin/python -m pytest tests/test_cli_workspace_safety.py tests/test_runtime_workspace_safety.py tests/test_report_writer.py tests/test_runtime_imports.py tests/test_runtime_revision_artifacts.py tests/test_cli_exec.py -q --maxfail=1`
</verification>

<success_criteria>
- The interrupted-commit regression no longer changes `reports/latest.json` when manifest persistence fails.
- The focused Phase 07 import/exec regression subset (`tests/test_runtime_imports.py`, `tests/test_runtime_revision_artifacts.py`, and `tests/test_cli_exec.py`) stays green across the shared writer/executor seam.
- `write_report()` emits canonical history paths for the evaluated revision and still supports backend suggestion generation from the passed `QSpec`.
- `.planning/phases/03-concurrent-workspace-safety/03-VERIFICATION.md` exists with `status: passed` and cites current rerun evidence that closes `RUNT-02`.
</success_criteria>

<output>
After completion, create `.planning/phases/08-milestone-verification-bookkeeping-closure/08-01-SUMMARY.md`
</output>
