---
phase: 999.1
slug: wechat-personal-ilink-cc-connect-claw-code-qcli-integration
status: draft
nyquist_compliant: true
wave_0_complete: false
created: 2026-04-21
---

# Phase 999.1 - Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Frameworks** | `pytest 9.0.2`, `ruff 0.15.8`, `mypy 1.20.0` |
| **Config files** | `pyproject.toml`, `mypy.ini` |
| **Quick run command** | `uv run pytest tests/test_cc_connect_gateway.py tests/test_claw_launcher.py -q --maxfail=1` |
| **Full suite command** | `uv run ruff check src tests integrations && uv run python -m mypy src && uv run pytest tests/test_cc_connect_gateway.py tests/test_claw_launcher.py tests/test_wechat_confirmation_flow.py -q --maxfail=1` |
| **Estimated runtime** | ~20 seconds for smoke, ~40 seconds for the full Phase 999.1 gate |

---

## Sampling Rate

- **After every task commit:** Run the owning task's smallest smoke verify command.
- **After Wave 1:** Run `uv run pytest tests/test_cc_connect_gateway.py tests/test_claw_launcher.py -q --maxfail=1`
- **After Wave 2:** Run `uv run pytest tests/test_wechat_confirmation_flow.py -q --maxfail=1`
- **After every plan wave:** Run `uv run ruff check src tests integrations && uv run python -m mypy src && uv run pytest tests/test_cc_connect_gateway.py tests/test_claw_launcher.py tests/test_wechat_confirmation_flow.py -q --maxfail=1`
- **Before `/gsd-verify-work`:** Full suite must be green and one operator smoke recipe for personal WeChat / ilink must be documented
- **Max feedback latency:** 40 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 999.1-ADAPT-01 | 999.1-01 | 1 | WX-01 | T-999.1-01 / T-999.1-02 | `cc-connect` request mapping, allowlist enforcement, and per-user workspace resolution are deterministic and do not fall through to shared state. | unit + integration | `uv run pytest tests/test_cc_connect_gateway.py -q --maxfail=1` | ❌ Wave 0 | ⬜ pending |
| 999.1-LAUNCH-02 | 999.1-02 | 1 | WX-02 | T-999.1-03 / T-999.1-04 | The launcher owns `claw` invocation details and FluxQ wrapper routing across the current shipped `qrun` surface; gateway never hardcodes `claw` CLI details. | unit + integration | `uv run pytest tests/test_claw_launcher.py -q --maxfail=1` | ❌ Wave 0 | ⬜ pending |
| 999.1-CONFIRM-03 | 999.1-03 | 2 | WX-03 | T-999.1-05 / T-999.1-06 | High-risk conversational flows require an explicit second-step confirmation and preserve the standard summary contract before `remote submit`. | integration | `uv run pytest tests/test_wechat_confirmation_flow.py -q --maxfail=1` | ❌ Wave 0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠ flaky*

---

## Wave 0 Requirements

- [ ] `tests/test_cc_connect_gateway.py` must exist for adapter / allowlist / workspace-resolution behavior.
- [ ] `tests/test_claw_launcher.py` must exist for launcher command construction and wrapper routing.
- [ ] `tests/test_wechat_confirmation_flow.py` must exist for conversational risk confirmation state transitions.
- [ ] `integrations/cc-connect/` must exist as the home for example configs, launcher script, and operator docs.
- [ ] A live personal Weixin / ilink operator smoke recipe must be documented in the final verification artifacts.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Personal WeChat message reaches `cc-connect`, crosses the HTTP gateway, invokes `claw`, and returns a FluxQ response | TBD | Requires real Weixin / ilink login, live `cc-connect` fork, and operator-side environment | Run `cc-connect weixin setup --project <project>` on the ingress host, send a personal WeChat message, and verify the gateway / launcher / `claw` / `qrun` chain replies successfully |
| High-risk `remote submit` confirmation flow in live chat | TBD | Requires real message loop plus a live operator acknowledgment cycle | Send a conversational `remote submit` request from personal WeChat, confirm that the bot asks for explicit second-step confirmation, then reply `确认` / equivalent and verify the final execution path |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all missing integration references
- [ ] No watch-mode flags
- [ ] Feedback latency < 45s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
