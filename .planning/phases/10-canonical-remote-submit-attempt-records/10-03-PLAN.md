---
phase: 10-canonical-remote-submit-attempt-records
plan: "03"
type: execute
wave: 3
depends_on:
  - "10-02"
files_modified:
  - src/quantum_runtime/cli.py
  - src/quantum_runtime/runtime/backend_registry.py
  - tests/test_cli_remote_submit.py
  - tests/test_cli_backend_list.py
autonomous: true
requirements:
  - REMT-01
  - REMT-02
must_haves:
  truths:
    - "Users can call one provider-neutral command, `qrun remote submit`, with the same ingress selectors used by `qrun exec`."
    - "The CLI requires `--backend` and returns the persisted attempt metadata immediately on success."
    - "`qrun backend list --json` truthfully reports IBM remote submit capability without adding auto-selection fields."
  artifacts:
    - path: "src/quantum_runtime/cli.py"
      provides: "Remote Typer namespace and provider-neutral submit command"
      contains: "@remote_app.command(\"submit\")"
    - path: "src/quantum_runtime/runtime/backend_registry.py"
      provides: "Updated IBM capability descriptor with remote submit enabled"
      contains: "\"remote_submit\": True"
    - path: "tests/test_cli_remote_submit.py"
      provides: "CLI parity and JSON-contract coverage for remote submit"
      contains: "test_qrun_remote_submit_json_accepts_all_supported_input_selectors"
    - path: "tests/test_cli_backend_list.py"
      provides: "Capability-projection regression for IBM submit support without auto-selection"
      contains: "test_backend_list_json_reports_ibm_runtime_submit_capability"
  key_links:
    - from: "src/quantum_runtime/cli.py"
      to: "src/quantum_runtime/runtime/resolve.py"
      via: "resolve_runtime_input"
      pattern: "resolve_runtime_input"
    - from: "src/quantum_runtime/cli.py"
      to: "src/quantum_runtime/runtime/remote_submit.py"
      via: "submit_remote_run"
      pattern: "submit_remote_run"
    - from: "src/quantum_runtime/runtime/backend_registry.py"
      to: "tests/test_cli_backend_list.py"
      via: "remote_submit capability assertion"
      pattern: "remote_submit"
---

<objective>
Expose Phase 10 through one provider-neutral CLI verb and update the repo’s capability surface to match the new runtime truth.

Purpose: Finish the repo-first user surface for canonical remote submit without adding lifecycle, reopen, cancel, pack, compare, or finalization work.
Output: `qrun remote submit`, matching CLI regressions, and truthful backend capability projection.
</objective>

<execution_context>
@/Users/xizhao/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xizhao/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-canonical-remote-submit-attempt-records/10-RESEARCH.md
@.planning/phases/10-canonical-remote-submit-attempt-records/10-VALIDATION.md
@.planning/phases/09-ibm-access-backend-readiness/09-03-SUMMARY.md
@src/quantum_runtime/cli.py
@src/quantum_runtime/runtime/backend_registry.py
@src/quantum_runtime/runtime/remote_submit.py
@tests/test_cli_exec.py
@tests/test_cli_backend_list.py

<interfaces>
From src/quantum_runtime/cli.py:
```python
backend_app = typer.Typer(add_completion=False, help="Backend discovery helpers.")
ibm_app = typer.Typer(add_completion=False, help="IBM Quantum Platform helpers.")
app.add_typer(backend_app, name="backend")
app.add_typer(ibm_app, name="ibm")

def _json_error(reason: str) -> None: ...
def _cli_error(reason: str, *, json_output: bool) -> NoReturn: ...
def _validate_output_modes(*, json_output: bool, jsonl_output: bool) -> None: ...
```

From src/quantum_runtime/runtime/backend_registry.py:
```python
class BackendCapabilityDescriptor(BaseModel):
    backend: str
    provider: str
    available: bool
    optional: bool = False
    reason: str | None = None
    module_dependencies: list[BackendDependency] = Field(default_factory=list)
    capabilities: dict[str, bool] = Field(default_factory=dict)
    notes: list[str] = Field(default_factory=list)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the provider-neutral `qrun remote submit` command with canonical ingress parity</name>
  <files>src/quantum_runtime/cli.py, tests/test_cli_remote_submit.py</files>
  <read_first>
src/quantum_runtime/cli.py
src/quantum_runtime/runtime/resolve.py
src/quantum_runtime/runtime/remote_submit.py
tests/test_cli_exec.py
tests/test_cli_ibm_config.py
.planning/phases/10-canonical-remote-submit-attempt-records/10-RESEARCH.md
  </read_first>
  <action>Add `remote_app = typer.Typer(add_completion=False, help="Remote execution helpers.")` beside the existing Typer namespaces and register it with `app.add_typer(remote_app, name="remote")`. Implement `@remote_app.command("submit")` with these exact options: `--workspace`, required `--backend`, `--intent-file`, `--intent-json-file`, `--qspec-file`, `--report-file`, `--revision`, and `--intent-text`, plus `--json`. Mirror the `qrun exec` one-input invariant exactly and route all input resolution through a single `resolve_runtime_input(...)` call. After resolution, call `submit_remote_run(workspace_root=workspace, resolved=resolved, backend_name=backend)`. Use `_json_error("expected_exactly_one_input")` for selector conflicts in JSON mode, translate `ImportSourceError` and `RemoteSubmitError` through `_json_error` in JSON mode and `typer.BadParameter` otherwise, and preserve `_handle_workspace_safety_error()` for workspace lease or recovery failures. Keep the text success output to one line: `Submitted remote attempt <attempt_id> to <backend_name> as provider job <provider_job_id>.` Do not add `--jsonl`, `remote status`, `remote reopen`, or `remote cancel` in this phase.</action>
  <verify>
    <automated>uv run pytest tests/test_cli_remote_submit.py -k 'accepts_all_supported_input_selectors or requires_explicit_backend or json_returns_attempt_metadata' -q --maxfail=1</automated>
  </verify>
  <acceptance_criteria>
- `rg -n "remote_app = typer.Typer|app.add_typer\\(remote_app, name=\"remote\"\\)|@remote_app.command\\(\"submit\"\\)" src/quantum_runtime/cli.py` returns the new namespace and command.
- `rg -n "--backend|expected_exactly_one_input|submit_remote_run" src/quantum_runtime/cli.py` shows the explicit backend requirement and canonical runtime handoff.
- `rg -n "test_qrun_remote_submit_json_accepts_all_supported_input_selectors|test_qrun_remote_submit_json_requires_explicit_backend|test_qrun_remote_submit_json_returns_attempt_metadata" tests/test_cli_remote_submit.py` returns the required CLI regressions.
- `uv run pytest tests/test_cli_remote_submit.py -k 'accepts_all_supported_input_selectors or requires_explicit_backend or json_returns_attempt_metadata' -q --maxfail=1` passes.
  </acceptance_criteria>
  <done>The repo exposes one provider-neutral submit verb that reuses the existing ingress contract and returns immediate attempt metadata without introducing lifecycle or finalization work.</done>
</task>

<task type="auto">
  <name>Task 2: Update backend capability surfacing and close the CLI truth gap</name>
  <files>src/quantum_runtime/runtime/backend_registry.py, tests/test_cli_backend_list.py, tests/test_cli_remote_submit.py</files>
  <read_first>
src/quantum_runtime/runtime/backend_registry.py
src/quantum_runtime/runtime/backend_list.py
tests/test_cli_backend_list.py
tests/test_cli_remote_submit.py
.planning/phases/09-ibm-access-backend-readiness/09-03-SUMMARY.md
  </read_first>
  <action>Update `src/quantum_runtime/runtime/backend_registry.py` so the `ibm-runtime` descriptor now advertises `capabilities["remote_submit"] = True` while keeping `remote_readiness = True`. Replace the readiness-only note text with language that is exact for Phase 10: submit is available, but lifecycle reopen/poll/cancel and terminal finalization are still pending later phases. Extend `tests/test_cli_backend_list.py` to assert the submit capability is now true and that `backend list --json` still omits `recommended_backend`, `selected_backend`, and `least_busy`. Extend `tests/test_cli_remote_submit.py` so the JSON payload assertions pin these exact keys on success: `attempt_id`, `provider`, `provider_job_id`, `backend_name`, `instance`, `primitive_kind`, `source_kind`, `source`, `workload_id`, `workload_hash`, `semantic_hash`, and `attempt_path`. Do not add any lifecycle capability bits, auto-selection fields, or provider-specific CLI submit verb in this task.</action>
  <verify>
    <automated>uv run pytest tests/test_cli_remote_submit.py tests/test_cli_backend_list.py -q --maxfail=1</automated>
  </verify>
  <acceptance_criteria>
- `rg -n "\"remote_submit\": True|submit is available|lifecycle" src/quantum_runtime/runtime/backend_registry.py` shows the updated IBM capability truth.
- `rg -n "test_backend_list_json_reports_ibm_runtime_submit_capability|test_backend_list_json_omits_auto_selection_fields" tests/test_cli_backend_list.py` returns both the new submit-capability regression and the preserved no-auto-select regression.
- `rg -n "attempt_id|provider_job_id|primitive_kind|semantic_hash|attempt_path" tests/test_cli_remote_submit.py` shows the pinned JSON contract.
- `uv run pytest tests/test_cli_remote_submit.py tests/test_cli_backend_list.py -q --maxfail=1` passes.
  </acceptance_criteria>
  <done>The CLI and backend inventory tell the same truth: submit is available through `qrun remote submit`, backend selection stays explicit, and lifecycle/finalization remain intentionally out of scope.</done>
</task>

</tasks>

<threat_model>
High-severity block: do not close this plan until the explicit-backend and no-auto-selection regressions for T-10-03-01 and T-10-03-02 are green.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user CLI flags -> canonical resolver | untrusted caller input selects one of the supported ingress surfaces |
| CLI submit command -> runtime submit seam | provider-neutral command must not drift from the existing canonical runtime contract |
| backend capability registry -> machine consumers | JSON capability output is consumed by agents and CI as a source of truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | T | `qrun remote submit` option parsing | mitigate | Require `--backend`, enforce the one-input invariant with `expected_exactly_one_input`, and route all successful paths through one `resolve_runtime_input()` call. |
| T-10-03-02 | T | backend capability JSON | mitigate | Set only `remote_submit = True` for IBM, keep `remote_readiness = True`, and preserve explicit tests that `recommended_backend`, `selected_backend`, and `least_busy` are absent. |
| T-10-03-03 | I | CLI JSON payload | mitigate | Pin the success payload to attempt metadata only and rely on runtime-layer secret scrubbing from Plans 10-01 and 10-02; do not echo credential references beyond `instance` and backend/provider identifiers. |
</threat_model>

<verification>
Run `uv run pytest tests/test_cli_remote_submit.py tests/test_cli_backend_list.py -q --maxfail=1`.
</verification>

<success_criteria>
- `qrun remote submit` exists and uses the same ingress selectors as `qrun exec`.
- IBM capability output now truthfully advertises submit support without changing the explicit-selection rule.
- No lifecycle, reopen, cancel, compare parity, pack parity, or terminal finalization work is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/10-canonical-remote-submit-attempt-records/10-03-SUMMARY.md`
</output>
