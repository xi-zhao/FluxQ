---
phase: 02-trusted-revision-artifacts
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/quantum_runtime/runtime/imports.py
  - tests/test_runtime_imports.py
  - tests/test_cli_exec.py
  - tests/test_cli_control_plane.py
  - tests/test_workspace_baseline.py
autonomous: true
requirements:
  - RUNT-03
must_haves:
  truths:
    - "Trusted replay/import paths block when persisted provenance, revision identity, or artifact digest evidence no longer matches the revision being reopened."
    - "Older runs that lack trusted replay metadata remain explicitly legacy-compatible instead of silently being treated as trusted evidence."
    - "Copied-report relocation, legacy relative-path normalization, and baseline reopening still work when their hashes and revision identity remain valid."
  artifacts:
    - path: "src/quantum_runtime/runtime/imports.py"
      provides: "Trusted-versus-legacy replay integrity evaluation and fail-closed import errors"
      min_lines: 420
    - path: "tests/test_runtime_imports.py"
      provides: "Regression coverage for trusted artifact drift, legacy compatibility, and copied-report normalization"
      min_lines: 420
    - path: "tests/test_cli_exec.py"
      provides: "End-to-end report/revision execution coverage for fail-closed trusted replay inputs"
      min_lines: 420
  key_links:
    - from: "src/quantum_runtime/runtime/imports.py"
      to: "tests/test_runtime_imports.py"
      via: "resolve_report_file(...) and resolve_report_revision(...) regression coverage"
      pattern: "resolve_report_file|resolve_report_revision"
    - from: "tests/test_cli_exec.py"
      to: "src/quantum_runtime/runtime/executor.py"
      via: "`qrun exec --report-file/--revision` fail-closed replay tests"
      pattern: "--report-file|--revision"
    - from: "tests/test_cli_control_plane.py"
      to: "src/quantum_runtime/runtime/control_plane.py"
      via: "show/status JSON surfacing of legacy replay integrity without treating it as trusted"
      pattern: "show|status|replay_integrity"
---

<objective>
Tighten replay/import trust semantics so trusted revisions fail closed on integrity drift while legacy revisions remain explicitly distinguishable and compatible.

Purpose: Phase 2 is incomplete until replay/import stops silently proceeding on trusted artifact drift and the runtime surfaces the difference between durable trusted evidence and older legacy-compatible runs.
Output: Import enforcement in `runtime/imports.py` plus runtime, CLI, and baseline regression coverage for trusted drift and legacy compatibility.
</objective>

<execution_context>
@/Users/xizhao/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xizhao/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-trusted-revision-artifacts/02-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@src/quantum_runtime/runtime/imports.py
@src/quantum_runtime/runtime/executor.py
@src/quantum_runtime/runtime/control_plane.py
@src/quantum_runtime/cli.py
@tests/test_runtime_imports.py
@tests/test_cli_exec.py
@tests/test_cli_control_plane.py
@tests/test_workspace_baseline.py

<interfaces>
From `src/quantum_runtime/runtime/imports.py`:
```python
class ImportSourceError(ValueError):
    code: str
    source: str
    details: dict[str, Any]

class ImportResolution(BaseModel):
    revision: str
    report_path: Path
    qspec_path: Path
    replay_integrity: dict[str, Any]
    report_summary: dict[str, Any]
    provenance: dict[str, Any]

def resolve_report_file(report_file: Path, *, workspace_root: Path | None = None) -> ImportResolution: ...
def resolve_report_revision(workspace_root: Path, revision: str) -> ImportResolution: ...
def resolve_workspace_baseline(workspace_root: Path) -> WorkspaceBaselineResolution: ...
```

From `src/quantum_runtime/runtime/executor.py`:
```python
def execute_report(*, workspace_root: Path, report_file: Path, event_sink: EventSink | None = None) -> ExecResult: ...
def load_qspec_from_report(report_file: Path) -> QSpec: ...
```

From `src/quantum_runtime/runtime/control_plane.py`:
```python
def show_revision(*, workspace_root: Path, revision: str | None = None, latest: bool = False) -> ShowResult: ...
def workspace_status(*, workspace_root: Path) -> StatusResult: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add trusted-versus-legacy replay/import regression coverage</name>
  <files>tests/test_runtime_imports.py, tests/test_workspace_baseline.py, tests/test_cli_exec.py, tests/test_cli_control_plane.py</files>
  <read_first>
- .planning/phases/02-trusted-revision-artifacts/02-RESEARCH.md
- src/quantum_runtime/runtime/imports.py
- src/quantum_runtime/runtime/executor.py
- src/quantum_runtime/runtime/control_plane.py
- tests/test_runtime_imports.py
- tests/test_workspace_baseline.py
- tests/test_cli_exec.py
- tests/test_cli_control_plane.py
  </read_first>
  <behavior>
- Test 1: A trusted report/revision with persisted artifact digests blocks resolution when a referenced output is missing or digest-mismatched; the regression should assert a stable `ImportSourceError.code` instead of the current degraded success path.
- Test 2: A legacy report that lacks the trusted digest block still resolves, but its replay metadata is explicitly marked as legacy-compatible rather than trusted.
- Test 3: Baseline reopening keeps working against legacy-compatible report inputs when the stored revision, report hash, and qspec hash still match.
- Test 4: `qrun exec --report-file ... --json` and `qrun exec --revision ... --json` fail closed for trusted artifact drift, while `qrun show --json` surfaces the legacy distinction without turning an otherwise valid legacy run into an error.
  </behavior>
  <action>Extend the existing regression suites before touching the importer implementation. In `tests/test_runtime_imports.py`, convert the current artifact-drift acceptance case into a blocking-trusted case and add a separate legacy-compatibility case for reports with missing `artifact_output_digests`. In `tests/test_cli_exec.py`, add end-to-end report/revision execution regressions that expect exit code `3` plus the new import error code when trusted output artifacts drift. In `tests/test_cli_control_plane.py`, add a show/status regression that confirms legacy runs surface a legacy replay-integrity state rather than silently appearing trusted. In `tests/test_workspace_baseline.py`, add a narrow regression that proves baseline reopening still works for legacy-compatible records with matching hashes.</action>
  <acceptance_criteria>
- `rg -n "test_resolve_report_file_rejects_trusted_artifact_digest_drift|test_resolve_report_file_marks_missing_digest_reports_as_legacy_compatible" tests/test_runtime_imports.py`
- `rg -n "test_qrun_exec_json_rejects_trusted_report_with_artifact_digest_drift|test_qrun_exec_json_rejects_trusted_revision_with_artifact_digest_drift" tests/test_cli_exec.py`
- `rg -n "test_qrun_show_json_exposes_legacy_replay_integrity_without_failing" tests/test_cli_control_plane.py`
- `rg -n "test_workspace_baseline_accepts_legacy_compatible_report_resolution" tests/test_workspace_baseline.py`
  </acceptance_criteria>
  <verify>
    <automated>uv run --python 3.11 --extra dev --extra qiskit pytest -q tests/test_runtime_imports.py tests/test_workspace_baseline.py tests/test_cli_exec.py tests/test_cli_control_plane.py</automated>
  </verify>
  <done>The regressions fail against the current advisory trusted-drift behavior and precisely define the intended trusted-versus-legacy replay contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement fail-closed trusted replay enforcement without regressing legacy or baseline flows</name>
  <files>src/quantum_runtime/runtime/imports.py, tests/test_runtime_imports.py, tests/test_workspace_baseline.py, tests/test_cli_exec.py, tests/test_cli_control_plane.py</files>
  <read_first>
- src/quantum_runtime/runtime/imports.py
- src/quantum_runtime/runtime/executor.py
- src/quantum_runtime/runtime/control_plane.py
- tests/test_runtime_imports.py
- tests/test_workspace_baseline.py
- tests/test_cli_exec.py
- tests/test_cli_control_plane.py
  </read_first>
  <behavior>
- Behavior 1: `_evaluate_replay_integrity(...)` distinguishes trusted replay evidence from legacy-compatible reports with an explicit machine-readable field instead of treating both as equivalent `ok` paths.
- Behavior 2: Trusted reports/revisions raise a blocking `ImportSourceError` when artifact output digests are missing-on-disk or mismatched, while legacy-compatible reports still resolve and retain `status == "legacy"` (or equivalent explicit legacy marker).
- Behavior 3: Copied-report relocation, workspace-prefixed legacy path normalization, and `resolve_workspace_baseline(...)` continue to work when hashes and revision identity match, because the importer preserves the existing provenance normalization logic rather than weakening it.
  </behavior>
  <action>Update `src/quantum_runtime/runtime/imports.py` so replay-integrity evaluation returns an explicit trust classification for trusted versus legacy-compatible report inputs, then enforce blocking failures for trusted artifact drift using stable `ImportSourceError` codes. Keep the existing qspec hash, semantic hash, copied-report relocation, and legacy path normalization logic intact. Preserve baseline compatibility by allowing legacy-compatible resolutions to pass when the baseline's revision/report/qspec hashes still match, and do not add delivery-bundle handling, compare policy flags, or workspace locking logic in this phase.</action>
  <acceptance_criteria>
- `rg -n "trusted|legacy" src/quantum_runtime/runtime/imports.py`
- `rg -n "artifact_outputs_missing|artifact_outputs_mismatched|ImportSourceError" src/quantum_runtime/runtime/imports.py`
- `rg -n "legacy_replay_integrity|artifact_digest_drift" tests/test_runtime_imports.py tests/test_cli_exec.py tests/test_cli_control_plane.py tests/test_workspace_baseline.py`
  </acceptance_criteria>
  <verify>
    <automated>uv run --python 3.11 --extra dev --extra qiskit pytest -q tests/test_runtime_imports.py tests/test_workspace_baseline.py tests/test_cli_exec.py tests/test_cli_control_plane.py</automated>
  </verify>
  <done>Trusted replay/import inputs fail closed on artifact digest drift, legacy-compatible runs remain explicitly distinguishable and reopenable, and baseline plus copied-report compatibility regressions still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted report/manifest/artifact files -> import resolver | Untrusted or tampered workspace artifacts cross into replay/import logic. |
| copied report path / baseline record -> runtime reopen flows | External or relocated paths are normalized back onto a workspace and must not bypass trust validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | T | `src/quantum_runtime/runtime/imports.py` replay-integrity evaluation | mitigate | Convert trusted artifact digest drift from advisory degradation into blocking `ImportSourceError` failures with stable codes. |
| T-02-02-02 | S | copied-report relocation and legacy path normalization | mitigate | Preserve canonical artifact provenance checks and only allow legacy compatibility when provenance normalization plus hash checks still agree. |
| T-02-02-03 | I | baseline reopen path | mitigate | Keep baseline resolution keyed to revision, report hash, and qspec hash so legacy compatibility does not weaken baseline integrity checks. |
</threat_model>

<verification>
Run `uv run --python 3.11 --extra dev --extra qiskit pytest -q tests/test_runtime_imports.py tests/test_workspace_baseline.py tests/test_cli_exec.py tests/test_cli_control_plane.py` after the importer changes land. This covers direct import resolution, legacy compatibility, report/revision execution, and agent-facing JSON surfaces together.
</verification>

<success_criteria>
Trusted replay and import paths reject provenance or artifact-digest drift with blocking errors, while older runs remain explicitly legacy-compatible and baseline plus copied-report workflows continue to reopen matching revisions successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/02-trusted-revision-artifacts/02-02-SUMMARY.md`
</output>
