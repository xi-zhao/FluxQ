---
phase: "07-compare-trust-closure"
plan: "03"
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - tests/test_cli_exec.py
  - tests/test_runtime_imports.py
autonomous: true
requirements:
  - POLC-01
must_haves:
  truths:
    - "exec/import 层自己的回归测试就能在 compare 之前发现 revision coherence 破坏，不必等到 milestone audit 才暴露问题。"
    - "两次 exec 后，`resolve_workspace_current()` 能直接重开最新 revision 的 history 工件并保持 `replay_integrity.status == \"ok\"`。"
    - "故意篡改第二个 revision 的 canonical history 时，trusted import 仍然 fail closed，证明 Phase 07 修的是 producer 而不是 consumer。"
  artifacts:
    - path: "tests/test_cli_exec.py"
      provides: "第二次 exec 后 report/qspec 语义一致性的 CLI 级回归。"
      contains: "rev_000002"
    - path: "tests/test_runtime_imports.py"
      provides: "multi-revision current workspace reopen 与 tamper fail-closed 的 runtime 回归。"
      contains: "resolve_workspace_current"
  key_links:
    - from: "tests/test_cli_exec.py"
      to: "src/quantum_runtime/runtime/executor.py"
      via: "连续两次 `qrun exec` 后直接校验第二个 revision 的 report/qspec coherence"
      pattern: "exec"
    - from: "tests/test_runtime_imports.py"
      to: "src/quantum_runtime/runtime/imports.py"
      via: "current workspace reopen 必须在健康路径通过、在真实篡改路径 fail closed"
      pattern: "resolve_workspace_current"
---

<objective>
把 Phase 07 的跨阶段修复变成 exec/import 自己能守住的回归，而不是把问题继续留给 compare 或后续审计发现。

Purpose: 审计暴露的是 `exec -> compare` seam，但真正的健康边界应该更靠前：producer 写完 revision 后，exec/import 层就应该能证明健康数据可重开、坏数据会 fail closed。本计划只补跨阶段回归 ownership，不扩展新 policy feature。
Output: CLI exec 回归、runtime import 回归，以及最终可直接跑的 Phase 07 全量 gate。
</objective>

<execution_context>
@/Users/xizhao/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xizhao/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/07-compare-trust-closure/07-RESEARCH.md
@.planning/phases/07-compare-trust-closure/07-VALIDATION.md
@.planning/phases/02-trusted-revision-artifacts/02-VERIFICATION.md
@src/quantum_runtime/runtime/executor.py
@src/quantum_runtime/reporters/writer.py
@src/quantum_runtime/runtime/imports.py
@tests/test_cli_exec.py
@tests/test_runtime_imports.py

<interfaces>
From `src/quantum_runtime/runtime/imports.py`:
```python
def resolve_workspace_current(workspace_root: Path) -> ImportResolution: ...
```

From `tests/test_cli_exec.py`:
```python
RUNNER = CliRunner()
result = RUNNER.invoke(app, ["exec", "--workspace", str(workspace), "--intent-file", str(...), "--json"])
```
</interfaces>

本计划必须保持的规则：
- 这是 regression hardening，不是新功能设计；不要新增 compare、baseline、doctor、benchmark 语义。
- 继续保留 Phase 02 的 fail-closed import 合同；新的回归必须同时证明健康路径和损坏路径。
- 优先在现有 `tests/test_cli_exec.py`、`tests/test_runtime_imports.py` 里补强，而不是再造一套旁路测试 harness。
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 在 CLI exec 套件里补上第二次 revision coherence 回归</name>
  <files>tests/test_cli_exec.py</files>
  <read_first>
- .planning/phases/07-compare-trust-closure/07-RESEARCH.md
- .planning/phases/07-compare-trust-closure/07-VALIDATION.md
- src/quantum_runtime/runtime/executor.py
- src/quantum_runtime/reporters/writer.py
- tests/test_cli_exec.py
  </read_first>
  <behavior>
- Test 1: 连续执行 GHZ -> QAOA 后，`reports/history/rev_000002.json` 与 `specs/history/rev_000002.json` 在 `path`、`hash`、`semantic_hash` 上完全一致。
- Test 2: `rev_000001` 的 history report 仍然只绑定 `rev_000001` 的 history qspec，不受第二次 exec 影响。
- Test 3: 若当前套件尚未直接覆盖，补一个最小 replay smoke，证明在两次 exec 之后 `qrun exec --revision rev_000002 --json` 能成功使用健康的第二个 revision。
  </behavior>
  <action>优先扩展现有 `test_qrun_exec_history_report_pins_revision_qspec_after_later_runs`，若该测试过于拥挤则新增独立测试，例如 `test_qrun_exec_second_revision_report_matches_revision_qspec_semantics`。直接读取 `reports/history/rev_000002.json`、`specs/history/rev_000002.json`，重新计算文件 hash 与 `summarize_qspec_semantics(...)`，明确断言第二个 revision 的 report 没有引用旧的 current alias。不要通过 mock 绕开磁盘层；这条回归要从公共 CLI 路径覆盖真实 seam。</action>
  <acceptance_criteria>
- `rg -n "test_qrun_exec_history_report_pins_revision_qspec_after_later_runs|test_qrun_exec_second_revision_report_matches_revision_qspec_semantics" tests/test_cli_exec.py`
- `rg -n "semantic_hash|specs/history/rev_000002.json|reports/history/rev_000002.json" tests/test_cli_exec.py`
  </acceptance_criteria>
  <verify>
    <automated>./.venv/bin/python -m pytest tests/test_cli_exec.py -q --maxfail=1</automated>
  </verify>
  <done>CLI exec 套件能在 compare 之前直接证明第二次 revision 的 report/qspec coherence 已恢复。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 在 runtime imports 套件里同时锁住健康 reopen 与真实篡改 fail-closed</name>
  <files>tests/test_runtime_imports.py</files>
  <read_first>
- .planning/phases/07-compare-trust-closure/07-RESEARCH.md
- .planning/phases/07-compare-trust-closure/07-VALIDATION.md
- src/quantum_runtime/runtime/imports.py
- tests/test_runtime_imports.py
- tests/test_cli_exec.py
  </read_first>
  <behavior>
- Test 1: 连续执行 GHZ -> QAOA 后，`resolve_workspace_current()` 返回 `rev_000002`、`reports/history/rev_000002.json`、`specs/history/rev_000002.json`，且 `replay_integrity.status == "ok"`。
- Test 2: 在 report 已写完后故意替换 `specs/history/rev_000002.json` 为另一个 qspec，`resolve_workspace_current()` 仍抛出 `ImportSourceError("report_qspec_semantic_hash_mismatch")` 或更精确的 qspec hash mismatch。
- Test 3: 新增测试不依赖 current alias fallback；它必须证明多 revision workspace 的健康路径直接由 canonical history 重开。
  </behavior>
  <action>在 `tests/test_runtime_imports.py` 里新增两个明确命名的回归，例如 `test_resolve_workspace_current_after_two_execs_uses_latest_revision_history` 与 `test_resolve_workspace_current_rejects_tampered_second_revision_history`。使用 `execute_intent()` 连续执行两次来覆盖 audit 暴露的真实 seam；先断言健康 reopen 成功，再在同一个 revision 的 canonical history qspec 上做最小篡改，确认 trusted import 仍 fail closed。不要把测试建立在 current alias fallback 或 copied-report relocation 上，这里要锁住的是 canonical history truth layer。</action>
  <acceptance_criteria>
- `rg -n "test_resolve_workspace_current_after_two_execs_uses_latest_revision_history|test_resolve_workspace_current_rejects_tampered_second_revision_history" tests/test_runtime_imports.py`
- `rg -n "report_qspec_semantic_hash_mismatch|report_qspec_hash_mismatch" tests/test_runtime_imports.py`
- `rg -n "rev_000002|replay_integrity\\[\"status\"\\] == \"ok\"" tests/test_runtime_imports.py`
  </acceptance_criteria>
  <verify>
    <automated>./.venv/bin/python -m pytest tests/test_runtime_imports.py tests/test_cli_exec.py -q --maxfail=1</automated>
  </verify>
  <done>runtime imports 套件既能证明 Phase 07 修复后的健康 reopen，又能证明真实 corruption 依旧 fail closed。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| producer changes -> regression suites | 后续阶段若再次破坏 exec/import seam，必须先在测试里被发现。 |
| canonical history -> `resolve_workspace_current()` | current workspace reopen 直接决定 compare/show/export 等读路径是否还能信任最新 revision。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | R | `tests/test_cli_exec.py` / `tests/test_runtime_imports.py` | mitigate | 把断言从“路径存在”提升到“hash + semantic_hash + healthy reopen/fail-closed 双路径”，避免未来 regression 可以靠假路径通过。 |
| T-07-03-02 | D | cross-phase seam ownership | mitigate | 把 audit 暴露的 seam 前移到 exec/import suites；最后一轮验证跑完整 Phase 07 gate，阻止高风险 drift 进入后续阶段。 |
</threat_model>

<verification>
本计划完成后执行完整 Phase 07 gate：

`./.venv/bin/ruff check src tests && ./.venv/bin/python -m mypy src && ./.venv/bin/python -m pytest tests/test_cli_compare.py tests/test_cli_runtime_gap.py tests/test_runtime_compare.py tests/test_runtime_imports.py tests/test_runtime_revision_artifacts.py tests/test_cli_exec.py -q --maxfail=1`
</verification>

<success_criteria>
- exec/import 套件自身就能守住 Phase 07 修复，不必再等 compare 红测或 milestone audit 才发现 seam 回归。
- `resolve_workspace_current()` 在健康多 revision workspace 上恢复正常，在真实篡改上继续 fail closed。
- Phase 07 的最终 gate 覆盖 producer repair、compare 恢复、和 cross-phase regression hardening 三个层面。
</success_criteria>

<output>
After completion, create `.planning/phases/07-compare-trust-closure/07-compare-trust-closure-03-SUMMARY.md`
</output>
