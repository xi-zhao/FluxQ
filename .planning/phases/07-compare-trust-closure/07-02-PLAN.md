---
phase: "07-compare-trust-closure"
plan: "02"
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - src/quantum_runtime/runtime/compare.py
  - src/quantum_runtime/cli.py
  - tests/test_cli_compare.py
  - tests/test_cli_runtime_gap.py
  - tests/test_runtime_compare.py
autonomous: true
requirements:
  - POLC-01
must_haves:
  truths:
    - "健康 revision 上，`qrun compare --baseline --fail-on subject_drift --json` 会返回既有的 `verdict`/`gate`/exit `2`，而不是在 trusted import 阶段提前报错。"
    - "left/right revision compare 重新走到 Phase 04 已定义的 compare policy surface，而不是返回 `report_qspec_semantic_hash_mismatch`。"
    - "真实的 revision 篡改仍然 fail closed 并保持 exit `3`，Phase 07 不会把 artifact corruption 伪装成普通 drift。"
  artifacts:
    - path: "src/quantum_runtime/runtime/compare.py"
      provides: "Baseline/current compare helper and compare result persistence."
      contains: "def compare_workspace_baseline"
    - path: "src/quantum_runtime/cli.py"
      provides: "CLI compare entrypoint that maps policy failure to exit 2 and trust failure to exit 3."
      contains: "@app.command(\"compare\")"
    - path: "tests/test_cli_compare.py"
      provides: "Baseline compare gate regressions, including the new tampered-current negative path."
      contains: "--baseline"
    - path: "tests/test_cli_runtime_gap.py"
      provides: "Milestone gap regression for left/right compare fail-on subject drift."
      contains: "test_qrun_compare_json_fail_on_subject_drift_returns_failed_gate"
    - path: "tests/test_runtime_compare.py"
      provides: "Runtime-level baseline compare contract for saved baseline records."
      contains: "test_compare_workspace_baseline_uses_saved_baseline_record"
  key_links:
    - from: "src/quantum_runtime/runtime/compare.py"
      to: "src/quantum_runtime/runtime/imports.py"
      via: "只有 trusted resolution 成功后才进入 compare policy evaluation"
      pattern: "resolve_workspace_baseline|resolve_workspace_current"
    - from: "src/quantum_runtime/cli.py"
      to: "src/quantum_runtime/runtime/compare.py"
      via: "CLI 输出与 exit 行为必须复用既有 compare helper，而不是包装层自造 verdict"
      pattern: "compare_workspace_baseline|compare_import_resolutions"
---

<objective>
恢复 baseline/current compare gate，让 `POLC-01` 的 live 行为重新回到 Phase 04 已承诺的 policy verdict surface。

Purpose: 这一步只负责让健康 revision 重新走到 compare policy，不能通过放宽 trusted import 来“修复”红测。Phase 07 必须明确区分两条路径：producer 修好后，健康 revision 返回 `verdict=fail`/`gate.ready=false`/exit `2`；真实篡改仍然保持 `ImportSourceError` + exit `3`。**已经被旧 bug 污染的历史 workspace 仍按 corruption 处理，不在本计划自动修复范围内；操作员需要 rerun/rebaseline/manual cleanup 后再期待 compare gate 通过。** 本计划同时保留 `tests/test_cli_compare.py`、`tests/test_cli_runtime_gap.py`、`tests/test_runtime_compare.py` 三层 compare regression coverage，不在本 phase 做去重。
Output: compare/baseline gate 红测转绿、保留 fail-closed 的负向回归、以及在必要时做最小 compare/CLI wiring 修补。
</objective>

<execution_context>
@/Users/xizhao/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xizhao/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/04-policy-acceptance-gates/04-RESEARCH.md
@.planning/phases/04-policy-acceptance-gates/04-VERIFICATION.md
@.planning/phases/07-compare-trust-closure/07-RESEARCH.md
@.planning/phases/07-compare-trust-closure/07-VALIDATION.md
@src/quantum_runtime/runtime/imports.py
@src/quantum_runtime/runtime/compare.py
@src/quantum_runtime/cli.py
@tests/test_cli_runtime_gap.py
@tests/test_cli_compare.py
@tests/test_runtime_compare.py

<interfaces>
From `src/quantum_runtime/runtime/compare.py`:
```python
def compare_workspace_baseline(
    workspace_root: Path,
    *,
    policy: ComparePolicy | None = None,
) -> CompareResult: ...
```

From `src/quantum_runtime/cli.py`:
```python
def compare_command(..., baseline_mode: bool = typer.Option(False, "--baseline"), fail_on: str | None = typer.Option(None, "--fail-on"), json_output: bool = typer.Option(False, "--json")) -> None: ...
```

From `src/quantum_runtime/runtime/imports.py`:
```python
def resolve_workspace_current(workspace_root: Path) -> ImportResolution: ...
def resolve_workspace_baseline(workspace_root: Path) -> WorkspaceBaselineResolution: ...
```
</interfaces>

本计划必须保持的规则：
- 不新增 policy feature；范围只限 `POLC-01` 的 audited integration break。
- 不在 compare 层捕获 `ImportSourceError` 并把 corruption 伪装成 drift verdict。
- 健康路径回到 Phase 04 既有 `ComparePolicy`/`CompareResult` 合同；损坏路径继续由 trusted import fail closed。
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 绿化 compare gate 红测并补上 fail-closed 负向回归</name>
  <files>tests/test_cli_compare.py, tests/test_cli_runtime_gap.py, tests/test_runtime_compare.py</files>
  <read_first>
- .planning/phases/07-compare-trust-closure/07-RESEARCH.md
- .planning/phases/07-compare-trust-closure/07-VALIDATION.md
- .planning/phases/04-policy-acceptance-gates/04-RESEARCH.md
- .planning/phases/04-policy-acceptance-gates/04-VERIFICATION.md
- src/quantum_runtime/runtime/imports.py
- src/quantum_runtime/runtime/compare.py
- src/quantum_runtime/cli.py
- tests/test_cli_runtime_gap.py
- tests/test_cli_compare.py
- tests/test_runtime_compare.py
  </read_first>
  <behavior>
- Test 1: 绿化这三条现有红测，且名称保持不变：`tests/test_cli_runtime_gap.py::test_qrun_compare_json_fail_on_subject_drift_returns_failed_gate`、`tests/test_cli_compare.py::test_qrun_compare_json_baseline_fail_on_subject_drift_returns_failed_gate`、`tests/test_runtime_compare.py::test_compare_workspace_baseline_uses_saved_baseline_record`。
- Test 2: 新增一个 tamper-path CLI 回归，例如 `test_qrun_compare_json_baseline_preserves_fail_closed_on_tampered_current_revision`，故意篡改第二个 revision 的 canonical history qspec/report 后，`qrun compare --baseline --json` 仍返回 exit `3` 和 `report_qspec_semantic_hash_mismatch`（或更精确的 qspec hash mismatch）。
- Test 3: 健康 baseline compare 仍然返回 `baseline.revision == "rev_000001"`、`verdict.failed_checks == ["subject_drift"]`、`gate.ready == false`。
  </behavior>
  <action>把这三条现有红测当成 Phase 07 的主合同，不要重命名或弱化断言。若现有断言仍允许 compare 通过吞掉 trust error 来“变绿”，就在 `tests/test_cli_compare.py` 中补上新的 tamper-path 回归：执行 GHZ、保存 baseline、执行 QAOA，然后故意改写 `specs/history/rev_000002.json` 或 `reports/history/rev_000002.json`，最后断言 compare 仍然 fail closed。runtime helper 侧继续用 `tests/test_runtime_compare.py::test_compare_workspace_baseline_uses_saved_baseline_record` 锁住 baseline record 与 left/right revision 的关系。</action>
  <acceptance_criteria>
- `rg -n "test_qrun_compare_json_fail_on_subject_drift_returns_failed_gate" tests/test_cli_runtime_gap.py`
- `rg -n "test_qrun_compare_json_baseline_fail_on_subject_drift_returns_failed_gate|test_qrun_compare_json_baseline_preserves_fail_closed_on_tampered_current_revision" tests/test_cli_compare.py`
- `rg -n "test_compare_workspace_baseline_uses_saved_baseline_record" tests/test_runtime_compare.py`
- `rg -n "report_qspec_semantic_hash_mismatch|report_qspec_hash_mismatch" tests/test_cli_compare.py`
  </acceptance_criteria>
  <verify>
    <automated>./.venv/bin/python -m pytest tests/test_cli_runtime_gap.py::test_qrun_compare_json_fail_on_subject_drift_returns_failed_gate tests/test_cli_compare.py::test_qrun_compare_json_baseline_fail_on_subject_drift_returns_failed_gate tests/test_runtime_compare.py::test_compare_workspace_baseline_uses_saved_baseline_record tests/test_cli_compare.py -q --maxfail=1</automated>
  </verify>
  <done>compare gate 的健康路径与损坏路径都被测试明确区分：健康 revision 走 policy verdict，真实篡改继续 fail closed。</done>
</task>

<task type="auto">
  <name>Task 2: 仅在必要时做最小 compare/CLI wiring 修补</name>
  <files>src/quantum_runtime/runtime/compare.py, src/quantum_runtime/cli.py</files>
  <read_first>
- src/quantum_runtime/runtime/compare.py
- src/quantum_runtime/cli.py
- src/quantum_runtime/runtime/imports.py
- tests/test_cli_runtime_gap.py
- tests/test_cli_compare.py
- tests/test_runtime_compare.py
  </read_first>
  <behavior>
- Behavior 1: 健康 baseline/current compare 必须在 `resolve_workspace_baseline()` 和 `resolve_workspace_current()` 成功后进入既有 `ComparePolicy` verdict 流程。
- Behavior 2: policy failure 仍映射到 exit `2`；trusted import failure 仍映射到 exit `3`。
- Behavior 3: compare surface 不新增 wrapper-only 语义，也不把 `ImportSourceError` 降级成普通 drift。
  </behavior>
  <action>在完成 `07-01` 后先运行本计划 Task 1 的 compare suite。只有当健康路径仍然缺失 baseline metadata、compare history 持久化错误、或 exit/verdict 映射仍不符合 Phase 04 合同时，才修改 `src/quantum_runtime/runtime/compare.py` 或 `src/quantum_runtime/cli.py`。若需要改动，必须保持 `compare_workspace_baseline()`/`compare_import_resolutions()` 的职责边界清晰：resolution 与 replay integrity 继续留在 `imports.py`，compare 只消费已解析的 trusted inputs。禁止在 compare 层新增“捕获 `ImportSourceError` 后继续比较”的分支。</action>
  <acceptance_criteria>
- `rg -n "def compare_workspace_baseline|resolve_workspace_baseline\\(|resolve_workspace_current\\(" src/quantum_runtime/runtime/compare.py`
- `rg -n "except ImportSourceError" src/quantum_runtime/cli.py`
- `bash -lc '! rg -n "except ImportSourceError" src/quantum_runtime/runtime/compare.py'`
  </acceptance_criteria>
  <verify>
    <automated>./.venv/bin/python -m pytest tests/test_cli_runtime_gap.py::test_qrun_compare_json_fail_on_subject_drift_returns_failed_gate tests/test_cli_compare.py::test_qrun_compare_json_baseline_fail_on_subject_drift_returns_failed_gate tests/test_runtime_compare.py::test_compare_workspace_baseline_uses_saved_baseline_record tests/test_cli_compare.py -q --maxfail=1</automated>
  </verify>
  <done>compare/baseline gate 恢复到 Phase 04 合同，并且没有通过降级 trusted import 错误来掩盖 producer corruption。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| saved baseline record -> compare verdict | CI 会直接把 FluxQ compare 的 baseline/current 结果当成 policy decision 依据。 |
| trusted import failure -> CLI exit mapping | artifact corruption 必须继续在 policy evaluation 之前被阻断，且以稳定的 machine-readable error 对外暴露。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | T | `src/quantum_runtime/runtime/compare.py` | mitigate | 禁止在 compare 层捕获 `ImportSourceError` 并继续比较；用 tamper-path 回归固定真实 corruption 仍为 exit `3`。 |
| T-07-02-02 | R | baseline/current compare contract | mitigate | 用现有三条 audited 红测加一个 tamper 负向测试同时固定 baseline metadata、`failed_checks`、`gate`、exit code。 |
</threat_model>

<verification>
本计划的 compare gate 验证命令：

`./.venv/bin/python -m pytest tests/test_cli_runtime_gap.py::test_qrun_compare_json_fail_on_subject_drift_returns_failed_gate tests/test_cli_compare.py::test_qrun_compare_json_baseline_fail_on_subject_drift_returns_failed_gate tests/test_runtime_compare.py::test_compare_workspace_baseline_uses_saved_baseline_record tests/test_cli_compare.py -q --maxfail=1`
</verification>

<success_criteria>
- `POLC-01` 的 live baseline/current compare 行为重新回到 `verdict`/`gate`/exit `2`。
- left/right compare 与 baseline compare 都不再被 `report_qspec_semantic_hash_mismatch` 提前截断。
- 对真实篡改的 fail-closed 行为被显式保留下来，而不是被 compare 包装层吞掉。
- compare gate 的三层现有 regression 在 Phase 07 继续保留并转绿，不以测试去重替代 trust-closure 修复。
- 已被旧 bug 污染的 workspace 继续以 fail-closed 方式暴露问题；本计划只恢复修复后/重跑后的健康 compare 路径。
</success_criteria>

<output>
After completion, create `.planning/phases/07-compare-trust-closure/07-compare-trust-closure-02-SUMMARY.md`
</output>
