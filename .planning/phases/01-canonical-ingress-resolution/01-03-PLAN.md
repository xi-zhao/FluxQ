---
phase: 01-canonical-ingress-resolution
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - tests/test_qspec_semantics.py
  - tests/test_cli_ingress_resolution.py
autonomous: true
gap_closure: true
requirements:
  - INGR-01
  - INGR-02
  - INGR-03
must_haves:
  truths:
    - "The semantic-hash regression artifact now meets the planned coverage contract with substantive tests instead of a verifier override."
    - "CLI ingress regressions prove true raw prompt parity and stay independent of the repository working directory."
  artifacts:
    - path: "tests/test_qspec_semantics.py"
      provides: "Expanded semantic identity regression coverage for equivalent and execution-distinct workloads"
      min_lines: 80
    - path: "tests/test_cli_ingress_resolution.py"
      provides: "CLI ingress regressions that use true raw prompt text and isolated filesystem execution"
  key_links:
    - from: "tests/test_qspec_semantics.py"
      to: "src/quantum_runtime/qspec/semantics.py"
      via: "direct calls to summarize_qspec_semantics"
      pattern: "summarize_qspec_semantics"
    - from: "tests/test_cli_ingress_resolution.py"
      to: "src/quantum_runtime/cli.py"
      via: "CliRunner.invoke(app, ...) inside isolated filesystem coverage"
      pattern: "RUNNER\\.invoke\\(app,"
---

<objective>
Close the remaining Phase 1 verification gap by strengthening the semantic-hash regression artifact and tightening the adjacent CLI ingress tests the verifier flagged as underfit.

Purpose: The failed verifier check is an execution miss in test coverage, not a product-behavior regression, so the clean fix is to add substantive tests rather than override the plan contract.
Output: One gap-closure regression slice that raises `tests/test_qspec_semantics.py` above the planned threshold and hardens raw-prompt/no-write CLI evidence.
</objective>

<execution_context>
@/Users/xizhao/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xizhao/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-canonical-ingress-resolution/01-VERIFICATION.md
@.planning/phases/01-canonical-ingress-resolution/01-02-PLAN.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@src/quantum_runtime/qspec/semantics.py
@src/quantum_runtime/intent/parser.py
@src/quantum_runtime/intent/planner.py
@src/quantum_runtime/cli.py
@tests/test_qspec_semantics.py
@tests/test_cli_ingress_resolution.py
@tests/golden/qspec_ghz.json
@tests/golden/qspec_qaoa_maxcut.json
@examples/intent-ghz.md
@examples/intent-qaoa-maxcut-sweep.md

<interfaces>
From `src/quantum_runtime/qspec/semantics.py`:
```python
def summarize_qspec_semantics(qspec: QSpec) -> dict[str, Any]: ...
```

From `src/quantum_runtime/intent/parser.py`:
```python
def parse_intent_file(path: Path) -> IntentModel: ...
def parse_intent_text(text: str) -> IntentModel: ...
def parse_intent_json_text(text: str) -> IntentModel: ...
```

From `src/quantum_runtime/intent/planner.py`:
```python
def plan_to_qspec(intent: IntentModel) -> QSpec: ...
```

From `tests/test_cli_ingress_resolution.py`:
```python
def _assert_workspace_artifacts_absent(workspace: Path) -> None: ...
def _invoke_json_command(*args: str) -> dict[str, object]: ...
def _plan_parity_block(payload: dict[str, object]) -> dict[str, object]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand semantic identity regression coverage past the plan threshold</name>
  <files>tests/test_qspec_semantics.py</files>
  <read_first>
- tests/test_qspec_semantics.py
- src/quantum_runtime/qspec/semantics.py
- src/quantum_runtime/intent/parser.py
- src/quantum_runtime/intent/planner.py
- tests/golden/qspec_ghz.json
- tests/golden/qspec_qaoa_maxcut.json
- .planning/phases/01-canonical-ingress-resolution/01-VERIFICATION.md
  </read_first>
  <action>Extend `tests/test_qspec_semantics.py` with substantive semantic-summary cases instead of filler lines. Keep the existing QAOA equivalence and distinct-workload assertions, then add two concrete regressions: first, a true raw-prompt GHZ equivalence path that builds three equivalent `QSpec` objects from `examples/intent-ghz.md`, structured JSON derived from that markdown file, and the exact raw prompt string `Build a 4-qubit GHZ circuit and measure all qubits.`; second, an execution-settings divergence case that deep-copies one canonical GHZ `QSpec`, changes execution-only fields such as `backend_preferences` and `constraints.shots`, and asserts `workload_id` plus `workload_hash` stay equal while `execution_hash` and `semantic_hash` change. Use `QSpec.model_copy(deep=True)` or the repository's equivalent deep-copy pattern rather than hand-building new models. The file must remain a focused semantic contract test and end above 80 real lines.</action>
  <acceptance_criteria>
- `rg -n "test_summarize_qspec_semantics_keeps_raw_prompt_ghz_hashes_aligned|test_summarize_qspec_semantics_changes_execution_hash_without_changing_workload_hash" tests/test_qspec_semantics.py`
- `rg -n "Build a 4-qubit GHZ circuit and measure all qubits\\.|model_copy\\(deep=True\\)|constraints\\.shots|backend_preferences" tests/test_qspec_semantics.py`
- `wc -l tests/test_qspec_semantics.py` reports at least `80`
  </acceptance_criteria>
  <verify>
    <automated>uv run --python 3.11 --extra dev --extra qiskit pytest -q tests/test_qspec_semantics.py</automated>
  </verify>
  <done>`tests/test_qspec_semantics.py` now clears the planned artifact threshold and proves both equivalent-ingress identity stability and workload-versus-execution hash separation.</done>
</task>

<task type="auto">
  <name>Task 2: Harden CLI ingress regressions around raw prompt parity and cwd isolation</name>
  <files>tests/test_cli_ingress_resolution.py</files>
  <read_first>
- tests/test_cli_ingress_resolution.py
- src/quantum_runtime/cli.py
- tests/test_cli_control_plane.py
- examples/intent-ghz.md
- examples/intent-qaoa-maxcut-sweep.md
- .planning/phases/01-canonical-ingress-resolution/01-VERIFICATION.md
  </read_first>
  <action>Update `tests/test_cli_ingress_resolution.py` so the no-write coverage no longer relies on the repository cwd and the raw prompt path is truly prompt-only. Move the `resolve` and `plan` no-write tests into `RUNNER.isolated_filesystem(temp_dir=tmp_path)` blocks, create their `workspace` paths under that isolated root, and keep using absolute example-file paths so the tests prove dry-run behavior from a non-repo working directory. Then add one explicit raw-prompt parity regression using the exact GHZ prompt string `Build a 4-qubit GHZ circuit and measure all qubits.` plus `examples/intent-ghz.md` and a structured JSON file derived from that markdown intent. Compare the existing `_plan_parity_block(...)` output across `--intent-text`, `--intent-file`, and `--intent-json-file`. Keep the current QAOA sweep parity tests in place because they still cover the richer planner surface.</action>
  <acceptance_criteria>
- `rg -n "isolated_filesystem|test_qrun_plan_json_keeps_raw_prompt_markdown_json_parity_for_ghz" tests/test_cli_ingress_resolution.py`
- `rg -n "Build a 4-qubit GHZ circuit and measure all qubits\\.|examples/intent-ghz\\.md" tests/test_cli_ingress_resolution.py`
- `rg -n "test_qrun_resolve_json_is_side_effect_free|test_qrun_plan_json_is_side_effect_free" tests/test_cli_ingress_resolution.py`
  </acceptance_criteria>
  <verify>
    <automated>uv run --python 3.11 --extra dev --extra qiskit pytest -q tests/test_cli_ingress_resolution.py</automated>
  </verify>
  <done>The CLI ingress regression suite now proves raw prompt parity with a real prompt string and stays green when invoked from an isolated non-repo filesystem.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw prompt and intent fixtures -> canonical `QSpec` summary | Equivalent ingress forms must collapse into the same semantic identity without source-format drift. |
| CLI dry-run commands -> cwd and workspace filesystem | `prompt`, `resolve`, and `plan` must not mutate workspace state or depend on the ambient repo directory. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | T | `summarize_qspec_semantics` contract | mitigate | Add regression tests that keep `workload_hash` stable across execution-only changes and align raw-prompt/file/json identity for equivalent GHZ inputs. |
| T-01-03-02 | T | CLI dry-run ingress tests | mitigate | Run `resolve` and `plan` no-write tests inside `CliRunner.isolated_filesystem(...)` and add a raw-prompt GHZ parity test at the public CLI boundary. |
</threat_model>

<verification>
Run `uv run --python 3.11 --extra dev --extra qiskit pytest -q tests/test_qspec_semantics.py tests/test_cli_ingress_resolution.py tests/test_runtime_ingress_resolution.py tests/test_cli_control_plane.py`, then confirm `wc -l tests/test_qspec_semantics.py` reports at least `80`.
</verification>

<success_criteria>
Phase 1 has one executable gap-closure slice, no override file is needed, and the verifier can close the remaining artifact contract using stronger automated evidence rather than paperwork.
</success_criteria>

<output>
After completion, create `.planning/phases/01-canonical-ingress-resolution/01-03-SUMMARY.md`
</output>
