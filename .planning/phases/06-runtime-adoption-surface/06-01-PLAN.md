---
phase: "06-runtime-adoption-surface"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/releases/v0.3.1.md
  - tests/test_release_docs.py
autonomous: true
requirements:
  - SURF-01
must_haves:
  truths:
    - "新读者从 README 和 `docs/releases/v0.3.1.md` 看到的是一致的 runtime/control-plane 叙事，而不是 generator demo 叙事。"
    - "README `## First Run` 与 release notes `## What To Try First` 提供同一条可照抄的首跑链路：先 baseline，再 compare，再 `doctor --ci`，最后才进入 delivery handoff。"
    - "仓库入口面会把读者引到现有 adoption asset，而不是把一组互相矛盾的命令留在顶层。"
  artifacts:
    - path: "README.md"
      provides: "顶层定位、首跑链路、文档入口。"
      contains: "qrun pack-inspect --pack-root .quantum/packs/rev_000001 --json"
    - path: "docs/releases/v0.3.1.md"
      provides: "与 README 对齐的 release quickstart。"
      contains: "qrun compare --workspace .quantum --baseline --fail-on subject_drift --json"
    - path: "tests/test_release_docs.py"
      provides: "README 与 release notes 一致性回归锁。"
      contains: "test_readme_and_release_notes_share_one_runtime_quickstart"
  key_links:
    - from: "README.md"
      to: "docs/releases/v0.3.1.md"
      via: "相同的 GHZ first-run/runtime handoff 命令链"
      pattern: "qrun baseline set --workspace \\.quantum --revision rev_000001 --json"
    - from: "README.md"
      to: "docs/fluxq-qaoa-maxcut-case-study.md"
      via: "Docs 索引把入口读者引到现有 case study"
      pattern: "docs/fluxq-qaoa-maxcut-case-study.md"
---

<objective>
统一顶层 messaging、quickstart 和 release-facing adoption surface，完整交付 SURF-01。

Purpose: Phase 06 的首要缺口不是再发明产品定位，而是修掉 README 与 release notes 里首跑流程的自相矛盾，让入口读者第一眼看到的是 agent-first runtime control plane，并且能照抄一条不会自打脸的命令链。
Output: 修正后的 `README.md`、一致的 `docs/releases/v0.3.1.md` quickstart，以及锁定这些 public surface 的 release-doc tests。
</objective>

<execution_context>
@/Users/xizhao/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xizhao/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-runtime-adoption-surface/06-RESEARCH.md
@.planning/phases/06-runtime-adoption-surface/06-VALIDATION.md
@README.md
@docs/releases/v0.3.1.md
@docs/product-strategy.md
@ARCHITECTURE.md
@.planning/phases/01-canonical-ingress-resolution/01-01-SUMMARY.md
@.planning/phases/04-policy-acceptance-gates/04-policy-acceptance-gates-04-SUMMARY.md
@.planning/phases/05-verified-delivery-bundles/05-verified-delivery-bundles-03-SUMMARY.md
@tests/test_release_docs.py

本计划只允许修改 docs/tests surface：`README.md`、release notes、对应回归测试。不要修改 `src/quantum_runtime/`、`pyproject.toml` 或 integration asset；这些分别属于后续 Plan 02/03。
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先把 README 与 release notes 的 runtime-first quickstart 合同锁进测试</name>
  <files>tests/test_release_docs.py</files>
  <read_first>
- .planning/phases/06-runtime-adoption-surface/06-RESEARCH.md
- README.md
- docs/releases/v0.3.1.md
- tests/test_release_docs.py
- .planning/phases/04-policy-acceptance-gates/04-policy-acceptance-gates-04-SUMMARY.md
- .planning/phases/05-verified-delivery-bundles/05-verified-delivery-bundles-03-SUMMARY.md
  </read_first>
  <behavior>
- 测试 1：README `## First Run` 必须包含一条完整且顺序正确的 GHZ adoption loop：`prompt -> resolve -> init -> plan -> exec -> baseline set -> compare --baseline --fail-on subject_drift -> doctor --json --ci -> pack -> pack-inspect -> pack-import`。
- 测试 2：README 中 `baseline set` 的索引位置早于 `compare --baseline`，`compare --baseline --fail-on subject_drift --json` 早于 `doctor --workspace .quantum --json --ci`，`pack-inspect` 早于 `pack-import`。
- 测试 3：`docs/releases/v0.3.1.md` 的 `## What To Try First` 使用同一条 GHZ first-run，不再混入旧的 `qrun prompt "Build a 4-qubit MaxCut QAOA ansatz with 2 layers on a ring graph" --json`，也不再把 `doctor --jsonl --fix` 当首跑 gate。
- 测试 4：README `## Docs` 必须显式列出 `docs/fluxq-qaoa-maxcut-case-study.md`，让入口 surface 能指向现有 adoption asset。
  </behavior>
  <action>在 `tests/test_release_docs.py` 新增一个聚焦函数 `test_readme_and_release_notes_share_one_runtime_quickstart()`，不要把顺序断言继续塞进现有超长 smoke test。该新测试要读取 `README.md` 与 `docs/releases/v0.3.1.md`，用 `.index(...)` 锁定以下精确字符串的相对顺序：`qrun baseline set --workspace .quantum --revision rev_000001 --json`、`qrun compare --workspace .quantum --baseline --fail-on subject_drift --json`、`qrun doctor --workspace .quantum --json --ci`、`qrun pack --workspace .quantum --revision rev_000001 --json`、`qrun pack-inspect --pack-root .quantum/packs/rev_000001 --json`、`qrun pack-import --pack-root .quantum/packs/rev_000001 --workspace downstream/.quantum --json`。同时加入负向断言，明确禁止旧的 MaxCut prompt 行和 `qrun doctor --workspace .quantum --jsonl --fix` 继续出现在 release `What To Try First`。</action>
  <acceptance_criteria>
- `rg -n "test_readme_and_release_notes_share_one_runtime_quickstart" tests/test_release_docs.py`
- `rg -n "qrun compare --workspace \\.quantum --baseline --fail-on subject_drift --json|qrun doctor --workspace \\.quantum --json --ci|qrun pack-inspect --pack-root \\.quantum/packs/rev_000001 --json|qrun pack-import --pack-root \\.quantum/packs/rev_000001 --workspace downstream/\\.quantum --json" tests/test_release_docs.py`
- `rg -n "Build a 4-qubit MaxCut QAOA ansatz with 2 layers on a ring graph|doctor --workspace \\.quantum --jsonl --fix" tests/test_release_docs.py`
  </acceptance_criteria>
  <verify>
    <automated>./.venv/bin/python -m pytest tests/test_release_docs.py -q --maxfail=1</automated>
  </verify>
  <done>README 与 release notes 的矛盾 quickstart 已经先在测试里被描述成可失败的 public contract。</done>
</task>

<task type="auto">
  <name>Task 2: 修正文档入口面，输出一条可直接采用的 runtime/control-plane 首跑链路</name>
  <files>README.md, docs/releases/v0.3.1.md</files>
  <read_first>
- README.md
- docs/releases/v0.3.1.md
- docs/product-strategy.md
- ARCHITECTURE.md
- tests/test_release_docs.py
- .planning/phases/01-canonical-ingress-resolution/01-01-SUMMARY.md
- .planning/phases/04-policy-acceptance-gates/04-policy-acceptance-gates-04-SUMMARY.md
- .planning/phases/05-verified-delivery-bundles/05-verified-delivery-bundles-03-SUMMARY.md
  </read_first>
  <behavior>
- 行为 1：README 与 release notes 都把 FluxQ 描述成 `agent-first quantum runtime CLI` / `runtime control plane`，并保留现有 `Natural language is ingress, not the source of truth.` 叙事。
- 行为 2：README `## First Run` 使用一条 GHZ-first 的命令链，明确展示 `baseline set` 早于 `compare --baseline --fail-on subject_drift --json`，并用 `doctor --workspace .quantum --json --ci` 作为 policy gate。
- 行为 3：release notes `## What To Try First` 与 README 对齐，不再把 GHZ 与 QAOA prompt 混在一个首跑块里，不再鼓励在 first run 阶段先执行 `doctor --fix`。
- 行为 4：README `## Docs` 新增 `docs/fluxq-qaoa-maxcut-case-study.md`，但不要在本计划里新增 doc 文件；Plan 02 会深化 adoption workflow。
  </behavior>
  <action>在 `README.md` 中只更新 `## First Run`、紧随其后的解释段落、以及 `## Docs` 列表。把现有 `## First Run` 命令块替换为下面这组精确命令，并在命令块后新增一句解释：`This first supported path is prompt/resolve -> init/plan/exec -> baseline -> compare -> doctor --ci -> pack -> pack-inspect -> pack-import.` 命令块必须是：`qrun prompt "Build a 4-qubit GHZ circuit and measure all qubits." --json`、`qrun resolve --workspace .quantum --intent-file examples/intent-ghz.md --json`、`qrun init --workspace .quantum --json`、`qrun plan --workspace .quantum --intent-file examples/intent-ghz.md --json`、`qrun exec --workspace .quantum --intent-file examples/intent-ghz.md --jsonl`、`qrun baseline set --workspace .quantum --revision rev_000001 --json`、`qrun compare --workspace .quantum --baseline --fail-on subject_drift --json`、`qrun doctor --workspace .quantum --json --ci`、`qrun pack --workspace .quantum --revision rev_000001 --json`、`qrun pack-inspect --pack-root .quantum/packs/rev_000001 --json`、`qrun pack-import --pack-root .quantum/packs/rev_000001 --workspace downstream/.quantum --json`。在 `docs/releases/v0.3.1.md` 中只更新 `## What To Try First`，使用相同的 GHZ-first 链路，删除旧的 QAOA prompt 行，并把 `qrun doctor --workspace .quantum --jsonl --fix` 改成 `qrun doctor --workspace .quantum --json --ci`。不要在本计划里改 `CHANGELOG.md`、`docs/versioning.md`、`pyproject.toml` 或 integration asset。</action>
  <acceptance_criteria>
- `rg -n "qrun compare --workspace \\.quantum --baseline --fail-on subject_drift --json|qrun doctor --workspace \\.quantum --json --ci|qrun pack-inspect --pack-root \\.quantum/packs/rev_000001 --json|qrun pack-import --pack-root \\.quantum/packs/rev_000001 --workspace downstream/\\.quantum --json" README.md docs/releases/v0.3.1.md`
- `rg -n "This first supported path is prompt/resolve -> init/plan/exec -> baseline -> compare -> doctor --ci -> pack -> pack-inspect -> pack-import\\." README.md`
- `rg -n "docs/fluxq-qaoa-maxcut-case-study.md" README.md`
- `rg -n "Build a 4-qubit MaxCut QAOA ansatz with 2 layers on a ring graph|doctor --workspace \\.quantum --jsonl --fix" docs/releases/v0.3.1.md`
  </acceptance_criteria>
  <verify>
    <automated>./.venv/bin/python -m pytest tests/test_release_docs.py -q --maxfail=1</automated>
  </verify>
  <done>顶层入口面与 release surface 对齐成一条不自相矛盾的 runtime-first quickstart，并被测试锁住。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README / release notes 文本 -> adopter 首次执行命令 | 文档中的命令顺序会直接决定用户是否先建立 baseline、是否走 CI gate、是否在 handoff 前先验证 bundle。 |
| 顶层产品定位文案 -> 包装/采用判断 | 入口 surface 的 wording 会影响读者把 FluxQ 当 runtime control plane 还是 code generator demo。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | T | README / release quickstart | mitigate | 用测试锁定 `baseline set -> compare --fail-on -> doctor --ci -> pack -> pack-inspect -> pack-import` 的精确顺序，阻止再次出现 broken first-run。 |
| T-06-01-02 | R | 顶层产品 messaging | mitigate | 保持 `agent-first quantum runtime CLI` / `runtime control plane` / `Natural language is ingress` 三个核心短语，并删除 release 首跑里的 generator-style 混搭命令。 |
| T-06-01-03 | D | adopter 首次采用流程 | mitigate | 在 README 与 release notes 中给出一条可照抄的最短支持路径，避免读者因矛盾命令而卡在首次运行。 |
</threat_model>

<verification>
执行本计划后，至少运行：

`./.venv/bin/python -m pytest tests/test_release_docs.py -q --maxfail=1`
</verification>

<success_criteria>
- SURF-01 的顶层 public surface 有明确 owner。
- README 与 `docs/releases/v0.3.1.md` 不再提供互相矛盾的首跑流程。
- 顶层入口面把 FluxQ 说成 runtime/control-plane 产品，并把读者引到现有 adoption asset。
</success_criteria>

<output>
完成后创建 `.planning/phases/06-runtime-adoption-surface/06-runtime-adoption-surface-01-SUMMARY.md`
</output>
