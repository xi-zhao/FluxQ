---
phase: 09-ibm-access-backend-readiness
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/quantum_runtime/cli.py
  - src/quantum_runtime/runtime/contracts.py
  - src/quantum_runtime/runtime/ibm_access.py
  - src/quantum_runtime/runtime/__init__.py
  - tests/test_cli_ibm_config.py
autonomous: true
requirements:
  - AUTH-01
user_setup:
  - service: ibm_quantum_platform
    why: "可选的真实 IBM smoke 和 CI auth 路径需要用户自己的 IBM Quantum Platform 凭证与实例。"
    env_vars:
      - name: QISKIT_IBM_TOKEN
        source: "IBM Quantum Platform / IBM Cloud API key"
    dashboard_config:
      - task: "找到要绑定的 IBM Quantum Platform instance 标识"
        location: "IBM Quantum Platform instance 页面"
must_haves:
  truths:
    - "用户可以通过 `qrun ibm configure` 非交互式写入 IBM profile 引用。"
    - "`.quantum/qrun.toml` 只保存非 secret IBM access 引用，不保存 token 本体。"
    - "仓库安装面通过 optional `ibm` extra 暴露 `qiskit-ibm-runtime~=0.46`。"
  artifacts:
    - path: "pyproject.toml"
      provides: "IBM optional extra"
      contains: "qiskit-ibm-runtime~=0.46"
    - path: "src/quantum_runtime/runtime/ibm_access.py"
      provides: "IBM profile 读写与 access 解析合同"
      exports:
        - "IbmAccessProfile"
        - "IbmAccessResolution"
        - "IbmConfigureResult"
        - "load_ibm_profile"
        - "write_ibm_profile"
        - "resolve_ibm_access"
    - path: "tests/test_cli_ibm_config.py"
      provides: "IBM configure / non-secret persistence 回归覆盖"
      contains: "qrun ibm configure"
  key_links:
    - from: "src/quantum_runtime/cli.py"
      to: "src/quantum_runtime/runtime/ibm_access.py"
      via: "qrun ibm configure 调用 profile 写入与 access 解析"
      pattern: "ibm.*configure|write_ibm_profile"
    - from: "src/quantum_runtime/runtime/ibm_access.py"
      to: ".quantum/qrun.toml"
      via: "WorkspacePaths.qrun_toml"
      pattern: "qrun_toml|remote.ibm|instance"
---

<objective>
建立 IBM access 的最小可信基础：安装面、非 secret profile 合同、以及 provider-specific CLI `qrun ibm configure`。

Purpose: 先把 AUTH-01 的配置入口和 secrets 边界钉死，后续 `doctor` 与 `backend list` 才能复用同一 profile/access 解析面。
Output: optional `ibm` extra、`runtime/ibm_access.py`、`qrun ibm configure`、以及 `tests/test_cli_ibm_config.py`
</objective>

<execution_context>
@/Users/xizhao/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xizhao/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/phases/09-ibm-access-backend-readiness/09-RESEARCH.md
@.planning/phases/09-ibm-access-backend-readiness/09-VALIDATION.md
@pyproject.toml
@src/quantum_runtime/cli.py
@src/quantum_runtime/runtime/__init__.py
@src/quantum_runtime/workspace/manager.py
@src/quantum_runtime/workspace/paths.py

<interfaces>
From `src/quantum_runtime/workspace/manager.py`:

```python
DEFAULT_QRUN_TOML = """[workspace]
default_exports = ["qiskit", "qasm3"]
history_limit = 50
"""
```

From `src/quantum_runtime/cli.py`:

```python
backend_app = typer.Typer(add_completion=False, help="Backend discovery helpers.")
app.add_typer(backend_app, name="backend")

@backend_app.command("list")
def backend_list_command(json_output: bool = typer.Option(False, "--json")) -> None: ...
```

From `src/quantum_runtime/runtime/contracts.py`:

```python
class ErrorPayload(SchemaPayload):
    status: Literal["error"] = "error"
    reason: str
    error_code: str
    remediation: str = DEFAULT_REMEDIATION
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立 IBM access 合同与 Wave 0 依赖</name>
  <files>pyproject.toml, src/quantum_runtime/runtime/ibm_access.py, src/quantum_runtime/runtime/__init__.py, tests/test_cli_ibm_config.py</files>
  <read_first>
- pyproject.toml
- src/quantum_runtime/runtime/__init__.py
- src/quantum_runtime/workspace/manager.py
- src/quantum_runtime/workspace/paths.py
- .planning/phases/09-ibm-access-backend-readiness/09-RESEARCH.md
- .planning/phases/09-ibm-access-backend-readiness/09-VALIDATION.md
  </read_first>
  <behavior>
- 测试 1: `write_ibm_profile()` 只把 `channel`, `credential_mode`, `instance`, `token_env` 或 `saved_account_name` 写进 `qrun.toml`。
- 测试 2: `resolve_ibm_access()` 在缺少 `instance` 或错误 credential 组合时返回稳定 reason code，而不是回落到隐式默认行为。
- 测试 3: `pyproject.toml` 暴露 `ibm` optional extra，值为 `qiskit-ibm-runtime~=0.46`。
  </behavior>
  <action>在 `pyproject.toml` 增加 `[project.optional-dependencies].ibm = ["qiskit-ibm-runtime~=0.46"]`，不要把 IBM SDK 提升为 base dependency；这对应 09-VALIDATION 的 Wave 0 安装要求。新建 `src/quantum_runtime/runtime/ibm_access.py`，定义 `IbmAccessProfile`, `IbmAccessResolution`, `IbmConfigureResult` 与 `load_ibm_profile()`, `write_ibm_profile()`, `resolve_ibm_access()`；`channel` 固定为 `ibm_quantum_platform`，`credential_mode` 仅允许 `env` 或 `saved_account`，`instance` 必填，token 只能以 env var 名称引用存在。`resolve_ibm_access()` 需要返回 IBM SDK 可用性、profile 是否配置、解析出的 `auth_source`、`instance`、以及失败时的稳定 `reason_code`；这里不要调用 submit/poll/cancel/finalize，也不要引入 backend 选择逻辑。通过 `src/quantum_runtime/runtime/__init__.py` 导出上述模型/函数，让 09-02 和 09-03 直接复用，不要通过 `runtime.__init__` 反向导入 `contracts.py` 造成循环依赖。新建 `tests/test_cli_ibm_config.py`，至少覆盖 non-secret profile 写入、`instance` 必填、以及 raw token 不会出现在 `.quantum/qrun.toml`；用 `tmp_path` 和真实文件回读，不要 mock 掉 workspace 文件系统。</action>
  <acceptance_criteria>
- `rg -n 'qiskit-ibm-runtime~=0\\.46' pyproject.toml` 能定位 IBM optional extra。
- `rg -n 'class IbmAccessProfile|class IbmAccessResolution|def write_ibm_profile|def resolve_ibm_access' src/quantum_runtime/runtime/ibm_access.py` 命中。
- `test -f tests/test_cli_ibm_config.py` 成功。
- `uv run pytest tests/test_cli_ibm_config.py -k 'profile or resolution' -q --maxfail=1` 退出码为 0。
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_cli_ibm_config.py -k 'profile or resolution' -q --maxfail=1</automated>
  </verify>
  <done>IBM extra、profile/access 合同和 Wave 0 测试基座已经存在，后续计划可在不重新设计 secrets 边界的前提下复用。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 `qrun ibm configure` 的非 secret 配置入口</name>
  <files>src/quantum_runtime/cli.py, src/quantum_runtime/runtime/contracts.py, src/quantum_runtime/runtime/ibm_access.py, tests/test_cli_ibm_config.py</files>
  <read_first>
- src/quantum_runtime/cli.py
- src/quantum_runtime/runtime/contracts.py
- src/quantum_runtime/runtime/ibm_access.py
- tests/test_cli_ibm_config.py
- .planning/phases/09-ibm-access-backend-readiness/09-RESEARCH.md
  </read_first>
  <behavior>
- 测试 1: `qrun ibm configure --credential-mode env --token-env QISKIT_IBM_TOKEN --instance <crn> --workspace <path> --json` 写入的只有 env 名称与 `instance`。
- 测试 2: `qrun ibm configure --credential-mode saved_account --saved-account-name fluxq-dev --instance <crn> --json` 写入 saved-account 引用，不写 token。
- 测试 3: 互斥或缺失参数通过稳定 error payload 失败，退出码保持为 3。
  </behavior>
  <action>在 `src/quantum_runtime/cli.py` 中新增 `ibm_app = typer.Typer(...)` 并通过 `app.add_typer(ibm_app, name="ibm")` 注册 provider-specific 子命名空间；实现 `@ibm_app.command("configure")`，参数至少包含 `--workspace`, `--credential-mode`, `--instance`, `--token-env`, `--saved-account-name`, `--json`。命令体必须调用 `write_ibm_profile()`，输出 `IbmConfigureResult`，并复用 `_echo_json()` / `_emit_json_payload()` 走现有 schema-versioned CLI 边界。同步在 `src/quantum_runtime/runtime/contracts.py` 增加本计划需要的 remediation，例如 `ibm_config_invalid`, `ibm_instance_required`, `ibm_token_external_required`，使 CLI 错误能给出可执行 remediation。不要新增 login flow、不要把 token 作为 CLI 参数持久化、不要写入 `.quantum/` 之外的额外 IBM secret 文件。</action>
  <acceptance_criteria>
- `rg -n 'ibm_app = typer\\.Typer|app\\.add_typer\\(ibm_app, name=\"ibm\"\\)|@ibm_app\\.command\\(\"configure\"\\)' src/quantum_runtime/cli.py` 命中。
- `rg -n 'ibm_config_invalid|ibm_instance_required|ibm_token_external_required' src/quantum_runtime/runtime/contracts.py` 命中。
- `rg -n 'credential-mode|saved-account-name|token-env' tests/test_cli_ibm_config.py` 命中。
- `uv run pytest tests/test_cli_ibm_config.py -q --maxfail=1` 退出码为 0。
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_cli_ibm_config.py -q --maxfail=1</automated>
  </verify>
  <done>用户已经有稳定的 IBM 配置入口，且 `.quantum/qrun.toml` 只保留 IBM access 引用与 `instance`，不保存凭证本体。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI flags → `.quantum/qrun.toml` | 不可信输入跨入持久化配置边界 |
| `.quantum/qrun.toml` → JSON 输出 | 持久化 profile 被重新投影为 agent/CI 可消费 payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | I | `qrun ibm configure` / `ibm_access.py` | mitigate | 仅持久化 `credential_mode`, `token_env`, `saved_account_name`, `instance`, `channel`；测试回读 `qrun.toml` 并断言 raw token 不出现。 |
| T-09-01-02 | T | `.quantum/qrun.toml` IBM profile block | mitigate | `instance` 设为必填；`credential_mode` 只允许 `env` / `saved_account`；参数组合不合法时返回稳定 error payload。 |
| T-09-01-03 | S | provider/channel selection | mitigate | `channel` 固定为 `ibm_quantum_platform`，不允许通过配置切到其它 provider。 |
</threat_model>

<verification>
- `uv run pytest tests/test_cli_ibm_config.py -q --maxfail=1`
- `uv run ruff check src tests`
- `uv run python -m mypy src`
</verification>

<success_criteria>
- `qrun ibm configure` 存在且可非交互式运行。
- `pyproject.toml` 暴露 `ibm` optional extra。
- `.quantum/qrun.toml` 保存 `instance` 与 credential reference，但不保存 token 本体。
</success_criteria>

<output>
After completion, create `.planning/phases/09-ibm-access-backend-readiness/09-01-SUMMARY.md`
</output>
