# Codebase Structure

**Analysis Date:** 2026-04-14

## Directory Layout

```text
Qcli/
├── src/
│   ├── quantum_runtime/          # Main Python package shipped by `pyproject.toml`
│   ├── quantum_runtime.egg-info/ # Local setuptools metadata
│   └── <many empty sibling dirs> # Present in the worktree, but no live source files were detected there
├── tests/                        # pytest suite plus golden snapshots
├── examples/                     # Sample markdown intent inputs
├── docs/                         # Product notes, release notes, and planning docs
├── integrations/                 # Host integration examples
├── scripts/                      # Local bootstrap and verification helpers
├── .github/workflows/            # CI and optional Classiq workflow definitions
├── aionrs/                       # Adjacent Rust crate
├── .planning/                    # Tracked GSD planning and codebase-map artifacts
├── .quantum/                     # Local runtime workspace generated by `qrun`
├── dist/                         # Built wheel and sdist artifacts
└── README.md                     # Product and workflow entry documentation
```

## Directory Purposes

**`src/`:**
- Purpose: Source root for the Python package and local build metadata.
- Contains: `src/quantum_runtime/`, `src/quantum_runtime.egg-info/`, and many empty sibling directories such as `src/commands/`, `src/components/`, and `src/tools/` that do not currently contain live FluxQ source files.
- Key files: `src/quantum_runtime/cli.py`, `src/quantum_runtime/__init__.py`

**`src/quantum_runtime/`:**
- Purpose: Shipped `quantum-runtime` package.
- Contains: CLI entrypoint, runtime orchestration, workspace helpers, intent parsing, canonical `QSpec` models, emitters, diagnostics, reporters, backend integrations, shared error types, and artifact-provenance helpers.
- Key files: `src/quantum_runtime/cli.py`, `src/quantum_runtime/runtime/executor.py`, `src/quantum_runtime/runtime/imports.py`, `src/quantum_runtime/workspace/manager.py`, `src/quantum_runtime/qspec/model.py`, `src/quantum_runtime/artifact_provenance.py`

**`src/quantum_runtime/runtime/`:**
- Purpose: CLI-independent orchestration and read-model layer.
- Contains: `control_plane.py`, `executor.py`, `imports.py`, `compare.py`, `inspect.py`, `export.py`, `pack.py`, `doctor.py`, `policy.py`, `contracts.py`, `observability.py`, `run_manifest.py`, `backend_registry.py`, `backend_list.py`, `exit_codes.py`, and the barrel `__init__.py`.
- Key files: `src/quantum_runtime/runtime/control_plane.py`, `src/quantum_runtime/runtime/executor.py`, `src/quantum_runtime/runtime/imports.py`, `src/quantum_runtime/runtime/compare.py`, `src/quantum_runtime/runtime/run_manifest.py`

**`src/quantum_runtime/workspace/`:**
- Purpose: File-layout schema, locking, revision metadata, baselines, and trace persistence.
- Contains: `manager.py`, `paths.py`, `manifest.py`, `baseline.py`, `locking.py`, `trace.py`, and the barrel `__init__.py`.
- Key files: `src/quantum_runtime/workspace/manager.py`, `src/quantum_runtime/workspace/paths.py`, `src/quantum_runtime/workspace/manifest.py`, `src/quantum_runtime/workspace/locking.py`

**`src/quantum_runtime/intent/`:**
- Purpose: Ingress parsing and rule-based intent lowering.
- Contains: `parser.py`, `markdown.py`, `structured.py`, `planner.py`, and the barrel `__init__.py`.
- Key files: `src/quantum_runtime/intent/parser.py`, `src/quantum_runtime/intent/planner.py`, `src/quantum_runtime/intent/structured.py`

**`src/quantum_runtime/qspec/`:**
- Purpose: Canonical IR definitions, normalization, semantic hashing, observables, and parameter workflow helpers.
- Contains: `model.py`, `validation.py`, `semantics.py`, `observables.py`, `parameter_workflow.py`, and the barrel `__init__.py`.
- Key files: `src/quantum_runtime/qspec/model.py`, `src/quantum_runtime/qspec/validation.py`, `src/quantum_runtime/qspec/semantics.py`

**`src/quantum_runtime/lowering/`, `src/quantum_runtime/diagnostics/`, and `src/quantum_runtime/backends/`:**
- Purpose: Artifact generation and runtime evidence.
- Contains: Qiskit, QASM3, and Classiq emitters in `src/quantum_runtime/lowering/`; simulation, diagrams, resource estimates, transpile validation, and benchmarks in `src/quantum_runtime/diagnostics/`; optional synthesis backend integration in `src/quantum_runtime/backends/classiq_backend.py`.
- Key files: `src/quantum_runtime/lowering/qiskit_emitter.py`, `src/quantum_runtime/lowering/qasm3_emitter.py`, `src/quantum_runtime/lowering/classiq_emitter.py`, `src/quantum_runtime/diagnostics/simulate.py`, `src/quantum_runtime/diagnostics/benchmark.py`, `src/quantum_runtime/backends/classiq_backend.py`

**`src/quantum_runtime/reporters/`:**
- Purpose: Serialize stable report artifacts and human-readable summaries.
- Contains: `writer.py`, `summary.py`, and the barrel `__init__.py`.
- Key files: `src/quantum_runtime/reporters/writer.py`, `src/quantum_runtime/reporters/summary.py`

**`tests/`:**
- Purpose: Flat pytest suite covering CLI behavior, runtime services, workspace persistence, exports, packaging, and release surface.
- Contains: `test_cli_*.py`, `test_runtime_*.py`, `test_workspace_*.py`, feature-specific tests such as `tests/test_artifact_provenance.py`, and `tests/golden/` fixtures.
- Key files: `tests/test_cli_exec.py`, `tests/test_runtime_imports.py`, `tests/test_runtime_compare.py`, `tests/test_workspace_locking.py`, `tests/test_pack_bundle.py`, `tests/golden/qspec_ghz.json`

**`examples/`:**
- Purpose: Sample markdown intents for manual runs, demos, and test fixtures.
- Contains: GHZ and QAOA examples.
- Key files: `examples/intent-ghz.md`, `examples/intent-qaoa-maxcut.md`, `examples/intent-qaoa-maxcut-sweep.md`

**`docs/`:**
- Purpose: Product strategy, versioning notes, release notes, and implementation plans.
- Contains: release notes under `docs/releases/`, planning documents under `docs/plans/`, and integration or strategy docs.
- Key files: `docs/product-strategy.md`, `docs/versioning.md`, `docs/aionrs-integration.md`, `docs/releases/v0.3.1.md`

**`integrations/`:**
- Purpose: Host-side integration examples and handoff files.
- Contains: `integrations/aionrs/CLAUDE.md.example` and `integrations/aionrs/hooks.example.toml`
- Key files: `integrations/aionrs/CLAUDE.md.example`, `integrations/aionrs/hooks.example.toml`

**`aionrs/`:**
- Purpose: Adjacent Rust crate for external agent CLI and provider integration work.
- Contains: `Cargo.toml`, `src/`, `tests/`, and its own `.github/workflows/`
- Key files: `aionrs/Cargo.toml`

## Key File Locations

**Entry Points:**
- `pyproject.toml`: Package metadata plus the `qrun = "quantum_runtime.cli:main"` console script.
- `src/quantum_runtime/cli.py`: Typer command surface for every shipped CLI command.
- `scripts/dev-bootstrap.sh`: Local install and verification entrypoint.
- `.github/workflows/ci.yml`: Main CI entrypoint.
- `.github/workflows/classiq.yml`: Optional Classiq-only workflow.

**Configuration:**
- `pyproject.toml`: Dependencies, setuptools discovery, pytest config, and Ruff config.
- `mypy.ini`: Type-check configuration.
- `README.md`: Supported commands, workspace layout, and usage flow.
- `AGENTS.md`: Repository-scoped working instructions for agents.

**Core Logic:**
- `src/quantum_runtime/runtime/control_plane.py`: Resolve, plan, status, show, and schema payloads.
- `src/quantum_runtime/runtime/executor.py`: Exec pipeline, revision reservation, artifact writes, alias promotion, and manifest commit.
- `src/quantum_runtime/runtime/imports.py`: Report or revision reopening, provenance normalization, and replay-integrity checks.
- `src/quantum_runtime/runtime/compare.py`: Semantic, report, backend, and replay-integrity comparison.
- `src/quantum_runtime/runtime/export.py`: Artifact re-export from current or resolved runtime inputs.
- `src/quantum_runtime/runtime/inspect.py`: Compact machine-readable workspace inspection.
- `src/quantum_runtime/runtime/pack.py`: Portable revision bundle assembly.
- `src/quantum_runtime/runtime/run_manifest.py`: Immutable per-revision join record.
- `src/quantum_runtime/artifact_provenance.py`: Canonical artifact path normalization shared by report writing and imports.

**Testing:**
- `tests/test_cli_*.py`: CLI contract and output-mode coverage.
- `tests/test_runtime_*.py`: Runtime module behavior.
- `tests/test_workspace_*.py`: Workspace lifecycle, locking, and baselines.
- `tests/golden/`: Golden artifacts for emitters and summaries.

## Naming Conventions

**Files:**
- Runtime source files use `snake_case.py`, for example `src/quantum_runtime/runtime/executor.py`, `src/quantum_runtime/workspace/manager.py`, and `src/quantum_runtime/qspec/model.py`.
- CLI and runtime tests use `tests/test_<area>.py`, for example `tests/test_cli_exec.py`, `tests/test_runtime_imports.py`, and `tests/test_workspace_locking.py`.
- Golden snapshots live in `tests/golden/`, for example `tests/golden/qiskit_ghz_main.py` and `tests/golden/qspec_qaoa_maxcut.json`.
- Example intents use `examples/intent-*.md`.
- Release notes use `docs/releases/v*.md`, and planning notes use `docs/plans/YYYY-MM-DD-*.md`.
- Unsuffixed implementation files are the live extension points. Side-copy files such as `src/quantum_runtime/reporters/writer-NSConflict-BlovedSwami-mac26.4.1.py` and `tests/test_runtime_imports-NSConflict-BlovedSwami-mac26.4.1.py` exist in the tree but are not the files re-exported by package barrels.

**Directories:**
- Domain packages live under `src/quantum_runtime/<area>/`, for example `runtime/`, `workspace/`, `intent/`, `qspec/`, `lowering/`, `diagnostics/`, `backends/`, and `reporters/`.
- Workspace history follows `<artifact>/history/` conventions under `.quantum/`, for example `reports/history/`, `specs/history/`, `manifests/history/`, `events/history/`, and `trace/history/`.
- Tests stay flat under `tests/` rather than mirroring the source package directory tree one-to-one.

## Where to Add New Code

**New Feature:**
- Primary code: `src/quantum_runtime/runtime/` for orchestration and read models, plus `src/quantum_runtime/cli.py` if the feature adds or changes a command.
- Tests: `tests/test_cli_<feature>.py` for CLI behavior and `tests/test_runtime_<feature>.py` for runtime behavior.

**New Component or Module:**
- Implementation: `src/quantum_runtime/intent/` for ingress parsing or lowering rules, `src/quantum_runtime/qspec/` for IR or semantic rules, `src/quantum_runtime/workspace/` for filesystem or locking logic, and `src/quantum_runtime/lowering/` or `src/quantum_runtime/diagnostics/` for emitters or evidence-producing logic.

**Utilities:**
- Shared helpers: `src/quantum_runtime/artifact_provenance.py`, `src/quantum_runtime/errors.py`, or a narrow helper module inside the package area that owns the behavior.

**Placement Guidance:**
- Put new FluxQ runtime code under `src/quantum_runtime/`. No live FluxQ source files were detected in sibling directories under `src/`, so do not extend `src/commands/`, `src/components/`, `src/tools/`, or similar empty directories for Python runtime work.
- Prefer adding package exports through existing barrels such as `src/quantum_runtime/runtime/__init__.py`, `src/quantum_runtime/workspace/__init__.py`, `src/quantum_runtime/qspec/__init__.py`, and `src/quantum_runtime/reporters/__init__.py` when the new surface is intended to be public within the package.

## Special Directories

**`.planning/`:**
- Purpose: Tracked planning docs and codebase-map artifacts consumed by GSD workflows.
- Generated: Mixed
- Committed: Yes

**`.quantum/`:**
- Purpose: Local runtime workspace with revisioned run artifacts and event logs.
- Generated: Yes
- Committed: No tracked files detected

**`src/quantum_runtime.egg-info/`:**
- Purpose: Setuptools metadata from local editable installs and builds.
- Generated: Yes
- Committed: No tracked files detected

**`dist/`:**
- Purpose: Wheel and sdist build outputs from `python -m build`.
- Generated: Yes
- Committed: No tracked files detected

**`.worktrees/`:**
- Purpose: Local parallel worktree clones used during development.
- Generated: Yes
- Committed: No tracked files detected

**`aionrs/`:**
- Purpose: Adjacent Rust project with its own source tree and CI.
- Generated: No
- Committed: Yes

---

*Structure analysis: 2026-04-14*
